# 端侧大模型部署

按章依次讲：导出 → 变轻 → 转换 Runtime → 量稳验收 → 交付。〇 是第 0 站，接着 ① 到 ⑯。后文再碰到已讲过的点，用章号点一下出处即可。

## 〇 先认端侧

### 端 · 近边 · 云：三个词先对齐

评审里最常翻车的，是把「能 ping 通的局域网盒子」或 MEC 应用写成「端侧」。三个词混着说，后面选型、验收、故障回落都会对不上。工程上先问：**数据在哪产生、算力归谁管、失败回落哪一层**。

**端（on-device）** — 数据产生/消费处的终端或嵌入式节点：本地有算力、存储、传感器与 OS 进程预算。落点：手机、车机 SoC、机器人盒、摄像头旁侧盒。本仓库叫**端侧**。权重进本机 DRAM；默认不靠跨城回程；杀进程、温控、前台保活都是硬约束。本机摘要只读通知文本、在 Neural Engine / Hexagon / 本地 NPU 跑完、原始内容不出壳——这才是端。验收：同机冷/热可加载；断网仍能完成约定任务子集；峰值 RSS 低于可用 DRAM（经验留 ≥15–20% OS/前台余量）。别把「能 ping 通局域网盒子」或 MEC 应用写成端侧。

**近边（MEC / access-edge）** — 接入网边缘的虚拟化宿主。ETSI GS MEC 003：MEC host = 平台 + 算力/存储/网络。落点：基站旁机房、城域 PoP、园区边缘节点。本仓库叫**近边**，不要写成端侧。ITU-T Y.3540（2023）：边缘计算落在「终端接入的网络边缘」处理来自/发往终端的数据；宿主仍是运营商/园区可控机房。例子：车队重模型放园区 PoP，车端做 VAD + 短上下文 SLM，超时再上推。验收看 RTT+排队是否进时延预算，编排主权、镜像版本、失败回落写进部署图。near-edge 是口语，不是 ETSI 正式术语；机柜贴「边缘」不等于有近边能力。

**云** — 集中式算力与长周期数据面，跳数更多、信任域更广。落点：公有云 Region、中心机房。适合训练、长尾开放域推理、跨用户聚合。Apple Private Cloud Compute 接端上 ~3B 不够的复杂请求，开放域长工具链仍走中心。验收：失败策略、密钥与审计归属、金丝雀与回滚。别用云端 70B 分数证明「手机同款体验」，也别把云延迟当端侧验收数。

ITU-T Y.4509（2025）把设备–边缘–云写成推理/训练**协作**架构。本章「端侧模型部署」默认指设备本体；近边与云只在切流/回落时出现。

| 层 | 工程口径 | 典型落点 | 本仓库叫法 |
|----|----------|----------|------------|
| 端 | 数据产生/消费处；本地算力与传感器 | 手机、车机、旁侧盒 | **端侧** |
| 近边 | MEC host + 管理面 | 基站旁、园区 PoP | **近边** |
| 云 | 集中式长周期数据面 | Region / 中心机房 | **云** |

### 信任边界 ≠ 物理边界

机柜贴了「边缘」不等于数据还在你手里。评审里常见翻车：PPT 只画「边缘节点」，却说不清密钥谁管、日志往哪流、断链后回落到哪——现场撞的往往不是算力，是主权说不清。

所以要分开两件事：信任边界问谁持钥、谁编排、谁审计、故障归谁；物理边界只问机柜在哪。同一机房可以同时有 MEC 宿主与 Edge Cloud。ETSI GR MEC 035：Edge Cloud 常与运营商同址，却可在 MNO 控制域之外，与 MEC 可协同、**不是同一信任边界**。共址不等于同域。

合规、密钥、运维、故障域跟信任域走。密钥交给不可控方，等于把「近边低时延」卖成「数据已离端」。

园区机房 GPU 距产线 5 ms RTT：若公有云账号编排、日志默认出园区，信任域是云。运营商 MEC 跑在基站旁、编排计费在 MNO 域内，才按近边立约。Apple Intelligence 日常 ~3B on-device、复杂走 Private Cloud Compute——分的是能力与信任策略，不是机柜距离。

部署图建议画三列：**算力归属 / 数据驻留 / 编排主权**；每条路径写清监督方、失败回落、密钥在哪。断近边链路时，端侧应降到本地规则/缓存，不能静默全量上云。拒收：机柜标签当信任域；「上了边缘就合规」；MEC 与第三方 Edge Cloud 混钥混日志却共用一张验收表。

### 设备–边缘–云是协作面，不是替代关系

Y.4509 把 AI 协作拆成推理协作与训练协作。端不够可以上推边/云，也可以端侧预处理后再传特征。按时延、算力、隐私切，别口号「全部上云」或「全部端侧」。

端侧 DRAM/带宽/功耗是硬顶；云吃长尾与重推理。切分表要写清：哪类原始数据允许离端、何时上推、失败怎么降。没有切分表的「端边云一体」过不了评审。

座舱例子：端上 1.7B–3B INT4 跟嘴短答；近边放多车共享较大模型；云做地图/知识长尾与夜间训练。端 TTFT P95 超预算、或意图判开放域长推理且策略允许特征离端 → 上推；麦克风 PCM 默认不出壳，只传文本/embedding。

每条路径要有触发条件、数据白名单、超时与回退；压测含弱网/断网；OTA 整组（端模型 + 近边镜像 + 策略表）可回滚。拒收：只有架构图；口头「再走云」无超时鉴权；训练回传伪装成遥测。

### 边界决策四问

答不清就别开「边缘」工单。

1. 原始数据能否离端？不能 → 端推理或端预处理。
2. 时延预算是否小于跨城 RTT + 排队？否 → 近边或端。
3. 模型/适配器更新与回滚谁负责？说不清 → 边界未定。
4. 故障时能否降到「仅本地规则/缓存」？不能 → 不要宣称高可用边缘。

每一问对应一张可签字的表：数据类别、SLA、版本列车、降级剧本。评审抽一问答不上，整包退回。

### 为什么要把推理挤上设备

云上顺：机器多、内存大、挂了重启。同一份权重进手机、盒子、车机，常见首帧不出、内存顶满、多路排队。硬顶是算力、内存、带宽、功耗，还要按节拍跑。

动机是约束，不是「端侧更强」。云仍适合训练、长尾复杂推理、全局知识；端侧适合短路径、可验收的本地任务。

| 动机 | 现场一句话 | 锚点（写清条件，别当成保证） |
|------|------------|--------------------------|
| 隐私 | 日常任务不出壳 | Apple（2024）：日常约 **~3B** 端上，更复杂走 Private Cloud Compute |
| 延迟 | 首字要跟手 | iPhone 15 Pro 约 **0.6 ms/prompt-token**、约 **30 tok/s**（未开 speculative；厂商栈） |
| 成本 | 单位会话更便宜 | Phi-3：on-device / offline / 成本敏感 |
| 离线 | 断网仍干活 | 车机与工业盒验收必含离线路径 |

隐私：原始音视频与个人上下文尽量留设备。数据流图标「原始/特征/日志/遥测」；默认关训练上传。拒收：「本地 AI 更隐私」却全量日志回传。

延迟：交互与控制对首包/持续时延敏感，回程抖动直接打穿预算。本机测 TTFT / tok/s，不抄云 Demo。拒收：只给 Region 延迟；把 Prefill 摊进 Decode 报「最高 tok/s」。

成本：产线每班数千次短指令，事事上云时专线与 API 费可超过本机 NPU 折旧。对照端推理电费 vs 云 API+回程。拒收：只比单次、不算峰值并发与重试。

离线：座舱、产线、飞行模式必须能干活。拔网线用例必过；缩 ctx / 切小模 / 规则兜底要可测。拒收：宣称离线却依赖首次联网拉权重。

### 时延与可用性

时延拆两段：**TTFT**（首 token 可展示）与 **Decode 稳态 tok/s**。可用性：弱网/断网/热降频后还能不能做完或降级。CV 的 FPS 搬不过来；LLM 必须 Prefill / Decode 分表。

用户骂的是「第一口」和「说着说着卡死」，不是宣传 TOPS。近边/云吃回程抖动、排队、握手；端上吃冷启动、DVFS、共板抢带宽。

推演：首字预算 < 300 ms，prompt = 256 token。若 Prefill 约 0.6 ms/prompt-token（Apple 公开自测，厂商栈），256 × 0.6 ≈ 154 ms，还剩语音前端与渲染；跨城 RTT 中位 40 ms、排队 P95 再 80 ms，云路径仅网络就可能吃大半预算。Decode 互动门槛常约 **3–10 tok/s**；旗舰 ~3B 量化常见落在十余到约三十 tok/s（厂商表条件，别当成保证）。

验收：固定 prompt 长与 past-KV；TTFT P50/P95；Decode 热稳态 P50/P95；冷/热分列；座舱对齐 VAD→首字；断网与降频后的缩 ctx/切小模。拒收：用 Prefill kernel 时间顶替 TTFT；BS>1 聚合吞吐冒充单用户；只报冷峰值隐瞒热趴；无离线却宣称高可用。

### 数据最小化（技术解读，非法条）

处理限于目的所必要。设计默认：少采、少存、少传、少副本、及时删。端侧少外传是手段之一，**不等于自动合规**——只把数据流与开关说清楚，不替代法务。

「模型在本地」≠「隐私已完成」。泄漏口常在：全量日志、崩溃 dump 含上下文、遥测默认开、校准集上传无指纹与目的绑定。

输入法改写：端上只读当前编辑框；上云只传脱敏片段与任务 id，禁止默认回传前后 2 KB。座舱：PCM 默认端侧 ASR；上传仅文本意图或固定维 embedding；训练语音单独开关与保留期。

数据流图四级：**原始 / 特征 / 日志 / 遥测**——每级写目的、保留期、出端条件、默认开/关。门禁：默认关训练上传；导出包与遥测通道分开签名。拒收：「本地更隐私」却全量回传，或把端侧当采集前端；调试日志进量产；特征与原始同 topic 混传。

### 产业已把「可挤上设备」做成产品线

选型看参数量、量化位宽、峰值 RSS、最长上下文、授权与安全对齐、失败上云策略。云端大模型分数证明不了手机同款体验。

**Apple** — 端上约 **~3B**；日常端上，复杂走 PCC。公开自测：iPhone 15 Pro 约 **0.6 ms/prompt-token**、约 **30 tok/s**（未 speculative）。偏系统短任务（摘要、改写、实体），不是开放域全能聊天。验收：系统 API、包增量、热稳态；PCC 延迟别算进「端上」。

**Phi-3-mini** — 约 **3.8B**；4-bit 约 **1.8 GB**；iPhone 14 / A16 离线 **>12 tok/s**（Microsoft 报告条件）。ONNX Runtime 有移动路径；用例含 on-device / offline / 成本敏感。验收：业务集掉点 + 同机 tok/s，不单看 MMLU。

**Llama 3.2 1B/3B** — 面向 edge/mobile。量化版（ExecuTorch，OnePlus 12 等）：相对 BF16 约 **2–4×**，体积约 **−56%**，RSS 约 **−41%**。写清引擎、后端、ctx、开关。

**Qwen2.5** — 0.5B / 1.5B / 3B 作端上或 draft；3B 标 mobile 友好。中文业务集 + 工具调用 JSON 合法率；draft+target 投机时两模版本绑定。

| 产品线 | 量级 | 条件写清的公开数 | 选型注意 |
|--------|------|------------------|----------|
| Apple | ~3B | 15 Pro：~0.6 ms/prompt-token，~30 tok/s | 系统栈；非全能 chatbot |
| Phi | ~3.8B | 4-bit≈1.8 GB；A16 >12 tok/s | ONNX/移动 |
| Llama 3.2 | 1B/3B | 量化约 2–4×，体积≈−56%，内存≈−41% | 写清 ExecuTorch/后端 |
| Qwen2.5 | 0.5B–3B | 3B mobile；更小作 draft | 中文与工具调用单测 |

### 何时不要硬挤上设备

- 开放域长推理、重度工具编排、超大私有库检索，端侧上下文/内存不够。
- 模型更新与安全补丁无法在端侧可靠分发与回滚。
- 「隐私」其实是审计/跨境，却无数据分类与驻留策略。
- 唯一证据是营销对比图，没有同机对照基准。

厂商「最高 xx tok/s / xx TOPS」是峰值叙事。课堂只认**写清模型、量化、上下文、是否热稳态**的数字。细则 ⑬-9 / ⑬-12。

端侧是约束下的默认选项，不是能力天花板；云与近边是协作后备，不是口头补丁。

### 访存墙：端侧 LLM 的主矛盾

端侧 LLM 多数时候卡在**权重和 KV 搬不动**，宣传 TOPS 不够用。

### 访存墙是什么

对话主路径是 **batch≈1 的自回归 Decode**：每出 1 token，大体要把常驻权重（再加随上下文增长的 KV）从慢存扫一遍。算术强度 \(I \approx \mathrm{FLOPs}/B_{\mathrm{moved}}\)。Cambricon-LLM（2024）：INT8、单 batch decode 约 **2 OP/byte**，比常见 CNN/BERT 低约 **30×–100×** → 算力空转，墙在搬。

CNN 一张图算很多、搬相对少；LLM decode 每步算得不多，却整份权重再搬一遍。「NPU 很强」换不来「对话跟手」——先问搬得动吗。

### 为何访存是主矛盾

Prefill 更像**算力墙**（矩阵大、可并行），主导 TTFT。Decode 更像**带宽墙**（每 token 重读权重），主导持续出字。TOPS 宣传主要抬 Prefill，**兑不成**对话 tok/s。

arXiv:2410.03613：Hexagon NPU 上 Llama-2-7B Prefill 可远高于 CPU/GPU，Decode **仅略优**——现场证据。量化在端侧常为**少搬字节**；无匹配 kernel 时解量化回高位，带宽收益蒸发。

两段杠杆不同。给 Decode 堆 FLOPs 宣传、给 Prefill 只压权重却不看首字，都会空转。

### Prefill 与 Decode：墙的差异

**Prefill** — 整段 prompt 一次（或 chunked）前向，填 KV，算力墙，主导 TTFT。**Decode** — 逐 token，每步重读权重，带宽墙，主导持续出字。

验收出三列：Prefill ms（或 TTFT）、单步 Decode ms、生成长度 × 单步。VLM 把视觉 token 单独进 Prefill。投机采样抬 Decode 有效 tok/s，修不了长 prompt 的 TTFT。

坑：两段混报一个「最高 tok/s」；Prefill 优化回应「说话卡」；Decode 带宽优化回应「首字慢」。

### 访存墙怎么量

| 表计 / 公式 | 用途 |
|-------------|------|
| \(I \approx \mathrm{FLOPs}/B_{\mathrm{moved}}\) | 是否 memory-bound |
| \(BW_{\mathrm{eff}} / \mathrm{bytes\_per\_token}\) | decode 理论上界（权重常驻时） |
| 稳态 tok/s、带宽利用率、结温、DVFS | sustained，不看冷峰值 |
| TTFT vs P50/P95 tok/s @ 固定 past-KV | 两段分表 |

3B INT4 Decode（理想权重项）：每 token 权重流量 ≈ \(3\times10^9 \times 0.5\,\mathrm{B} = 1.5\,\mathrm{GB}\)（未计 KV/激活/框架）。理论上界 ≈ \(BW_{\mathrm{eff}} / 1.5\,\mathrm{GB}\)。旗舰 LPDDR5X 峰值约 **60–77 GB/s** → 理想约 **40–51 tok/s**；有效带宽按 50–70% 再加 KV，现场常十几～三十多 tok/s——与 Apple ~3B 约 30 tok/s 同量级可对读，条件不同勿比绝对值。Orin ~**200 GB/s** → 同公式理想约 **~133 tok/s**（3B INT4）；Edge-LLM 表上 8B INT4、BS=1、past-KV=2048 约数十 tok/s。反例：Llama-70B INT8 ≈ 70 GB；UFS 4.0 ≈4 GB/s 纯 flash 卸载理论上界约 **0.06 tok/s**（Cambricon-LLM），远低于互动 **3–10 tok/s**。

课堂先问：每 token 搬多少、有效带宽多少、温区能否 sustained。再谈 TOPS。细则 ⑬-9 / ⑬-12。

### 访存墙常见误判

1. 「NPU xx TOPS → 对话一定快」→ 先问有效带宽与权重是否常驻。
2. 「能加载 = 能交付」→ 装进 DRAM 只过容量门；带宽/温区不过则演示绿、现场趴。
3. 「MCU / TinyML 也是端侧 AI」→ MLPerf Tiny 通常 **小于 100 KB**，不是 GB 级 LLM。
4. 「量化只为省体积」→ 端侧更大收益常是少搬字节；无匹配 kernel 时收益蒸发。

### 访存墙锚点（条件写清）

| 锚点 | 条件 | 数 |
|------|------|----|
| 算术强度 ≈2 OP/byte | Cambricon-LLM；INT8；单 batch decode | 相对 CNN/BERT 低 30×–100× |
| Phi-3-mini | 3.8B；4-bit ≈**1.8 GB**；iPhone 14 / A16 | 离线 **>12 tok/s** |
| Orin Edge-LLM | AGX Orin 64GB；INT4 AWQ；BS=1；past KV=2048 | Qwen3-4B ≈**50.7**；8B ≈**30.6 tok/s**（厂商表） |
| 骁龙 | 8 Gen 3；Llama-2-7B 系 | AI Hub QNN w4a16 ≈**12.85 tok/s**（S24，ctx=1024）；营销「最高约 20」须追问条件 |
| MLPerf Tiny | IC/VWW/KWS 等 | 通常 **小于 100 KB**；勿当 LLM 尺子 |

### 发展：四级台阶（可度量）

把「能演示」拆成四道门。营销绿表不算过关。

| 台阶 | 当时算赢 | 今天还要补 | 课堂计量 |
|------|----------|------------|----------|
| L1 能加载 | 文件进设备、出一帧 | 格式对 ≠ 能交付 | 峰值 < 可用 DRAM；OOM 即拒 |
| L2 能量化塞入 | INT4/W4A16 跑通 | 精度和速度同表 | 体积、峰值内存、业务掉点 |
| L3 工具链可复现 | 转换→Runtime→Profiling 可重复 | 按站排障 | SoC + 引擎版本 + 导出指纹 |
| L4 交付门禁 | 共板账、指纹、验签 | 演示绿不够 | p50/p95 TTFT、稳态 tok/s、温控掉速、崩溃/OOM |

### L1 能加载（容量门）

固定 SoC 冷启动可加载；峰值 < 可用 DRAM（经验留 ≥15–20%）；无系统杀进程。一开即死、只能短 ctx、换机型必炸——拒收。格式对得上 ≠ 交付；「FP16 能跑」不代表量化路径存在。装不进，后面优化都是空谈。下一站仍是 ①（硬件与 Roofline）；体积公式 ⑬-9。

### L2 能量化塞入（体积+精度门）

目标档可复现导出；体积进场景预算；任务集掉点在合同阈内；decode 不低于场景 soft 线。塞进了但胡话、仅 PPL 好看业务崩、无校准指纹——拒收。Q4 不保证无损；更低 bit 不一定更快。塞进去但不能用，等于没交付。按序讲到 ③ 变轻、⑦ 公式；尺子与对齐见 ⑬-2 / ⑬-14，选型见 ⑭。

### L3 工具链可复现（指纹门）

指定板上复现同数量级；关键算子落点可审计（非静默 CPU fallback）；SoC + 引擎版本 + 导出指纹可复跑。只有演示 APK、「我机器上可以」、SDK 漂移无指纹——拒收。开源能跑 ≠ 量产栈就绪；桌面 TRT-LLM 数字搬不过 Orin Edge-LLM。演示机可跑、产线复现失败，是端侧事故主因之一。按序讲到 ⑤ 冒烟、⑩ 上板、⑭ 选型；合同见 ⑯。

### L4 交付门禁（长稳+可追溯）

场景硬约束全绿；热稳态相对冷峰值掉速在阈内；OTA 可回滚。演示 30 tok/s、热了掉到个位数；共板抢带宽；无指纹无法追责——拒收。平均 tok/s 无 P95/温度、BS=8 聚合冒充单用户——误判。空调房绿表挡不住座舱投诉。按序讲到 ⑥ 框架、⑪ 流程、⑮ 门禁；TTFT/OTA 口径见 ⑬-12 / ⑬-16。

锚点（写清条件）：

- **Phi-3-mini 3.8B**：4-bit ≈**1.8 GB**；iPhone 14 / A16 **>12 tok/s**（Microsoft 报告）。
- **Llama 3.2 1B/3B**：edge/mobile；量化体积约 **−56%**、内存约 **−41%**（厂商博客条件）。
- **Qwen2.5**：**3B** mobile 友好；0.5B/1.5B 常作端上或 draft。
- **车机/盒**：Orin ~**200 GB/s**、数十 GB DRAM；Edge-LLM 8B INT4、BS=1 约数十 tok/s。TOPS 规格 ≠ 官方 LLM 栈就绪；版本列车见 ⑭ / ⑮。

往后仍是这些层变厚：小参数 LLM / 多模态往端上挤；低 bit 变默认；访存墙比宣传 TOPS 显眼；共板排班；端云按延迟和隐私切流；校准集、指纹、CI 从加分变拒收。

### 落地场景（选场景 = 选拒收理由）

同一句「部署上去」，验收尺完全不同。

| 场景 | 硬约束 | 典型模型量级 | 必测 | 常撞墙 |
|------|--------|--------------|------|--------|
| 手机助理 / 改写摘要 | 隐私；皮肤温度；包增量；后台杀进程 | 常驻 **1–3B INT4**；旗舰可试 7B w4a16 短 ctx | 稳态 tok/s；TTFT；包增量；mAh；DRAM 峰值 | 体积、耗电、系统 API、热降频 |
| 车机座舱 | 跟嘴；离线；温区；OTA **版本整组** | Orin 级常 **1.7B–8B INT4** | TTFT P95；decode 稳态；内存峰值；版本指纹；共板 | 语音前端、温区、工程栈≠桌面 TRT-LLM |
| 产线 / 机器人盒 | 多路长稳；功耗·W | Jetson / 工业 NPU；**0.6B–8B** | tok/s·W；热稳态；共板带宽；崩溃/OOM | 多路抢 DDR、几天不停机 |
| MCU 唤醒 / 门禁 | 毫瓦；极小模型 | TinyML | **不要用 LLM tok/s** | 另一赛道 |

**手机** — 系统推理 API 或厂商 NPU；低 bit 常驻；长尾上云/PCC。翻车：插电冷跑 25+ tok/s，夏天户外 DVFS 掉到个位数，日志还默认回传——体验+隐私双杀。锚点：Phi-3 >12 tok/s（A16/4-bit）；AI Hub Llama-2-7B Chat ≈12.85 tok/s（8 Gen 3/QNN/ctx1024）。

**车机** — 端上短答 + 近边/云长尾；禁止搬桌面 TRT-LLM 数字。翻车：空调房 30 tok/s，暴晒掉到个位数，VAD 未对齐导致「说完半天才出字」。锚点：Edge-LLM Orin64 Qwen3-8B INT4 ≈30.6 tok/s（BS1, KV2048）。

**盒子** — 固定队列与共板排班；崩溃拉起 + 指纹上报。翻车：单路实验室绿，四路相机+ASR+LLM 后 DDR 打满间歇 OOM，换班无法复现——缺 L3 指纹与共板账。

工业质检看漏检与换班，云边看金丝雀和回滚；专线 ⑮ / ⑬-16。本章只谈手机、车机、盒子。

### 现场拒收尺（先认尺，下一站仍是 ①）

「能跑」进不了评审。立项前至少定五张表。测法全表在 ⑬；CV 串讲在 ⑫。本章先把口径说清楚，不跳章去做实验。

### 表 A · DRAM 峰值 / 模型体积

OOM、杀进程、包体超配额、长对话中途崩——都是容量与共板问题；「能聊两句」挡不住。同构建指纹下测：冷/热峰值 RSS；权重+runtime 常驻；KV 随 ctx 曲线；共板叠加；导出哈希与体积一致。挂钩：⑬-9；〇 L1；⑩；⑮。

| 测什么 | 怎么坏 | 挂钩 |
|--------|--------|------|
| 冷/热峰值 RSS | OOM、杀进程 | ⑬-9；〇 L1 |
| 权重 + runtime 常驻 | 超商店/分区配额 | ③；⑭ |
| KV 随 ctx 增长 | 中途崩或截断无提示 | ⑬-9；⑬-17 |
| 共板叠加 | 单模绿、上座舱红 | ⑩；⑬-16 |
| 导出指纹与体积 | 同名不同哈希装错包 | ⑮；⑯ |

### 表 B · TTFT / Prefill

首字迟钝当场失败；长提示卡死；VLM 一帧图拖垮首包。固定 prompt 长（及视觉 token）测 TTFT P50/P95；Prefill tok/s 或 E2E ms；冷/热分列；座舱加 VAD→首字。禁止用纯 kernel 时间顶替端到端 TTFT。挂钩：⑬-12；⑨。

| 测什么 | 怎么坏 | 挂钩 |
|--------|--------|------|
| TTFT P50/P95 | 首字迟钝 | ⑬-12 |
| Prefill tok/s / E2E ms | 长提示卡死；CPU fallback | ⑨；⑬-12 |
| 视觉 token 数（VLM） | 一帧拖垮首包 | ⑬-1；⑬-19 |
| 冷 vs 热 | 只报热、瞒冷启 | ⑮ |
| VAD→首字 | 说完半天才出字 | ⑪ |

### 表 C · Decode 稳态 tok/s

演示快、热了趴；吞吐虚高体验仍卡；ctx 越长越爬。固定 past-KV 下 P50/P95；温区/功耗墙内 sustained；**单用户 BS=1**；长上下文掉速；CPU/GPU/NPU 同条件对照。挂钩：⑬-12；〇 L4；⑭。

| 测什么 | 怎么坏 | 挂钩 |
|--------|--------|------|
| P50/P95 tok/s @ 固定 past-KV | 热了趴 | ⑬-12；〇 L4 |
| 温区内 sustained | DVFS 未进报告 | ⑬-16 |
| BS=1（勿用 BS=8 聚合） | 吞吐虚高 | ⑭ |
| 长上下文掉速 | ctx 越长越爬 | ⑬-17 |
| 三后端同条件对照 | 闭源数字无法复现 | ⑤；2410.03613 |

### 表 D · 量化精度掉点

塞进了但不能用；能聊不能调用；压 bit 后护栏塌。业务集/锚点集 vs FP16/BF16；校准指纹与复测；工具调用 JSON 合法率；安全拒答/越狱集；逐层/算子误差归因。挂钩：⑬-14；〇 L2；⑬-8。

| 测什么 | 怎么坏 | 挂钩 |
|--------|--------|------|
| 业务集 vs FP16/BF16 | 不能用 | ⑬-14；〇 L2 |
| 校准指纹与复测 | 临时 50 条季度复现失败 | ⑮；⑯ |
| 工具调用 JSON 合法率 | 能聊不能调用 | ⑬-18 |
| 安全拒答 / 越狱集 | 护栏塌 | ⑬-8；⑪ |
| 逐层/算子误差 | 「整体差不多」掩盖崩点 | ⑧；⑬-14 |

### 表 E · 功耗 / 温控掉速

always-on 烫手与续航投诉；无散热掉崖；相机+ASR+LLM 同时炸墙。会话级 mAh 或整机功率；结温–频率–tok/s 三联；共板功率包络；降级策略可测；车规高低温点。挂钩：⑬-16；⑮。

| 测什么 | 怎么坏 | 挂钩 |
|--------|--------|------|
| 会话级 mAh / 整机功率 | 烫手、续航投诉 | 手机；⑬-16 |
| 结温–频率–tok/s | 无散热掉崖 | Orin；⑮ |
| 共板功率包络 | 多模同时炸墙 | ⑩；⑬-16 |
| 降级策略可测 | 只会硬崩 | ⑮；⑬-16 |
| 车规温区点 | 只测室温 | ⑪ |

营销话术默认态度：

| 说法 | 判定 | 教法 |
|------|------|------|
| 「NPU xx TOPS → 一定很快」 | 规格叙事 | decode 吃带宽；TOPS 更贴 prefill |
| 「最高 70 tok/s」 | 模糊营销 | 追问模型、量化、上下文、是否热稳态 |
| 「Orin 254 TOPS 能跑大 LLM」 | 半真 | TOPS 真；内存与官方 LLM 栈才是门 |
| 「MCU 也能端侧 AI」 | 易误导 | Tiny ≠ LLM |

### 职业：三种活（交的是产物，不是岗位名）

把训好的模型接到设备上，又准又稳，还能复现交付。算法盯离线分数，嵌入式盯板子和驱动，部署夹在中间：把「实验室能训」收成「现场能交」。

| 更像哪种活 | 日常 | 交出去的 | 常撞的坑 |
|------------|------|----------|----------|
| 把模型交到板上 | 转换、上板、对齐、盯长稳 | 板上结果、性能单、交付清单 | 演示机可跑、产线复现失败 |
| 把模型变轻变快 | 量化、蒸馏、换结构，掉点能查 | 压缩方案、精度–体积曲线、归因 | 只报体积、不报业务掉点 |
| 啃 Runtime / 算子 | 图编译、算子覆盖、异步队列、热节流 | 排障记录、kernel / 工具链补丁 | 静默 CPU fallback 当「已加速」 |

三种活是交付物类型，不是岗位编制。可兼两活，评审看桌上哪张单。JD「熟悉深度学习 / 了解 NPU」不够，看对方要你交什么。

- **交到板上** — L3/L4；主读 ⑤⑩⑪⑮
- **变轻变快** — L2；主读 ③⑦⑬-2/⑬-14
- **Runtime / 算子** — 访存墙与落点审计；主读 ⑨⑭⑯

能力大致四级：跟着转上板出首帧 → 能量化并对齐、说清慢在算还是搬 → 独立闭环含长稳版本门禁 → 扛共板或场景专线。刷离线榜的人一直有；缺的是走完导出 → 变轻 → 上板 → 对齐 → 交付的人。

### 出处速查（备课）

| 用途 | 文献 |
|------|------|
| 端/边/云 | ETSI GS MEC 003；ETSI GR MEC 035；ITU-T Y.3540；ITU-T Y.4509 |
| SLM | Phi-3 tech report (arXiv:2404.14219)；Llama 3.2 / 量化博客；Qwen2.5；Apple Foundation Models；Private Cloud Compute |
| 访存墙 | Cambricon-LLM (arXiv:2409.15654)；手机 COTS LLM (arXiv:2410.03613) |
| 车机/盒 | NVIDIA Orin / Jetson；TensorRT Edge-LLM benchmarks |
| 手机路径 | Qualcomm AI Hub Llama-v2-7B-Chat；白皮书需对照第三方 |
| 非 LLM 边界 | MLPerf Tiny |
| 数据最小化（技术解读） | EDPB Guidelines 4/2019；ICO/CNIL AI 指引（非法条结论） |

数字进合同以板上 profiler 为准；上表只作认方位的产业锚点，不替代 ⑬ / ⑯ 口径。

## ① 前置底座

> 上一站认过端侧；这一站补硬件层次、Roofline 和结构名词，好让后面的量化与性能听得懂。讲到 ④/⑧ 再用 profiler 核对。下一站 ②。

结构和硬件没摸清，后面的量化、融合都是碰运气。这一站只补后文听得懂所需的底座：器件与存储层次、Roofline 怎么判、Transformer 名词怎么对上端侧账单。算子与结构细节见 ⑨；KV/TTFT 验收尺见 ⑬；IR 合同见 ⑯。

### 先建立三张图

现场评审常被一张「xx TOPS」海报带跑。先分清：算落在哪颗核、数存在哪一层、慢是算不满还是搬不动。

**第一张：异构四件套。** CPU 擅长控制流与例外路径；GPU 擅长大批 SIMT 矩阵；NPU/DSA 把固定图、权重预排布、片上 SRAM 换成能效；DSP 常接音频/预处理。端侧评审先问「这一层算子落在哪颗核」，不要用一张 TOPS 海报代替分派。NPU 不是「更快的 CPU」：很多是静态图 / 数据流，别把五级乱序流水硬套上去（体系结构细表见 JD 补全页，主线先记住这一句）。

**第二张：存储层次。** 近端（寄存器 / 片上 SRAM）快而小，远端（LPDDR / DRAM）大而慢。LLM **decode 逐步**时，每 token 几乎要把权重扫一遍量级的数据，算术强度常落到数 FLOPs/byte——墙在搬，不在「算力纸面」。手机 / 车机比数据中心更窄：片上 SRAM 更小、LPDDR 更紧，任何「权重常驻 NPU」都要对 footprint，不能口头加速。

**第三张：Roofline。** 横轴算术强度（FLOPs/byte），纵轴实际吞吐。斜线是带宽屋顶，横线是峰值算力。点落在斜线上就是 **memory-bound**，堆 TOPS 没用，要砍搬的量（量化、GQA、KV 量化、融合少写回）。点落在横线上才是 **compute-bound**，才轮到核利用率、Tensor Core / Cube 占满。端侧对话默认 **BS=1**：云课上的 occupancy 故事经常失灵。④ / ⑧ 的 profiler 用来把「猜」换成「落在哪条屋顶上」；本章先会读图。

### 大模型核心原理（端侧账单版）

Decoder-only 自回归：嵌入 → N 层 Block → LM Head，**一次只出一个 token**。Prefill 吃整段 prompt，矩阵大，更可能靠近 compute-bound；Decode 依赖上一个 token，KV 与权重在路上，执行单元常空转——这就是 〇 里访存墙的结构来源。

注意力是性能核：MHA 每个 Q 头配一套 K/V，KV-Cache 按 `2 × layers × num_kv_heads × head_dim × seq × dtype_bytes` 涨（**必须用 KV 头数**，不是 Q 头；⑬-0 勘误同口径）。GQA/MQA 让多 Q 头共享少 K/V 头，是端侧降带宽的第一结构杠杆。FFN/SwiGLU 往往参数量最大，量化收益也最显眼。RMSNorm 比 LayerNorm 少均值，常与残差融成单算子省一次写回。RoPE 把相对位置旋进 Q/K，长上下文外推的坑放到 ⑬-17。Tokenizer（BPE/SentencePiece）决定「用户以为的字」和「你报的 tokens/s」不是同一口径——验收必须写清 tokenizer 与 chat template（⑬-4 / ⑬-14）。

结构图、Attention/FFN 内核、VLM 串联——进 ⑨ 细讲。本章能做到：听到 GQA 就想到 KV 字节公式里的 `num_kv_heads`。

### 编程与编译基础

导出与对脚本在 Python/PyTorch（`torch.export` / `onnx.export`）；引擎与自定义算子在 C++（指针、RAII、线程、对齐）。张量布局 NHWC/NCHW/tiled 不是审美问题：NPU 算子表按布局列支持度，转错一次就静默 CPU fallback。静态图 vs 动态图、IR、离线 Pass——决定「能在编译期折叠什么」；合同语言放到 ⑯，本章只建立：运行时不该再做能离线做完的事。

### 硬件体系结构

- **处理器类型** — CPU/GPU/NPU/DSP 定位差异，异构系统负载分派
- **存储层次与带宽** — 寄存器→L1/L2→DRAM 逐级延迟，DRAM带宽是端侧最大瓶颈
- **Roofline 模型** — 算力-带宽屋顶线判定 compute/memory-bound，指引优化方向
- **SIMD / 向量化** — NEON/AVX 单指令多数据，加速逐元素运算与量化解包
- **车规嵌入式** — 交叉编译、RTOS/HAL、ASIL，内存功耗受限的量产边界

**学完这一站，口头能说清：** 能画三层存储并指出 decode 卡在哪一层；能用 Roofline 一句话说清「再加点 TOPS 会不会变快」；能区分 Q 头与 KV 头。下一站 ② 把模型送进转换→编译→Runtime。

## ② 部署主链路

> 上一站补了硬件与名词；这一站把训练图送上设备，固定走转换 → 编译 → 运行时。三站产物不同，混着说「转引擎」后面不好排错。下一站 ③。

训练图到设备上的可执行物，固定走三站：**转换 → 编译 → 运行时**。三站产物不同、失败形态不同；混着说「转引擎」后面无法排错。⑤ 选一条链冒烟，⑭ 展开厂商名，⑯ 把 IR/融合/数值写成合同。本章把三站角色说清楚。

### 三站分别交出什么

**转换**把训练框架图变成交换格式（常见 PyTorch → ONNX，或 `torch.export` 直出）。你在要 opset 覆盖、动态轴（batch / seq）声明、不支持算子的拆解或替换。失败长这样：节点在 ONNX 里还在，到芯片图里消失或落到 CPU；预处理布局（NCHW/HWC、BGR、0–1 vs 0–255）和训练不一致，后面余弦怎么调都不稳。常量折叠属于转换/编译期：能离线算完的不要留到 Runtime。

**编译**把交换图匹配到目标核：融合、重排、死代码消除、内存复用、codegen。产物是 **engine / context binary / OM(C)** 这类带执行计划的包，不是「又一份 ONNX」。失败长这样：Pass 融掉你要对齐的节点；动态 shape 被写成死 shape；plugin 缺失导致整图拒收或静默 fallback。选型口头对照即可：QNN（高通 HTP）、TensorRT / Edge-LLM（NVIDIA 边缘）、ORT EP（多后端，端上要盯 EP 落点）。细表在 ⑭，合同在 ⑯。

**运行时**加载产物、分配对齐好的 buffer、把输入送进核、处理异步与多核、扛住温控降频。失败长这样：冷启动把 TTFT 预算吃光；每次推理 malloc；NPU 忙的时候 CPU 在拷贝；峰值功耗触发 DVFS，实验室数字现场复现不了。⑫ 会用一条 CV 链把三站跑通；本章先能指着日志说「这是转换问题还是编译问题还是 Runtime 问题」。

### 主链路三站对照

排错时先问「卡在哪一站」——三站产物名和失败形态不一样，混着查只会空转。

| 站 | 典型产物名 | 失败长什么样 | 验收一句话 |
|----|------------|--------------|------------|
| 转换 | `model.onnx`（或 `ExportedProgram`） | 不支持 op / 动态轴未声明 / GraphBreak 切段 | 固定 shape 前向可跑；opset/动态轴写入清单 |
| 编译 | QNN：`lib*.so`→context binary；TRT：`*.engine`；昇腾：`.om` 或 `.omc`（⑭ 分轨） | builder/omg/atc 报错；产物体积异常；大量 fallback | 目标后端可 load；关键层在加速器上 |
| Runtime | 进程内会话 + I/O buffer | Load fail；冷启动 OOM；数值 NaN/胡言 | 同输入余弦/任务门过；冷热 load 各一次 |

细则命令与命名栈 → ⑤（冒烟）/ ⑩（板上）/ ⑭（选型与版本列车）。

### 模型转换

- **转换链路** — PyTorch→ONNX→QNN/TensorRT，或 torch.export 直接导出
- **ONNX opset 与动态shape** — opset 决定算子覆盖，batch/序列动态轴需显式声明处理
- **算子兼容处理** — 不支持算子拆解/替换/CPU fallback，需评估精度与时延
- **常量折叠** — 仅依赖常量的计算提前到转换期，减少运行时开销

### 推理引擎与编译

- **引擎选型** — QNN、TensorRT、TensorRT Edge-LLM、onnxruntime 定位取舍
- **图优化** — 算子融合、重排序、死代码消除、内存复用，离线优化大头
- **算子映射与plugin** — 节点匹配内置算子，不匹配则自定义kernel或plugin扩展
- **codegen 与打包** — 权重+执行计划编译成二进制：TensorRT engine / QNN context binary

### 运行时与调度

- **产物加载** — 读取engine/so/context binary，反序列化，冷启动与缓存策略
- **Buffer 管理** — tensor分配/对齐/生命周期，复用缓冲减少内存拷贝
- **异步与异构调度** — NPU+GPU+CPU 多核并行，流式管线隐藏等待延迟
- **功耗与温控** — 考虑功耗墙与降频，避免峰值算力致体验抖动

**学完这一站，应能：** 能默画三站和各自产物名；能举一种「看起来像精度问题、其实是 CPU fallback」的排错路径。下一站 ③ 在这条链上把权重变轻。

## ③ 量化与压缩

> 上一站把导出链角色说清了；这一站要把权重变轻。先学会选路；公式与海思步骤放到 ⑦。下一站 ④。

权重太大装不进、搬不动，端侧常见的下一步就是量化。量化先砍**搬的量**，往往比空加 TOPS 更能改 decode。公式、校准统计、海思 PTQ 六步放到 ⑦；新格式 IQ/FP4 与端上真实约束放到 ⑬-2；本章先会**选路**和**验收**。

### 先选路，再调参

端上动手前，现场通常已经撞过这几堵墙：内存不够、带宽不够、校准数据少、引擎量化和训练伪量化对不齐。按这条约束选路，不要按论文发布时间选。

1. **先 INT8 激活是否吃得消。** 权重 INT8 + 激活 INT8 是工具链最熟的路（QNN / TRT calibrator）。激活离群严重就考虑 SmoothQuant 把离群「挪」到权重，或敏感层留高精度。
2. **内存仍爆再上权重 INT4。** 朴素 per-tensor INT4 对 LLM 几乎不可用；工程上是 **per-group** + GPTQ（Hessian 逐层补偿）或 AWQ（保护显著通道）。两者都是后训练，不要和 QAT 混成一种「高级量化」。
3. **QAT 留给 INT4 仍垮、且你有训得起的 GPU 与对齐得了的 fake-quant。** QAT 精度上限高，但伪量化和引擎实现差一个 round 模式就会整网漂。部署一致性写进 ⑯ 的数值协议。
4. **剪枝 / 蒸馏 / 低秩** 是另一条变轻轴：结构化剪枝、N:M 稀疏要硬件认；蒸馏改的是模型本身。量产常见是量化为主、蒸馏补精度，不要并列五条同时开没人能回归。

评估不要只报一个 perplexity：下游任务（问答/工具调用）、逐层误差、chat template 是否同一套（⑬-14）。数字必须带：位宽、粒度、校准集、是否 weight-only。

### 量化基础

- **精度位宽** — FP16/INT8/INT4/FP4，位宽越低越省内存带宽，精度损失越大
- **scale 与 zero-point** — 真实值 = scale×(量化值−zero)，量化参数决定数值映射
- **对称 vs 非对称** — 对称以0为中心、非对称含零点，影响激活分布表示与实现复杂度
- **量化粒度** — per-tensor / per-channel / per-group(128元素组,INT4常用)，越细精度越高
- **量化公式** — q=round((r/scale)+zp)，误差源于舍入 round 与裁剪 clip

### 后训练量化 PTQ

- **校准方法** — MinMax / Percentile(抗离群) / EMA 统计激活分布选scale
- **校准集选择** — 数据少分布不准、多则过拟合，需覆盖真实部署分布
- **误差来源** — 裁剪clip、舍入round、权重与激活分布不对称三大误差源
- **scale 优化目标** — 最小量化噪声选scale，用 MSE 或 KL 散度 (TensorRT校准器)

### 量化感知训练 QAT

- **伪量化算子** — 前向插入 fake quant (量化再反量化)，让模型感知量化噪声
- **直通估计器 STE** — 反传梯度绕过 round，使量化参数可训练
- **QAT vs PTQ** — QAT 精度更高但需训练GPU，用于INT4精度敏感场景
- **部署一致性** — 训练伪量化细节须与推理引擎量化对齐，否则精度失真

### 低比特进阶 INT4/GPTQ/AWQ

- **INT4 量化** — 4bit 权重 per-group 量化，内存/带宽降为1/4，朴素量化损伤大
- **GPTQ** — Hessian 二阶信息逐层最小二乘补偿，主流INT4后训练方法
- **AWQ** — 激活感知权重缩放，仅保护显著通道，无需重训
- **SmoothQuant** — 激活离群值平滑迁移至权重，解决激活难量化，配INT8激活
- **LLM.int8() 与混合精度** — 离群列分精度处理，敏感层(attention)留高精度其余压缩

Weight-only（W4A16 等）主收益是**降权重带宽**，适合 BS≈1 的 Decode。选型前先问：目标 Runtime 是否有对应 GEMM/Attention；没有就按「压缩格式」而非「加速格式」评估——无匹配 kernel 时运行时解量化回高位，体积好看、带宽收益蒸发。GPTQ/AWQ 机制在 ⑦ 展开；格式族与 NPU 真实约束见 ⑬-2。勿在未测板上编造 tok/s。

### 精度评估

- **困惑度 Perplexity** — 语言生成通用指标，越低越好，量化退化常用它衡量
- **任务指标** — MMLU/GSM8K/问答/代码等下游任务准确率对比
- **逐层误差分析** — 对比量化前后同输入输出，定位精度损失热点层
- **异常值处理** — 识别隔离影响巨大的权重/激活离群值 outlier

PPL/MMLU 只证明「语言建模没塌」。端侧交付还要对齐：任务/业务集（含拒答与格式，⑬-22）；同机 TTFT + **BS=1** 稳态 tok/s（条件写清，⑬-12）；导出与校准指纹可复现（⑥/⑮）。掉点超合同阈、或 Decode 掉出场景 soft 线，都算量化方案失败。合同口径见 ⑯。

### 轻量化技术

- **剪枝** — 结构化(通道/头/层)与非结构化稀疏，结构化更适合端侧加速
- **N:M 稀疏** — 每M个权重保留N非零，配合硬件稀疏加速器
- **知识蒸馏** — 教师大模型训练学生小模型：logits蒸馏 / 中间特征蒸馏
- **低秩分解** — 权重矩阵拆为低秩因子减少参数量
- **组合拳** — 量化+剪枝/蒸馏+结构化联合优化，端侧量产常用

**学完这一站，应能：** 能按内存/带宽/校准成本选出 PTQ INT8、GPTQ/AWQ INT4 或 QAT，并说出一个「引擎与 fake-quant 不一致」的翻车。公式与海思步骤下一站不讲——排在 ⑦；现在先进入 ④ 看变轻之后速度卡在哪。

## ④ 推理性能优化

> 上一站把模型变轻了；这一站要回答：变轻之后，速度卡在哪。命令行、GGUF 档位、profiler 开关放到 ⑧；TTFT/TPOT 口径在 ⑬-12；分层 profiling 在 ⑬-13。本章先画一张**手段地图**。

### 先拆两个阶段

车上用户说完一句话，系统要先「吃完整段提示」，再「一个字一个字往外吐」。前半叫 **Prefill**：矩阵大，融合、算力占满、少启 kernel 更值钱，TTFT 主要花在这里。后半叫 **Decode**：每步几乎扫一遍权重 + 读写 KV，Roofline 上常是 memory-bound；手段是 GQA、KV 量化/分页、权重 INT4、少写回的 Attention 内核。两阶段平均成一个「tokens/s」立项对不上——⑬ 要求分报。

端侧对话默认 **BS=1**。云 Serving 靠 Continuous Batching、大 batch 填 occupancy；座舱/手机上常常没有第二路请求可填，这根杠杆用不上。投机采样（小模型草稿 + 大模型验证）在端上还要付**第二份常驻内存**，草稿失败还得回退；接受率低反而更慢——适合进白名单，不要默认开。

FlashAttention / online softmax 想解决的是：大 `S = QKᵀ` 不要完整回写远端内存。端侧 NPU 不一定叫这个名字，但问题是同一句——注意力中间张量有没有打爆片外。GEMM 分块、bank conflict、INT4 打包解包，是把 Roofline 上的「搬」落到内核实现。

### 性能分析定位瓶颈
- **性能工具** — 高通QNN Profiler / NVIDIA Nsight / TensorRT Profiler 逐算子耗时。
- **关键指标** — FLOPs、MACs、实际带宽占用、算子时延、总时延。
- **Roofline 判断** — 画算力-带宽屋顶线，判定 compute-bound 或 memory-bound。
- **阶段拆分** — prefill(计算密集)与decode(访存密集)分开分析，手段不同。
- **回归基准** — 可复现基准脚本，逐次改动对比基线。

### 计算图优化与算子融合
- **算子融合** — QKV融合、残差+LayerNorm融合、attention整体融合，减少中间张量与kernel启动。
- **↪ 执行重排序** — 调整执行顺序优化缓冲区复用与数据局部性。
- **内存复用** — 中间张量共享内存池，降低峰值内存。

### KV-Cache 动态内存管理
> 原理与取舍在本节；Paged/KV 量化等**开关与命令**见 ⑧；公式与预算表见 ⑬-9 / ⑬-21。
- **Paged KV Cache** — 仿操作系统分页按需分配/释放，消除预分配浪费与碎片。
- **KV 量化** — KV Cache 降 INT8/INT4 省内存，评估精度损失。
- **GQA 与结构瘦身** — 减少K/V头从结构上缩小缓存。
- **前缀共享** — 多请求共享 system prompt 前缀 KV，省重复计算存储。
- **内存池** — 预分配大块连续内存按页管理，减少malloc抖动。

### 内存带宽与 DDR 优化
- **数据布局** — NHWC vs NCHW vs tiled，匹配硬件访存模式。
- **DDR burst 与对齐** — 访存对齐 burst 长度，避免跨burst低效读写。
- **weight 重排** — 按执行顺序重排权重，保证连续读取。
- **INT4 打包解包** — 两INT4合成一字节降位宽，配解包kernel。
- **预取与异步** — prefetch/DMA异步隐藏访存延迟，提cache命中率。

### Token 生成速度优化
- **TTFT 首Token延迟** — 由prefill决定，优化在图融合与算力利用。
- **TPOT 与吞吐** — 持续生成访存密集，靠带宽与KV优化。
- **批处理边界（端侧默认）** — 交互助手/座舱 HMI 默认 **BS=1**；优化 TTFT/ITL 与温控，不把云侧 continuous batching 当默认。多路共用加速器时先做共板资源账（⑬-15），再谈是否值得上 in-flight batching；用 BS=8 聚合吞吐冒充单用户体验视为验收作假。

### 🃏 投机采样 Speculative Decoding
> 原理与取舍在本节；投机 mode / draft 长等**开关**见 ⑧。
- **基本原理** — 小draft模型连猜K个token，大模型并行验证纠正。
- **Draft 来源** — 独立小模型 / 自推测(浅层) / N-gram近似。
- **加速比与接受率** — 加速≈平均接受token数；接受率低反而慢。

### Attention / GEMM 内核
> 原理与取舍在本节；`--attention_plugin` / FMHA 等**开关**见 ⑧。
- **FlashAttention** — 分块tiling避免大矩阵回写HBM，提升IO效率。
- **Online Softmax** — 分块安全计算softmax，数值稳定。
- **量化 GEMM** — INT8/INT4矩阵乘，scale反量化融合。

**经典 GEMM 优化**
- 共享内存分块、bank conflict避免、寄存器tiling。

**学完这一站，应能：** 能分开说 TTFT（prefill）和 TPOT（decode）各该动哪类优化；能解释为何 BS=1 时不要先上 continuous batching。下一站 ⑤ 选 QNN 或 TensorRT，把这条链真正跑通一次。

---

## ⑤ 平台工具链

> 手段地图有了，这一站选一条链真正跑通：高通骁龙（QNN）或英伟达 Jetson Orin（TensorRT）。目标是走通 ② 的三站，用 profiler 验证 ① 的 Roofline 直觉。全厂商对照、版本列车、产物别名在 ⑭；昇腾/麒麟板上在 ⑩。这一站不求学会所有 SoC。

### 先搞清三件事（再选栈）

1. **高通**：SNPE 主产物 `.dlc`；QNN 量产主产物 **context binary**；QAIRT=底层 Runtime；Genie=LLM 编排（tokenizer+KV+多 bin）。DLC ≠ context binary。
2. **NVIDIA**：CV → TensorRT `*.engine`；车机/Jetson LLM → **TensorRT Edge-LLM**。桌面 **TensorRT-LLM 官方不支持 Jetson**；Orin/Thor 都不要拿 TRT-LLM 当量产冒烟。
3. **详表与版本列车 → ⑭**；本章只跑通一条：转换→编译→profiler/net-run。

### 路径 A · 高通 QNN（CV 冒烟）

| 站 | 产物 | 失败长相 | 验收一句 |
|----|------|----------|----------|
| 转 | `libmodel.so`（或经 DLC） | converter 拒节点 / 未量化硬上 HTP | `qnn-net-run` 出数值文件 |
| 编 | context binary（`*.serialized`） | Load fail；跨 SDK 小版本混用 | 板端 HTP 可 prepare/load |
| 测 | profiler 层表 | 大算子全在 CPU | 敏感 Conv/GEMM 落 HTP；耗时可复现 |

### 路径 B · Jetson TensorRT（CV 冒烟；LLM 另开 Edge-LLM）

| 站 | 产物 | 失败长相 | 验收一句 |
|----|------|----------|----------|
| 转/编 | 板端 `*.engine` | x86 编的 engine 上板 load 失败 | `trtexec --loadEngine` 可跑 |
| 测 | 延迟日志 | 未 `nvpmodel`/`jetson_clocks` 数字虚高 | 注明功率模式；warmup 后稳态 ms |

LLM：用 Edge-LLM 的 export→本机 build→run；服务器上的 TRT-LLM 目录不能当 Orin 发布物。

### 冒烟怎么才算过

三件事分清之后，选一条链、固定版本，跑完：**转换产出交换图 → 编译产出 engine/context binary → Runtime 加载推理 → profiler 给出逐层时延**。再加一条精度对照（余弦或任务集，阈值写进实验笔记）。过关标准不是「命令没报错」，而是：

- 产物名能说清（DLC ≠ context binary；engine ≠ ONNX）；
- 没有静默 CPU fallback（ORT/QNN 都要看节点落点）；
- 时延拆得出 prefill/decode 或至少逐层，而不是一个墙钟数字。

高通侧记住角色：converter 出图，AIMET 管量化约束，`qnn-context-binary-generator` 出可加载包，profiler/tester 分别管速和准。NVIDIA 侧：`trtexec`/builder 出 engine，Calibrator 走 INT8 PTQ，Edge-LLM 才是边缘 LLM 的 KV/运行时故事——**Orin 上曾经折腾 TRT-LLM ≠ 官方量产路径**；Thor 路线转向 Edge-LLM，具体 JetPack 号要核验（⑭ 纠正）。CUDA Graphs、DLA 分派是进阶，冒烟阶段先 GPU 路径稳定。

### 高通 QNN 方向
- **硬件架构** — 骁龙NPU(HTP)/Hexagon DSP、Qualcomm AI Engine Direct、L2/LLP内存层次。
- **模型转换** — qnn-onnx-converter 将 ONNX 等转为 QNN 模型。
- **量化(AIMET)** — AIMET 官方QAT/PTQ；适配HTP量化约束与算子支持度。
- **编译** — qnn-context-binary-generator 生成 context binary。
- **性能分析** — qnn-profiler 逐层/逐算子耗时与带宽分析。
- **验证与量产** — qnn-tester精度比对；context binary 跨cache共享。

### 英伟达 TensorRT 方向
- **硬件架构** — Orin GPU CUDA core + DLA，异构调度。
- **CUDA 编程** — 线程/block/grid、内存层级、共享内存与bank conflict。
- **TensorRT 优化** — trtexec / builder / dynamic shape / plugin / 自动层融合。
- **INT8 量化** — TensorRT calibrator 做 PTQ 校准。
- **TensorRT Edge-LLM** — 边缘LLM/VLM推理框架，内置KV cache管理。

**精度与调度**
- FP16/INT8/INT4 路径；CUDA Graphs 减少下发开销。

**学完这一站，应能：** 一条链的产物名 + profiler 截图口径能讲给别人复现。下一站 ⑥ 把「跑通一次」收成可回归的版本与门禁框架。

---

## ⑥ 工程化与量产

> 冒烟过了，还不等于能发版。这一站只搭**框架**：流水线、回归、资源账、文档。⑪ 写 LLM 量产全流程；⑮ 写门禁矩阵、OTA、车规、值班 SOP。ASIL 条款不在这里展开。

先看职责怎么拆——⑥ 讲清楚「有什么」，⑪ 补 LLM 特有步骤，⑮ 才定「拦不拦发布」：

| 骨架块 | ⑥ 讲清什么 | ⑪ 补什么 | ⑮ 定什么 |
|--------|------------|----------|----------|
| 编排 | 阶段、输入输出产物名、失败退出码 | LLM 特有步骤（KV/自回归/模板） | 发布是否允许打 tag |
| 版本/指纹 | 模型/量化/引擎/校准集要有哪些哈希字段 | 导出链怎么生成这些字段 | OTA 整组与回滚 |
| 回归 | 精度/性能/资源三类回归的**存在** | 任务集与板上脚本 | CI 容差、拦发布 |
| 预算 | 权重/激活/KV/runtime **分项意识** | 选型与 Roofline 预筛 | 余量、温区/功耗门 |

表里的「存在」落到日常，就是下面这套最小闭环。

### 量产最小闭环

实验室一次成功不能发布。最小闭环是：配置（量化档、shape、opset、引擎版本）进仓库 → CI 拉同一校准集与任务集 → 产出带哈希的 engine/context → 精度与时延阈值拦截 → 指纹写入发布清单。哈希必须覆盖：**权重、量化配置、编译器/DDK 版本、chat template**。少一项，现场「同一模型」会对不齐（⑮ 会把这套收成门禁表）。

资源账按 ① 的层次拆：权重、激活峰值、KV（用 `num_kv_heads`）、Runtime/OS 余量（〇 的 15–20% 经验）。算力用 Roofline 约束，不拿峰值 TOPS 除目标 tok/s。带宽按 decode 逐步扫权重来估，超了就回到 ③ 变轻，而不是加并发。

回归分三类，阈值分开：精度（余弦/任务）、性能（TTFT/TPOT/内存）、功能（算子落点、无 CPU fallback）。看板要能看见缓慢退化，而不是只挡崩溃。

### 自动化流水线
- **编排脚本** — Shell/Python 串联 转换→量化→编译→验证，一键跑通。
- **CI/CD** — 提交触发 编译+精度+性能回归，早发现退化。
- **版本管理** — 模型/量化配置/引擎产物统一版本与哈希追溯。
- **参数化配置** — 量化选项/shape 抽成配置，批量矩阵实验。

### 回归验证
- **精度回归** — 大量用例 baseline vs 新版对比，超阈值告警。
- **性能回归** — 时延/吞吐/TTFT/内存基准测试并出报告。
- **看板阈值** — 可视化历史指标趋势，拦截缓慢退化。

### 资源评估
- **内存预算** — 权重+激活+KV-Cache+运行时分项估算。
- **算力与时延** — TOPS×算子效率 与目标时延换算定平台。
- **带宽与功耗** — decode权重带宽 vs DDR上限；峰值/平均功耗预算。

### 落地沉淀与文档
- **技术文档** — 部署方案、量化策略、性能调优报告、工具说明。
- **SOP 标准化** — 沉淀 部署→量化→编译→验证→发布 流程。
- **问题排查清单** — OOM、精度崩溃、算子不支持、数据对齐等坑位。
- **知识传递** — 经验沉淀为checklist，可复现可培训。

**学完这一站，应能：** 能列出发布指纹最少包含哪几项；能把一次失败分到精度回归、性能回归还是产物版本不一致。下一站 ⑦ 把 ③ 的量化选路落成公式与校准步骤。

## ⑦ 量化数学与校准细则

> ③ 已经选过路——PTQ、QAT 还是 GPTQ/AWQ。这一站把选路落成**可算的式子和可跟的步骤**：scale/zp、对称与否、粒度、校准统计、海思 PTQ 六步。新格式与端上能不能跑见 ⑬-2；数值协议进 ⑯。

### 量化基本原理与数学公式
- **量化映射** — FP32→INT8内存缩4倍，整数运算加速2-4倍
- **核心公式** — q=round(r/scale)+zero_point，反量化 r=(q-zero_point)×scale

**对称与非对称**
- 对称 zp=0、范围[-128,127]、scale=r_max/127
- 非对称 zp≠0、范围[0,255]，适合 ReLU/激活单侧分布

- **round 不可导与 STE** — round 斜率处处为0，QAT 需借助 STE 直通估计器反向传播
- **scale 决定误差** — round 最多引入 0.5 scale 偏差，异常值经 scale 放大噪声
- **符号约定** — Int-X-Y 首位为权重位数、次位激活位数

### Per-tensor 与 Per-channel 量化粒度
**两种粒度**
- Per-tensor 共享一组 scale/zp，实现简单但跨通道精度浪费
- Per-channel 每输出通道独立 scale/zp，精度高但开销大

**工业标准组合**
- 权重 per-channel + 激活 per-tensor
- TensorRT、PyTorch QAT 均遵循此策略

### 校准集（标定数据）的设计与要求
**标定数据**
- PTQ 前向收集激活 min/max 分布，计算 scale/zero_point
- 数量 100-500 张，覆盖真实场景分布

**batch 填充与加载**
- np.concatenate 复制填充到目标 batch，存 input.bin
- 海思 DDK 经 config.protxt 指定，由 dopt_so.py --cal_conf 加载

### 校准方法：MinMax、KL 散度、Percentile、Entropy
- **MinMax** — 最简不截断，但对离群点敏感、精度低

**KL 散度**
- 找阈值使量化前后 KL(P||Q) 最小，TensorRT 默认采用
- KL(P||Q)=ΣP(x)·log(P(x)/Q(x))

- **Percentile/Entropy** — Percentile 取百分位阈值跳过极端尾部，Entropy 与 KL 同族

### PTQ 训练后量化完整流程

选路在 ③；这里补的是动手细节：**scale/zp 怎么来、校准怎么选、海思六步怎么跑、精度塌了按什么顺序修**。

**PTQ 流程**
- 承接③：校准覆盖真实分布 → 选定 MinMax/KL/Percentile → 权重倾向 per-channel、激活 per-tensor（工业常配）→ 导出与 Runtime 粒度一致 → 张量级过关后再看任务集
- CV 的 mAP/cos 阈是**项目合同数**（⑯），不是全球常数；「INT8 常掉 1–3%」只是量级示意，未写死条件勿写进合同

**海思 6 步**
- gpu_quant.py 配置→input.bin→config.protxt→dopt_so.py→修重名→OMC
- dopt_so.py 关键参数 --framework 5(--cal_conf/--compress_conf)

- **精度修复清单** — 保准好数据→KL 校准→per-channel→敏感层留 FP16/INT16→QAT→蒸馏

### QAT 量化感知训练：STE 与 FakeQuant

何时上 QAT，③ 已经说过。落到训练图里，关键是 STE 与 FakeQuant 怎么和导出对齐。

- **STE 直通估计器** — 前向真实量化、反向梯度直接传回（视为1而非0）
- **FakeQuant 伪量化** — 训练时量化再反量化，让模型提前适应 INT8 误差

**QAT 细节**
- PyTorch 官方 QAT 采用 STE；BN 折叠合并进卷积 W'、b'
- 学习率取原 1/10，训练 3-10 epoch，mAP early stopping
- 导出需去 FakeQuant、固化 scale、真量化为 INT8

### scale 选择与学习式量化：LSQ、PACT、混合精度
- **长尾分布问题** — 少数异常值拉大 scale，使主体量化粗糙
- **LSQ** — scale 作为 nn.Parameter 梯度下降，通常提升 0.5-1% mAP
- **PACT** — 激活裁剪到可学习阈值 α，砍异常尾部换取主体精细
- **量化坍陷与敏感度** — 误差层层放大致 mAP 崩塌；BRECQ 逐层余弦相似度敏感度分析
- **混合精度** — 敏感层 FP16、不敏感层 INT8；跳过首层 Conv 与末6层检测头

### 激活量化的难点与 SmoothQuant
- **激活量化难点** — 激活 per-tensor 精度低，且有长尾离群值拉大 scale
- **SmoothQuant** — 量化迁移：数学等价变换把激活难度迁到权重侧，适配 W8A8
- **激活层选择** — ReLU 适合非对称量化；SiLU/Sigmoid 值域窄需更细 scale

### Weight-only 量化：GPTQ（Hessian 补偿）与 AWQ（保护重要通道）

③ 选 Weight-only 时，只需要记住「压权重、保激活」。这里比的是两条算法怎么补偿误差：GPTQ 走 Hessian，AWQ 保护敏感通道；两者都和 KV 优化正交，可以叠用。

- **Weight-only 路线** — 只压权重如 W4A16/W8A16，激活保高精度，LLM 减内存主流
- **GPTQ** — 逐层最优量化+误差补偿，近似 Hessian 二阶信息最小二乘补偿
- **AWQ** — 激活感知，保护约 1% 关键通道，为其单独选 scale
- **部署配套** — 与 KV Cache 显存优化联合；配 llama.cpp/GGUF、MindIE 落地

### 精度评估：余弦相似度、平均绝对偏差与任务级指标
- **三层评估体系** — 张量级逐层输出、特征级中间图、任务级 mAP

**张量指标**
- 余弦相似度 cos=(A·B)/(||A||·||B||)，>99% 达标
- ave_diff=Σ|A-B|/Σ|A|×100%，<5% 良好

**任务级 mAP**
- YOLO 用 IoU=0.45、conf=0.4 跑验证集，下降应 <3%
- 综合标准：cos>99%、偏差<5%、mAP 降<3%、检测头>99.5%

文中 cos、ave_diff、mAP 掉点、LSQ「约几个点」都只是**某类 CV 项目常见量级示意**，不是跨模型保证。立项把阈值写进 ⑯；LLM 以任务集 + ⑬-22 为准。未在目标板、目标 Runtime、目标校准集上测过的掉点/加速比，不要写进合同。

### LLM 精度评估：Perplexity 与 MMLU 等在生成任务中的泛化
- **LLM 专用指标** — Perplexity 度量建模能力、PPL 越低越好；MMLU/GSM8K 看 accuracy 跌幅
- **部署指标** — Prefill/Decode、TTFT/TPOT 的定义与测法见 ④ / ⑧ / ⑬-12。量化过关看的是另一件事：固定 seed 与 chat template，看 PPL 增量与任务准确率跌幅，并保留逐层 cos 定位敏感层。
- **评估纪律** — 固定 seed、eval 模式与统一预处理/后处理

### 轻量化组合：知识蒸馏、剪枝与量化的协同
- **三术定位** — 剪枝减参数、蒸馏提小模型精度、量化降比特（FP32→INT8 省75%内存）
- **蒸馏分类** — Logit-based/Feature-based/Relation-based；回归用 L1、分类用 KL/NKD

**推荐链路**
- 结构化剪枝→蒸馏→量化→ONNX→OMC→部署
- 剪枝按 L1 范数，通道数对齐 4/8/16 倍数

## ⑧ 性能命令与平台开关

> ④ 画了手段地图；这一站把判断落到命令和开关上：KV 公式用 `num_kv_heads`、GGUF/K-quant 档位、`trtexec` / llama.cpp / profiler。原理只留排错需要的一句。端侧边界与路径对比见 ⑬。

### KV Cache 原理与显存计算
- **缓存动机** — decode 复用旧 K/V，省掉 O(seq_len²) 重算，每步 O(1) 增长

**显存公式**
- bytes/token=2×num_layers×num_kv_heads×head_dim×precision_bytes
- FP16=2B、INT8=1B、FP8=1B、INT4=0.5B；GQA/MQA 是首要杠杆

- **数值怎么报** — 先代入 `2×L×num_kv_heads×head_dim×dtype`（**必须是 KV 头，不是 Q 头**），再写 MiB/token；GQA 相对 MHA 的倍数用 `num_heads/num_kv_heads` 估算。完整预算见 ⑬-9。示例以目标模型 `config.json` 现场手算为准，不在教程里写死未核对的单点。
- **显存预算** — 总显存=每序列 token×每 token 字节×并发；杠杆为 max_seq_len、kv_cache 占比、concurrency

### Paged KV / KV Offload / KV 量化
**Paged KV**
- KV 按固定 block(128 token)分页；共享 prefix 锁块复用，省 prefill 计算
- 配合 --enable_chunked_context 流式 prefill

**KV Offload**
- 不活跃 block 换出到 host pinned memory，--kv_cache_host_memory_bytes
- 代价：host↔device 拉回带宽翻倍

**KV 量化**
- --kv_cache_quantization int8 降 precision_bytes，decode 带宽双利好
- 代价长上下文精度损失，与权重量化正交、默认不量化

- **Orin 特例** — 无独立显存走共享 DRAM/NVMM，nvpmodel -m 0 + jetson_clocks 满频

### Prefill / Decode：诊断命令

〇/④ 已经讲过两阶段因果和访存墙。现场排错时，真正缺的是：**取哪份证据、拧哪个开关**。

| 阶段 | 看什么 | 常用命令/开关（示例） | 端侧默认 |
|------|--------|----------------------|----------|
| Prefill | TTFT、GEMM/Attention 是否算满 | `nsys`/`ncu`；逐 iteration 日志；`--enable_chunked_context`；prompt/prefix cache | 降峰值、稳首字；首字慢别先去拧 Decode 带宽开关 |
| Decode | ITL/TPOT、DRAM vs SM | 同上；`--kv_cache_quantization`；GQA 结构；投机相关 mode | **BS=1**；端上别急着上 continuous batching 博吞吐 |

- **阶段诊断** — verbose 看 per-iteration；nsys 看 kernel 名；ncu 看 DRAM vs SM throughput
- **优化清单** — prefill: prompt cache、chunked prefill；decode: KV 量化、投机采样、GQA。每次只改一个开关；改后 `compare_logits` 或任务集护栏（⑦/⑬-14）。tok/s 必须写模型、量化、past-KV、温区。

### 投机采样：开关与验收

④ 讲过：小模型（或 Medusa 头）草拟、大模型并行校验。落到板上，先看这几个开关：`--speculative_decoding_mode`（如 `draft_tokens_external` / `medusa` / `lookahead`）、`--max_num_tokens` 与 draft 长约束。端侧还要先确认 draft 常驻内存与温区；接受率低会更慢。过关看**同机 Decode 有效 tok/s 与 TTFT 是否被拖垮**；加速比以本机 A/B 为准，别抄云侧「1.5×~3×」当合同。

- **TensorRT-LLM 配置** — `--speculative_decoding_mode` + `--max_num_tokens≥max_draft_len`
- **Medusa 头** — target 尾部挂多并行预测头，省独立 draft 模型

### FlashAttention / FMHA：开关

④ 讲过 tiling + online softmax，目的是少把中间结果写回远端。命令侧先核对：`--attention_plugin`、`--context_fmha`、`--gemm_plugin` 是否与 dtype/平台匹配。Prefill 常更受益；Decode 仍要盯 KV 体积与带宽。Jetson 上规则 GEMM/量化可评估 DLA，Attention 默认走 GPU——用 profiler 证落点，别拿 TOPS 口头保证。

- **TRT 开关** — `--gemm_plugin`、`--attention_plugin(auto/bf16/fp8)`、`--context_fmha`
- **算子定位法** — GEMM/Attention/RMSNorm·RoPE·Softmax，用 ncu 看 DRAM vs SM

### 性能指标定义与权衡 (TTFT/TPOT/ITL/throughput)
- **指标定义** — TTFT 首 token，ITL/TPOT 相邻 token，throughput=单位时间生成总量

**权衡**
- 加大并发→吞吐↑但 TTFT/ITL 变长；SLO(p95) 决定并发上限
- llama.cpp: TTFT=prompt eval、TPOT=eval/ generated tokens

### 调优实验框架：基线→profiler→逐项改参
- **核心纪律** — 先 nsys/ncu 拿证据再改参，每次只改一个变量
- **四步闭环** — Step0 FP16 基线 → Step1 profiler 定位三类瓶颈 → Step2 逐项改参 → Step3 compare_logits 精度护栏
- **记录模板** — 日期/GPU/模型量化/参数/TTFT/ITL/throughput/瓶颈算子/收益

### TensorRT-LLM 量化选型与命令流
- **位宽选型** — 端侧 Ampere 推荐 W4A16(AWQ/GPTQ)>INT8 W8A8>FP16；H100 用 FP8
- **硬件限制** — Ampere 无 FP8；LLM 瓶颈在权重带宽 weight-bound，INT4 砍 1/4 搬运
- **命令流** — convert_checkpoint.py --dtype float16 → trtllm-build → quantize.py --qformat int8_sq/awq/gptq → trtllm-build
- **KV Cache 量化** — 独立开关 --kv_cache_quantization int8/fp8，先权重量化再单开 KV 量化

### 量化精度评估与精度-资源权衡矩阵
- **PPL 评估** — eval_ppl.py stride=512；PPL 增幅 <0.5 且 top-1 一致率 >0.9 可用
- **logits 对比** — 固定同 prompt+seed，比 KL/余弦/L2/top-k，四条对比纪律
- **权衡矩阵** — bench_matrix.py 对(模型,位宽)跑 PPL+KL+显存+吞吐，低位宽 ADC 递进验证
- **校准坑** — calib_size≥64 条、校准/评估隔离、max_seq_len 须与部署一致

### GGUF 格式与 Q4_K_M / K-quant 量化
- **GGUF 格式** — llama.cpp 格式，含 Header/Tensor Info/Tokenizer/超参/Tensor Data
- **量化谱系** — Q2_K~200MB...Q4_K_M~300MB(推荐甜点)、Q6_K、Q8_0 近无损
- **Q4_K_M 原理** — 4bit+K-quant(重要性分组)+Medium 粒度；super-block(32)→sub-block(16) 算 scale/zp
- **LLM 量化特殊性** — 激活离群值用 per-group(32/64/128) 比 CV per-channel 更细
- **转换命令** — convert_hf_to_gguf.py --outtype q4_k_m，或 convert.py→llama-quantize

### llama.cpp/GGML 编译后端与 kernel dispatch
- **三层关系** — GGUF 存权重、GGML 张量计算库、llama.cpp 上层推理应用
- **GGML 分层** — 计算层 ggml_compute_forward_* → 后端 CPU(SSE/AVX2/AVX512)/CUDA/Vulkan/Metal/SYCL
- **kernel dispatch** — 编译期 #if 静态选 AVX512>AVX2>NEON；CUDA 运行时 m==1 走 GEMV、大矩阵走 cuBLAS
- **CMake 参数** — -DGGML_CUDA=ON、-DCMAKE_CUDA_ARCHITECTURES="75;80;86;89" 指定架构
- **运行卸载** — -ngl 32 分层 GPU offload；llama-bench/llama-cli 实测对比

### Jetson/Orin 平台特性与端到端工程化交付
- **平台认知** — Orin Ampere 无 FP8、共享 DRAM；LLM 直吃 HF 权重绕开 ONNX
- **engine 强绑定** — engine 须 Orin 本地 build；版本约束 TRT-LLM↔CUDA↔TRT↔JP 版本对齐
- **build 参数** — max_seq_len 是 KV 上界、chunked_context 必开、gemm_plugin float16、context_fmha
- **量化纵深** — 端侧 INT4 AWQ/GPTQ 首选，KV Cache 默认不量化单独决策
- **一键化流水线** — stages 独立脚本+YAML 声明+断点续跑+统一退出码+.done.json 失效重跑
- **端侧验收** — NPI 思维：内存预留≥20%、引擎 sha256 一致、latency 达标、ppl_delta 可解释、manifest 三层指纹

## ⑨ Transformer/VLM · AI编译器

> 第 9 站：展开 Attention / FFN / VLM 结构；板上算子见 ⑩。下一站 ⑩。

⑧ 把 Prefill/Decode 和开关摸清了；上板前还得认清 Attention / FFN / VLM 在导出图里长什么样、编译器把它们吃成什么。本章摊开结构与 AI 编译器栈（LLVM / MLIR / TVM / 图编译），板上算子落到 ⑩。

### Transformer 编码器-解码器整体架构与核心算子
- **架构组成** — Encoder 输出上下文表示，Decoder 自回归生成，串接 Cross-Attention
- **Self-Attention** — Attention(Q,K,V)=softmax(Q·K^T/√d_k)·V，除以 √d_k 防梯度消失
- **计算五步** — 线性投影 Q/K/V→Q·K 得分→√d_k 缩放→softmax→加权求和 V
- **Multi-Head** — MultiHead(Q,K,V)=Concat(head_1..head_h)·W_O，多子空间增强表达
- **FFN** — FFN(x)=GELU(xW_1+b_1)·W_2+b_2，隐藏维为 mlp_ratio=4 倍
- **PyTorch 实现** — qkv=nn.Linear(d,d*3) 一次投影，split 出 q/k/v，masked_fill 因果 mask

### Decoder-only 架构与因果注意力（Causal Attention）
- **架构特点** — GPT/LLaMA/Qwen 用 Decoder-only：RoPE、单向因果、Next Token Prediction
- **因果 mask** — 上三角 -inf 掩码，代码 torch.tril+masked_fill(float('-inf'))
- **Pre-LayerNorm** — 归一化先于子层：x=x+Attention(LN1(x)); x=x+FFN(LN2(x))
- **完整 Decoder** — embedding→n_layers Block→ln_f→lm_head，可权重绑定、std=0.02 初始化
- **自回归生成** — 取最后位置 logits/temperature 后 torch.multinomial 采样拼接

### MHA / GQA / MQA 注意力变体与 KV Cache 优化
- **三种变体** — MHA 每头独立 K/V；GQA 组内共享 KV(LLaMA 2/3)；MQA 全部头共享一组 KV
- **KV Cache 复用** — 每步只算新 K/V 并 append，复用已生成 token 的 K/V
- **显存公式** — 每 token=2×num_layers×num_heads×head_dim×dtype_size，例 LLaMA-7B 约 512KB/token
- **优化方向** — GQA/MQA 减 KV、INT8/INT4 量化 KV Cache、PageAttention 分页减少碎片

### RoPE 旋转位置编码与 RMSNorm 归一化
**RoPE**
- q'_m=R_m·q_m、k'_n=R_n·k_n，θ_i=base^(-2i/d)，base=10000
- 相对位置感知、超长外推、不影响向量范数

- **绝对位置编码** — PE(pos,2i)=sin(pos/10000^(2i/d_model)) 余弦对，因 Self-Attention 置换不变

**RMSNorm**
- RMSNorm(x)=γ·x/RMS(x)，RMS(x)=√(mean(x²)+ε)，只除 RMS 不中心化
- 端侧更偏好 RMSNorm，效果与 LayerNorm 相当

### FFN 变体：SwiGLU 激活与门控线性单元
- **SwiGLU** — Swish(x·W_gate)·(x·W_up)·W_down，Swish(x)=x·sigmoid(x)

**对比**
- 三权重 W_gate/W_up/W_down，中间维取标准 FFN 的 2/3、效果更好
- GELU 用于 ViT/标准 Transformer，SwiGLU 属现代 LLM 主流

- **编译视角** — 逐元素激活可与 Linear 算子融合(Fusion)减少中间回写

### VLM 视觉语言模型：ViT 视觉编码器与交叉注意力对齐
**ViT 结构**
- Patch Embedding 切 16×16 patch + [CLS] token + 绝对位置编码 + Encoder Block
- 输出 [B,197,768]（224/16=14，14²+1 个 CLS）

- **交叉注意力** — Q 来自文本(query)，K/V 来自图像(encoder_hidden_states)
- **跨模态对齐** — ITC 对比学习拉近匹配对、ITM 二分类匹配、LM 以图像为条件生成
- **部署难点** — patch token 长、cross-attention 二次复杂度，需处理变长与融合访存

### Swin-style 窗口注意力与 Patch/Mel 频谱前处理
- **WindowAttention** — 相对位置偏置 relative_position_bias_table，仅局部窗口内算注意力
- **PatchEmbed** — nn.Conv2d 切 patch 映射高维，音频频谱图转 token[B,N,C]
- **HTS-AT 链路** — 音频→STFT→LogmelFilterBank→Mel 谱[B,1,T,Mel]→PatchEmbed
- **Swin 五步** — Cyclic Shift→Window Partition→Window Attention(attn_mask)→Reverse→残差 FFN

### AI 编译器栈：前端 IR / 中端优化 / 后端 Codegen 与多层 IR
- **三层流水线** — 前端(AST)→中端(IR+优化 Pass)→后端(指令选择/寄存器分配/机器码)
- **IR 的意义** — 前端先到 IR 再接多后端，N×M 组合变 N+M
- **多层 IR** — TVM: Relay IR(图)→TIR(张量/循环)→目标代码(CUDA/LLVM IR/ARM)
- **与工程对应** — ONNX GraphProto 即框架无关 IR；ONNX 图优化=IR 上 Pass；TensorRT Engine=codegen 产物

### LLVM IR、Pass 与后端 Codegen（指令选择/寄存器分配）
- **LLVM 流程** — Clang→LLVM IR→Pass 优化→Codegen(ARM/x86/NPU 机器码)
- **IR 结构** — Module→Function→BasicBlock→Instruction；SSA 每变量只赋值一次、phi 节点汇合
- **Pass 本质** — IR 上『读入→改写→吐出』：常量折叠/死代码消除/算子融合(conv+bn)/量化(float→int8)
- **后端 Codegen** — 指令选择(SelectionDAG 选目标指令)→指令调度→寄存器分配(L0A/L0B/L0C)
- **硬件知识** — ISA、SIMD(ARM NEON/x86 AVX/NPU Vector)、内存层级 L1/L2/L3→DDR 差10倍+

### MLIR 多层 IR 与 Dialect / Lowering / Pattern Rewrite
- **多层 IR** — Model IR→Tensor IR→Linalg IR→SCF IR→LLVM IR→机器码，信息逐层保留
- **Dialect** — 前缀算子词表：linalg./scf./gpu./vector./arith./func.，编译即翻译 Dialect
- **Lowering** — 从高层抽象展开细节，例 conv2d 展开为三层 for 循环嵌套
- **Pattern Rewrite** — 匹配 IR 模式→替换新模式的优化机制，Add 重结合等
- **quant dialect** — IR 显式表达量化 bit/per-tensor/scale/zero_point，支持混合精度

### TVM 计算表达与 Schedule：TE/TIR、Tiling/向量化/AutoTVM
- **端到端链路** — ONNX→Relay IR→TE/TIR→AutoTVM→LLVM/CUDA/NPU 代码生成
- **TE 表达** — te.placeholder/te.reduce_axis/te.compute 描述计算；Schedule 描述循环策略
- **Schedule 原语** — Tiling 分块、reorder 重排、vectorize 向量化、parallel 并行
- **Tiling 原理** — L1 1ns/L2 4ns/主存 100ns，切 32×32 块塞进 L1 反复用
- **AutoTVM** — XGBTuner+XGBoost 搜索、tune(n_trial=100) 挑最快、apply_history_best 应用
- **昇腾类比** — AutoTVM≈tbe_tune_main 搜算子、Record≈知识库/hit_bank、relay.quantize≈DDK 量化
- **Roofline** — 计算密度(FLOP/Byte) 判瓶颈：左侧带宽受限、右侧算力受限

### GPU 并行模型与异构计算：CUDA/Warp/Shared Memory、Triton 与 Tensor Core 图编译
- **GPU 思维** — 80+ SM、Warp=32 线程锁步、Shared Memory SM 内高速区、Global 共享仓库
- **CUDA 组织** — Grid/Block/Thread，i=blockIdx.x*blockDim.x+threadIdx.x
- **关键概念** — im2col 转矩阵乘、Bank Conflict、warp shuffle、Tensor Core 加速矩阵乘
- **Triton** — 编译器友好 block-level 语言，@triton.jit、tl.load/tl.store，自动映射 warp/线程
- **编译关联** — Flash Attention=Tiling 算法；Conv+BN+ReLU 融合=Pass 落地；vLLM kernel 与 CUDA 相关

### 端侧图编译与算子编译：XLA/昇腾 CANN/OMG、IREE/RISCV 后端落地
- **两大任务** — 图编译(图级 Pass)与算子编译(Tiling/向量化/指令选择)
- **昇腾链路** — ONNX→DDK 量化(dopt_so)→OMG→OMC；te_fusion 即 NPU 代码生成器
- **实用技巧** — OMC 文件大小判断成功(20MB 正常 vs 37MB 异常)；tbe_tune_main 搜最优写 hit_bank
- **IREE/RISCV** — IREE 是 MLIR 编译器；Triton→IREE→RISCV 后端，定制写 HAL 驱动
- **推理框架** — llama.cpp(GGML q4_K_M)、vLLM(PagedAttention)、SGLang(prefix caching)、Continuous Batching

## ⑩ 平台实战 · 昇腾/麒麟板

> 第 10 站：昇腾 / 麒麟上板实操。下一站 ⑪。

> **开板前先分清后缀**：板子上最常见翻车是后缀混了——云边 Ascend 常用 **ATC → `.om`**，本文座舱 / HiAI DDK 链是 **OMG → `.omc`**（如 omc0428）。`.omc` ≠「压缩版 `.om`」，混了必 Load fail。对照表见 ⑭。MindIE 是云侧大模型引擎，**不是**座舱 Runtime。

现场就按这条座舱 OMC 链走：CANN / 达芬奇、OMG 离线转换、TBE 知识库、麒麟实测与 mslite NPU 改造。

### 座舱 OMC 链：各站产物与验收

| 站 | 产物 | 失败长相 | 验收一句 |
|----|------|----------|----------|
| 量化+转 | `model.omc` | 体积异常（经验：~37MB 相对正常~20MB）；重名节点 | OMC 可被测速脚本 Load |
| 上板测速 | `model.csv` / `op.csv` | `Load model failed`（先对 DDK 再对图） | ≥5 次平均；弃冷启动前几次 |
| 精度 | 比对日志 | 余弦崩、任务 mAP/指标掉点 | 与 ONNX 基线过团队门禁 |
| 麒麟边界 | `.omc` / `.ms` 预编译物 | `buildModel_v2` 在线编译 **-1** | 只走离线预编译；shape 与硬编码一致 |

### 昇腾NPU硬件与达芬奇架构认知（Ascend 310B1 / 3D Cube）
**昇腾NPU硬件与达芬奇架构认知**
达芬奇AICore三单元：Cube(16×16×16 3D Cube，单次4096乘加)、Vector、Scalar，配L1 Buffer。
- 310=8TOPS；310B1=11TOPS主力；910训练、610车载
- GPU训练→ONNX→量化→OMC→盒子；训练FP32、推理INT8

### CANN 计算架构与 DDK 工具链（FE/GE/TBE/Runtime + 版本匹配）
**CANN 计算架构与 DDK 工具链**
CANN≈CUDA：FE解析ONNX建图、GE图优化调度、TBE算子调优、Runtime驱动内存。
- DDK(01-13稳定/04-28主用)匹配版本与芯片；内置built-in/custom知识库
- omc0428环境source set_tbe_env_ddk.sh提供te_fusion

### OMG/omg 离线模型转换（ONNX→OMC，含量化 compress_conf 与 tune_dump）
**OMG/omg 离线模型转换**
OMG转ONNX→OMC：FE解析+GE图优化(Conv+BN+ReLU融合)+TBE查询知识库编译kernel。
- omg --framework 5(ONNX)；--compress_conf量化；ENABLE_TUNE_DUMP出tune_dump
- 成功OMC约20MB/异常37MB；重名节点跑fix_quant_onnx.py

### TBE 算子搜索完整流程（tune_dump→筛选→搜索→知识库→重转）
**TBE 算子搜索完整流程**
tune_dump筛选耗时算子→tbe_tune_main搜索→知识库→重转，是推理提速核心。
- EVB_CONF=device.ini+REPEAT_TUNE=True；全量约24h，针对性>1000μs约2h/个
- 实测YOLOv11 25ms→7.5ms约70%，需后台跑20-60h

### 知识库机制与 hit_bank 命中调优（built-in/custom JSON）
**知识库机制与 hit_bank 命中调优**
知识库JSON记"算子名":[编译耗时,性能分]，分子built-in(DDK自带)与custom(搜索生成)。
- hit_bank命中即用优化实现；check_hit_bank.py统计命中率
- 生命周期：搜索→05合并→scp分发→转OMC自动采用→新DDK重搜

### 昇腾盒子 ADB 部署、测速与结果排错（model.csv / op.csv）
**昇腾盒子 ADB 部署、测速与结果排错**
adb connect/push/shell管理盒子；02_measure_omc_speed.py出model.csv/op.csv。
- 冷启动40ms取第4次后稳定值约8-9ms；测速≥5次取平均
- Load model failed回omc0428重转；compare_onnx_and_omc.py精度验证

### 麒麟V90 座舱板实测与能力边界（HiAI .omc / NNAPI / 离线编译）
**麒麟V90 座舱板实测与能力边界**
麒麟V90支持HiAI(.omc)/mslite(.ms)/NNAPI三通道，不支持在线编译须离线预编译。
- buildModel_v2在线编译失败(-1)；GPU机缺hiaicc/converter_lite
- hiai_compile_run硬编码1x3x160x320；量化+减max_seq_len做预算

### MindSpore Lite NPU 改造与板上 Transformer 实测（TinyBERT + kMSDeviceTypeKirinNPU）
**MindSpore Lite NPU 改造与板上 Transformer 实测**
mslite API：MSDeviceType{KirinNPU=2}、SetProvider(npu,'ge')、MSModelPredict/GetInputs。
- Context加KirinNPU+CPU回退；按真实shape填input_ids/attention_mask/token_type_ids
- NDK交叉编译；循环计时输出Avg ms+内存占用

---

## ⑪ 端侧工程化 · 量产落地

> 第 11 站：LLM 量产全流程，与 ⑮ 的门禁对接。下一站 ⑫。

板上冒烟过了，还过不了量产：ISO 26262 / OTA、批次一致性、温区 derating 会把「能跑」打回原形。本章把云到车的 LLM/VLM 全流程、QNN / TensorRT Edge-LLM、语音降噪端侧实现串起来，细门禁对接 ⑮。

### 端侧LLM/VLM从云端训练到车机量产全流程
**端侧LLM/VLM量产全流程**
云端训练(FSDP/DeepSpeed)→导出ONNX→量化(AWQ/GPTQ W4A16+KV Cache)→平台编译(QNN/TensorRT/OMG)→真机测速→发布。
- CV/LLM差异：动态shape、KV Cache双层量化、自回归循环管理
- export→quant→compile→benchmark→validate脚本链，30min→5min可复现

### 端侧LLM推理全链路基础：Tokenizer到采样的完整链路
**端侧LLM推理全链路：Tokenizer到采样**
Tokenizer(BPE/BBPE，Qwen省30-50%token)→Embedding→Transformer(MHA+SwiGLU)->LM Head→采样→更新KV Cache。
- 词表决定embedding内存：Qwen 151K vs LLaMA 32K
- 端侧INT4采样：Top-P 0.9+Temp≤0.5-0.7；开Repetition Penalty 1.1-1.2

### INT4低比特量化与混合精度部署（GPTQ/AWQ/W4A8/FP4）
**INT4低比特量化与混合精度部署**
LLM瓶颈=权重搬运：DDR 30GB/s下7B FP16≈467ms/INT4≈117ms；W4A16甜区(Q4_K_M)。
- GPTQ用Hessian二阶补偿；AWQ保salient channel加权缩放，优先
- SmoothQuant迁移离群值；FP4(E2M1)零附近最优，Blackwell/Hexagon原生

### LLM推理性能优化方法论：Prefill/Decode与Roofline分析
**LLM推理性能优化：Prefill/Decode与Roofline**
Prefill=GEMM计算密集(FlashAttention)，Decode=GEMV访存密集(INT4/KV Cache量化/投机采样)。
- Roofline ridge=算力/带宽；Decode算术强度<50 ops/byte即memory-bound
- 三层profile：算子>30%、带宽>80%、利用率<50%；Prefill仅占10-20%

### KV Cache管理与量化（端侧LLM显存关键）
**KV Cache管理与量化**
KV Cache缓存Key/Value，大小=2×layers×heads×d×seq×dtype，7B/seq4096约2GB。
- KV INT8(per-token)压2x损失<0.5pp；INT4压4x损失1-2pp
- PagedAttention分block按需分配+block table，vLLM/TRT-LLM已实现

### Qualcomm QNN工具链全流程与昇腾经验迁移
**Qualcomm QNN 工具链全流程**
ONNX→qnn-converter(DLC)→PTQ(--do_quantize/act_bw/input_list)→qnn-context-binary→qnn-net-run。
- 对标：converter≈OMG、HMX(INT4/8)≈Cube、Context Binary≈OMC、ADX混合精度
- ONNX语言/校准集/三层profiling思路与昇腾互通

### TensorRT Edge-LLM核心机制与桌面版差异
**TensorRT Edge-LLM 与桌面版差异**
桌面版单次前向出.plan engine；Edge-LLM专为自回归：GEMV访存+KV Cache+In-flight Batching。
- 三大机制：In-flight/Continuous Batching、PagedAttention、KV Cache量化
- QNN/Edge-LLM/llama.cpp三足鼎立；vLLM依赖CUDA不上端侧

### 车规量产工程约束与量产思维（ISO 26262/OTA/批次一致性）
**车规量产工程约束与量产思维**
ISO 26262分ASIL-A~D，座舱DMS/OMS/语音通常ASIL-B；LLM输出需安全兜底。
- 高温-40~85℃降频留余量、功耗5-15W、批次一致性、寿命10-15年
- OTA AB分区双备份可回滚；以PP50/P99作为性能基线

### 端侧LLM/VLM模型选型与Roofline预筛（内存/带宽/算力三维博弈）
**端侧LLM/VLM模型选型与Roofline预筛**
LLM选型瓶颈=带宽+KV Cache，INT4起步(7B约3.5GB)；1.5B-3B务实区间。
- Roofline预筛：内存→Decode带宽→Prefill算力三步
- 语音助手Decode>20 token/s；蒸馏+量化恢复精度主流

### VLM多模态端侧部署与差异化精度策略
**VLM多模态端侧部署与差异化精度策略**
ViT一次前向+LLM自回归串联；ViT-L/14于384²产生256 visual token拉长KV Cache。
- ViT量化敏感留INT8；LLM用W4A16/W4A8，LM Head/Embedding FP16、KVCache INT8
- 量化致特征对齐偏移/幻觉↑，需整体精度验证

### 语音降噪与增强的端侧实现（RNNNoise/DCCRN/FullSubNet）
**语音降噪与增强的端侧实现**
频域掩码：STFT预测M→Ŝ=M·Y→ISTFT；掩码分IBM/IRM/cIRM(修正幅度+相位)。
- RNNNoise：Bark 22维+3层GRU约70K参数，可跑Cortex-M7
- DCCRN复数卷积+LSTM；FullSubNet全带+子带双路径最优

### 语音增强端侧部署优化：流式推理与INT8动态量化
**语音增强流式推理与INT8动态量化**
逐帧缓存递归状态h/c实现流式<40ms；RNN须动态量化(权重INT8激活FP32)。
- OnnxWrapper展平h/c为输入输出，opset14+dynamic_axes
- MCU限GRU+INT8参数<200K；INT8后PESQ降<0.1可接受

### 算子级性能调优与三层Profiling方法论
**算子级性能调优与三层Profiling方法论**
ENABLE_TUNE_DUMP→op.csv测速→筛>1000μs→tbe_tune_main→知识库→重转。
- 先确认工具链正常(OMC 20MB正常vs 37MB异常)再优化
- 双层评估：逐层余弦>99%+任务级mAP；敏感层诊断量化坍陷

---

## ⑫ 主线串讲实验课 · CV 通路与排错复盘

> 第 12 站：用 CV 全链路把前面概念串成可跑通路，做排错复盘。下一站 ⑬。

〇–⑪ 概念已经按序讲过；本章不是重开第 1 课，而是把它们收成一条可跑的 CV 链（环境 + PT→ONNX→INT8），专门暴露「概念会对、命令对不上」的缝。深度学习零基础只作速查。三平台命令、OMC 搜索、llama.cpp 档位当练习本；厂商对照见 ⑭，LLM 尺子进 ⑬。

### 机器学习与深度学习基础概念
**机器学习与深度学习基础概念**
训练=前向+反向+优化器；数据集/Epoch/Batch Size/Loss/Learning Rate，以监督学习为主。
- 架构CNN(YOLO/ResNet)/RNN/Transformer/GCN；激活ReLU/Sigmoid/Softmax
- 过拟合增数据/正则/早停；Warmup/Cosine Annealing/Step Decay

### 模型部署核心概念：转换、量化、算子与异构硬件
**模型部署核心概念**
链路PT→ONNX(通用语言)→芯片格式OMC/engine/RKNN/mslite→设备部署。
- 量化FP32→INT8体积4x提速3x，q=round(r/scale)+zero_point；PTQ vs QAT
- CPU/GPU/NPU(3D Cube)分工；DDK含OMG/TBE/知识库JSON

### 30天端侧部署工程师学习路线图（五阶段路径）
**30天端侧部署工程师学习路线图**
五阶段Day1-30：ML基础→CV部署→昇腾NPU→Transformer/VLM→LLM端侧→专家整合。
- 三平台：本地PC+GPU(4×V100)+昇腾座舱板分工协作
- 里程碑：YOLOv11 INT8(相似度>99%、18.2→4.6MB)；每日检查点+复习清单

### 三平台实验环境与部署工具链（SSH/ADB/Conda）
**三平台实验环境与部署工具链**
本地tzone_venv、GPU服务器(CUDA12.1)、KirinV90座舱板(Android12)三端，SSH/ADB操作。
- conda只改自有tzone；SSH须source conda.sh；pip用阿里云镜像
- adb push模型+smart_parking_test测速；ls /vendor/lib64 grep npu

### CV模型部署全链路：PT→ONNX→INT8量化→精度评估
**CV模型部署全链路**
PT→ONNX(opset=11、NCHW)→gpu_quant.py/dopt_so.py量化→compare脚本余弦评估。
- FP32 18.2MB→INT8 4.6MB；余弦>99%合格，低于加exclude_nodes
- per-channel/per-tensor粒度权衡；KD蒸馏/剪枝补精度

### 昇腾NPU部署：OMC转换、DDK/OMG与算子搜索知识库
**昇腾NPU部署：OMC/DDK/OMG与算子搜索**
OMG --target omc --framework 5出model.omc；ENABLE_TUNE_DUMP出op.csv供搜索。
- 流程：测速→check_hit_bank→筛选>1000μs→04_search(20-60h)→05追加→重转
- 达芬奇3D Cube做NPU推理；DDK版本匹配KirinV90

### LLM端侧部署：llama.cpp、GGUF量化与KV Cache机制
**LLM端侧部署：llama.cpp/GGUF/KV Cache**
llama.cpp(C++/GGML_CUDA)使用GGUF，convert_hf_to_gguf→llama-quantize。
- K-quant Q4_K_M/Q5_K_M/Q8_0；AWQ(通道保护)vs GPTQ(Hessian)
- prefill计算密集/decode访存密集；KV Cache显存瓶颈，GQA共享KV

### 车载芯片平台与推理引擎技术栈全景
**车载芯片平台与推理引擎技术栈全景**
芯片谱系Orin(FP16/INT8/INT4)/Thor(FP8/NVFP4)/J5/J6 BPU/TDA4 DSP/高通NPU。
- 引擎：TensorRT-LLM/Edge-LLM、vLLM(PagedAttention)、llama.cpp、MNN
- 编译器LLVM/MLIR/TVM/XLA；岗位主线：端侧LLM部署/AI编译器/量化

### AI编译器、核心算子实现与LLM优化技术
**AI编译器、核心算子实现与LLM优化技术**
图优化算子融合(Conv+BN/QKV/Flash)+常量折叠+内存池；Attention分块tiling。
- LLM专用：GQA、投机采样、Continuous Batching、结构化输出
- 档位FP16~NVFP4；PTQ/QAT(STE)+敏感层混合精度；指标TTFT/P99/Jitter

### 端侧部署常见问题排错（全链路调试知识库）
**端侧部署常见问题排错**
全链路排错：SSH(Test-NetConnection)/Conda(阿里镜像)/ADB(winget)均已覆盖。
- ONNX：opset降11/onnxsim简化/预处理HWC-BGR-255一致
- OMC：check_hit_bank→04_search→05更新；LLM设HF_ENDPOINT镜像；踩坑记notes周记

---

## ⑬ LLM / VLM 补全（2024–2026）

> 第 13 站：定内存 / TTFT / 对齐等口径，并补 2024–2026 的 LLM/VLM 内容。下一站 ⑭。

基准日 2026-09-15。数字只给公式和数量级，不编未公开实测。

## ⑬-0 勘误（相对原文不精确/过时处）

**显存/内存公式口径**
- 原文多处写 `2×layers×heads×d×seq×dtype`：`heads` 须为 **KV 头数**（GQA 的 `num_kv_heads`），不是 Q 头。
- LLaMA-7B「约 512KB/token」对 **MHA + FP16** 量级大致成立；对 **GQA 模型（如 Qwen2.5-7B）** 应按 `num_kv_heads` 重算，通常显著更小。
- 「7B/seq4096 约 2GB」仅在特定 MHA/FP16 假设下成立；GQA+INT8 KV 时数量级常降到数百 MB 级，部署前用公式重算。

**量化档位与硬件**
- 「H100 用 FP8 / Ampere 无 FP8」方向对；端侧 Orin=Ampere 仍以 **W4A16** 为主。
- 「FP4(E2M1)…Blackwell/Hexagon 原生」需拆开：**NVFP4** 偏 NVIDIA Blackwell；**MXFP4** 为 microscaling 族，部分 NPU/新卡逐步支持；骁龙 HTP 以厂商文档/算子表为准，勿默认「凡 FP4 皆原生」。
- 「W4A8 甜区」易误导：端侧 **GPU/llama.cpp** 常见甜区是 **W4A16**；**W4A8** 需激活 INT8/FP8 路径与 kernel，多数手机 NPU 未必就绪。

**投机采样与加速数字**
- 「实测常见 1.5×~3×」为云侧/带宽未饱和常见区间，**非端侧保证**；端侧常因双模型内存、接受率低而净收益≈0。
- Medusa/Lookahead 在 TRT-LLM 有开关；端侧量产需单独评估草稿头内存与接受率，勿照搬桌面数字。

**引擎与平台表述**
- 「vLLM 依赖 CUDA 不上端侧」大体成立；边缘可用 **TRT Edge-LLM / llama.cpp / ExecuTorch / QNN**，不要把 vLLM 当车机主路径。
- 「Q4_K_M ~300MB」仅对应 **很小参数量**（约亚十亿级）量级感；**7B Q4_K_M 通常 ~3.5–4.5 GiB 级**，勿与体积表混用。
- 词表「Qwen 省 30–50% token」依赖语种与任务，中文场景常更省，不要写成全局定律。

**VLM**
- 「ViT-L/14 @384² → 256 visual token」仅为一种配置示例；动态分辨率/AnyRes/pixel shuffle 下 token 数可变，应按实际 preprocessor 统计。
---

## ⑬-1 2024–2026 端侧 SLM / 小 VLM 选型

### 选型三维（先筛再测）
**内存墙 → 带宽墙 → 体验墙**
- 内存：权重+KV+视觉编码器+runtime 是否装进可用 DRAM（常留 ≥20%）。
- 带宽：decode ≈ 每 token 扫一遍权重；INT4 权重是端侧默认杠杆。
- 体验：TTFT、稳态 tokens/s、长上下文是否掉帧/降频。

### 文本 SLM 家族速览（端侧视角）
**Qwen2.5 / Qwen3 小尺寸**
- 0.5B–3B：手机/车机助手主力候选；7B：旗舰 SoC + INT4 + 短上下文。
- GQA + 大词表：中文 token 效率高，但 **embedding/lm_head 内存** 不可忽视。
- Qwen3：注意思考/非思考模式与 chat template 差异，端侧要固定模板版本。

**Llama 3.2（1B/3B 等）**
- Meta 明确面向端侧的小尺寸线；生态（GGUF/ExecuTorch/MLX）成熟。
- 英文强、多语言看词表与数据配比；许可证与商用条款需单独过审。

**Phi（小参数高密度）**
- 参数少、合成/精选数据路线；适合工具调用、短上下文问答。
- 长上下文与多语言需实测；端侧常作 **draft 模型** 或垂直助手。

**Gemma（2B 级等）**
- Google 小模型线；注意版本间架构/词表变更。
- 与 Android/AICore 生态近，车规外场景可评估。

**MiniCPM 等国产小模型**
- 面向端侧优化的系列常见；关注量化友好度与中文对话一致性。
- 选型以 **同硬件对照基线** 为准，勿只看参数量。

### 小尺寸 VLM
**InternVL / Qwen-VL / MiniCPM-V 小档**
- 端侧优先：**视觉编码器可 INT8 + 语言塔 W4A16** 的组合。
- 视觉 token 数决定 TTFT 与 KV；选「支持 token 压缩/动态分辨率」的版本更友好。
- OCR/单据场景：分辨率与视觉塔精度优先于语言塔再压比特。

### 端侧 MoE：小专家是否可行
**可行条件**
- **激活参数**小（如每 token 只激活数个专家）→ 算力友好。
- **总权重**仍大 → 内存/Flash 驻留压力大；端侧常需专家卸载或只部署稠密小模型。
- 路由导致专家切换 → 缓存局部性差，decode 带宽更伤。

**工程结论（数量级思维）**
- 手机/车机 **并发=1、内存紧张**：优先稠密 1.5B–3B INT4。
- MoE 仅当：总权重可放进存储层级、运行时只驻留激活专家、或云端协同。
- 「小专家数 × 大总参」不等于端侧友好；看 **常驻内存** 而非 FLOPs 宣传。

### 选型清单（可直接进评审）
- 任务：闲聊 / 工具调用 / RAG / OCR-VLM / 多语。
- 硬约束：峰值 RAM、DDR 带宽、NPU 算子表、温控功耗墙。
- 软件约束：chat template、工具 JSON schema、安全兜底。
- 基线模型：同尺寸 2–3 个候选 + 同一量化档 + 同一 prompt 集。

---

## ⑬-2 量化新格式与端侧真实约束

### GGUF：K-quant 之外的 IQ-quant（I-quant）
- **K-quant 回顾** — 超块/子块层次 scale，Q4_K_M 仍是通用默认甜点（兼容性+速度可预期）。

**IQ-quant 要点**
- 更激进比特分配 + **importance matrix（imatrix）** 校准，常见如 IQ4_XS、IQ3_*、IQ2_*。
- 同体积往往 **更省空间或同空间更高保真**；代价：解量化/kernel 更复杂，部分后端 **prefill/decode 更慢或支持不齐**。
- 工程建议：量产默认 Q4_K_M/Q5_K_M；内存极紧再试 IQ4_XS，并用 **本机 llama-bench + 任务集** 验收，勿只看 PPL。

### FP8 / FP4 族：MXFP4 · NVFP4 · FP4 · FP8
**FP8**
- E4M3/E5M2 等；云侧 Hopper+ 成熟；端侧 Orin 无原生 FP8 Tensor Core，勿当默认。
- 作激活/KV 时，关注是否有对应 GEMM/attention 路径。

- **NVFP4** — NVIDIA 侧 FP4 方案（偏 Blackwell 代际）；与 TRT-LLM 量化表绑定，Ampere 边缘卡通常不可用。

**MXFP4（Microscaling）**
- 块内 FP4 值 + 多级 scale（microscaling）；部分新 NPU/框架出现 W4A8_MXFP 类路径。
- 端侧采用前先查：**芯片算子是否支持、scale 布局是否匹配、校准工具链是否闭环**。

- **落地口诀** — 有啥算力用啥格式；**格式≠精度保证**，无 kernel 就等于软件解量化回高位，带宽收益蒸发。

### BitNet / 1.58-bit
- **概念** — 权重极值低比特（含三值/1.58bit 路线），靠专用或高度优化内核换带宽。

**端侧现实**
- 需模型本身按该范式训练/蒸馏，不是任意 LLM 一键 PTQ。
- 无专用内核时，解码可能不升反降；列为 **预研项**，非 2026 车机默认量产路径。

### 算法族：HQQ · QuaRot · SpinQuant · AQLM · QQQ
- **HQQ** — 快速 PTQ、校准开销相对低；适合快速出 INT4 候选，再上任务集精筛。

**QuaRot / SpinQuant**
- 用旋转/正交变换改善量化友好性（压低异常值难度）；常与权重/激活量化组合。
- 部署要保证 **推理侧旋转融合进权重或在线旋转可融合**，否则多一次大矩阵。

- **AQLM** — 加性码本类极低比特，压缩狠；端侧解码内核与工程链不如 GPTQ/AWQ/GGUF 普及。
- **QQQ 等** — 权重-激活联合或特定 W4A8 配方的研究/工程变体；选型看目标硬件是否吃 W4A8。

**实用优先级（端侧）**
1. AWQ/GPTQ → GGUF K-quant（CPU/GPU 通用）
2. 需更压体积 → IQ-quant + imatrix
3. 芯片原生 FP8/FP4 → 跟厂商配方
4. 旋转类/AQLM/BitNet → 有内核与精度闭环再上

### W4A16 vs W4A8 vs W8A8：NPU/GPU 真实约束
**W4A16（权重 4bit，激活 16bit）**
- **端侧 GPU / llama.cpp 主流**：降权重带宽，激活保持 FP16/BF16，实现简单。
- Decode 仍 memory-bound，但收益稳定可预期。

**W8A8**
- 权重+激活都 INT8：算力墙场景（大 prefill、有 INT8 Tensor/NPU）更香。
- 激活离群值敏感；常配 SmoothQuant/旋转/敏感层回退。

**W4A8**
- 理论兼顾权重带宽与激活算力；需要 **W4A8 GEMM** 与激活量化在线开销可控。
- 许多端侧 NPU **只高效支持 W8A8 或 weight-only**；无路径时退化为 W4 解量化 + A16/A8，收益打折。

**选型表（文字）**
- 手机 CPU/GPU 通用推理：优先 W4A16。
- 高通 HTP/强 INT8 NPU + 大 prefill：评估 W8A8。
- 宣称 W4A8：先跑算子支持矩阵，再谈精度。
- KV 量化与权重量化正交：单独开关、单独回归。

---

## ⑬-3 推理算法补全（端侧视角）

### Chunked Prefill
- **动机** — 超长 prompt 一次性 prefill 峰值激活/临时缓冲爆炸；切成 chunk 限制峰值。

**端侧要点**
- 降峰值内存，可能略增总 prefill 时间；车机并发=1 时主要为了 **防 OOM/防抖动**。
- 与 paged KV 搭配：边算边写入 KV block。

### Prefix Caching / Prompt Cache
- **前缀缓存** — 相同 system/工具前缀的 KV 块复用，多会话或多轮命中时省 prefill。
- **Prompt cache（引擎侧）** — 对整段 prompt 哈希命中则跳过重复计算；端侧会话少时收益看前缀是否稳定。

**端侧限制**
- 缓存本身占 RAM；前缀一变（时间戳、用户名、动态政策）即失效。
- 安全：缓存键须包含 **模板版本+LoRA id+采样无关的前缀字节**。

### 🃏 Speculative Decoding 端侧限制
**双模型内存**
- draft + target 同时驻留；1.5B+7B INT4 可能直接打穿手机预算。
- 自推测/Medusa 头可减轻「第二套完整权重」，但仍占激活与额外头参数。

**接受率与带宽**
- 端侧 decode 已极 memory-bound；接受率低时验证开销浪费带宽。
- 草稿过强/分布不匹配 → 频繁拒绝；温度高、任务开放域 → 接受率下降。

### Medusa / EAGLE / Lookahead
- **Medusa** — 在目标模型上挂多头并行猜多 token；省独立 draft 权重，改头训练/导出链。
- **EAGLE 类** — 用特征层/草稿头做更准的多步猜测；实现与导出复杂，端侧需确认引擎支持。
- **Lookahead** — 窗口/雅可比式多候选验证；TRT-LLM 等有模式开关，端侧要量内存与图固定 shape。
- **落地建议** — 先保证单模型 INT4 达标；再试点 **无第二权重** 的头式投机；最后才上双模型。

### Constrained Decoding / Structured Output
- **用途** — JSON/工具调用/正则文法约束，降低乱格式。

**端侧成本**
- 每步掩码 logits 或 FSM 前进：CPU 开销与实现复杂度上升。
- 与投机采样组合困难：草稿 token 可能违反约束，接受逻辑更复杂。

- **实践** — 工具调用优先：**JSON schema 约束 + 校验重试**；约束解码作增强而非唯一正确性来源。

### 投机采样与温度/采样冲突
**冲突点**
- 验证阶段按目标分布接受/拒绝；高温度、高随机性 → 接受率↓。
- top-p/top-k/min-p 改变候选集，需与投机实现一致，否则分布不匹配。

**工程纪律**
- 投机路径固定采样配置做接受率曲线；开放域闲聊高温度时，允许关闭投机。
- 不要假设「开了投机一定更快」。
---

## ⑬-4 Tokenizer · Chat Template · Stop Token 端侧坑

### Tokenizer 与词表内存
**内存构成**
- `vocab` 字符串/合并规则 + embedding 表 +（常另计）lm_head。
- 大词表（如 150K+）：embedding≈`vocab×hidden×dtype`；INT4 主权重省下的内存可被词表吃回。

**多语言 BPE**
- 中文友好词表 → 同句 tokens 更少 → prefill 更短、KV 更小。
- 多语混杂：稀有语种被切碎 → 序列变长，端侧体感「同模型变慢」。

### Chat Template
**必须对齐**
- 与训练一致的 `chat_template`（含 special tokens、role 标记、思考链开关）。
- 服务端改模板、端侧未改 → 拒答、乱角色、工具调用失败，常被误判为「量化坏了」。

- **版本管理** — 模板哈希进模型包 manifest；OTA 时模板与权重同版本发布。

### Stop Token / 特殊符号
**常见坑**
- 只停 `EOS` 不停工具结束符 → 多说话或截断工具参数。
- 多 EOS / 用户自定义 stop 与字节级 BPE 边界不一致 → 漏停。
- VLM：图像占位 token 数量必须与视觉塔输出一致，否则错位生成。

**检查单**
- encode→decode 幂等抽样；特殊 token id 表打印进日志；对比 HF `apply_chat_template` 字节级一致。
---

## ⑬-5 端侧 RAG

### Embedding 模型量化
**原则**
- 向量维度与召回指标（Recall@k）优先于极限压缩。
- INT8 常可接受；INT4 需看召回跌幅，重要域用 FP16 embedding + INT4 LLM。

- **运行时** — embedding 多为短前向，NPU/CPU 均可；与 LLM **分进程/分会话加载** 可削峰。

### 向量库（端侧）
**sqlite-vec 等**
- 嵌入式向量检索：随 SQLite 走，运维简单，适合万～十万级向量。
- 大语料仍需分片、倒排/产品量化（PQ）或云端检索 + 端侧生成。

**工程要点**
- 预计算向量落盘；查询延迟计入 TTFT 预算。
- 元数据过滤（车型/地区/权限）先于 ANN，减少无效召回。

### Rerank 是否上端
**建议**
- 轻量交叉编码器可上端，但延迟与内存叠在 LLM 前。
- 默认策略：**端侧 bi-encoder 召回 Top-k（小 k）→ 规则/轻重排 → LLM**；重型 rerank 放云或省略。
- 车机：宁可召回略糙 + 强 system 约束，也不要三次大模型前向。
---

## ⑬-6 LoRA / Adapter 与前缀共享

### LoRA 热切换
- **内存** — 每套 LoRA：约 `Σ 2×rank×(in+out)` 量级（视插层数）；多客制化 = 多份常驻或来回加载。
- **热切换成本** — 换 adapter：权重合并或运行时注入；合并进基模简单但切换慢；运行时多 LoRA 要内核支持。

**多 LoRA**
- 端侧建议：**同时常驻 ≤1–2 套**；其余落盘按场景加载。
- 与量化基座对齐：LoRA 训练所用基座比特/模版须一致。

### System Prompt 前缀共享
- **KV 前缀** — 相同 system + 工具定义 → 共享 KV prefix block，多会话摊销 prefill。
- **与 LoRA 交互** — 不同 LoRA 下同前缀 **不可随意共享 KV**（分布已变）；缓存键含 `lora_id`。

**实践**
- 把稳定长前缀与易变用户话切开；稳定段尽量静态，提升命中率。
---

## ⑬-7 VLM 部署进阶

### Visual Token 压缩
- **Pixel shuffle / 合并** — 空间合并降 token 数，换一定细节损失；TTFT/KV 直接受益。
- **Token prune** — 推理期丢弃低信息视觉 token；需模型/模块支持，简单乱剪会伤 OCR。

### 动态分辨率
- **行为** — 按原图比例分块/缩放，token 数随图变 → **动态 shape 与内存峰值**。

**端侧策略**
- 设上限：最长边、最大 tile 数、最大 visual tokens。
- 编译引擎：固定上限 shape + 运行时有效长度，避免无限涨。

### 视频多帧
- **成本** — 帧数 × 每帧 visual tokens → prefill 与 KV 近似线性涨。

**策略**
- 关键帧采样 + 低帧率；帧间共用语言上下文；必要时帧特征离线缓存。
- 实时视频助手：优先「短窗帧差/稀疏帧」，勿满帧进 LLM。

### OCR VLM 差异化量化
**策略**
- 视觉塔/连接器：偏 INT8 或更高；语言塔：W4A16。
- 读字场景：分辨率与视觉精度 > 语言塔再抠比特。
- 回归集必须含小字/倾斜/表格；仅用通用 VQA 会漏伤。
---

## ⑬-8 采样与安全兜底

### 采样参数（端侧常用）
- **Repetition penalty** — 抑制套娃重复；过大伤语言流畅、过小长文重复。助手场景常见轻微 >1。
- **min-p** — 相对峰值概率设地板，滤掉长尾噪声；比单纯 top-p 更稳的情况存在，需与模板联调。
- **XTC 等** — 激进多样性策略；端侧产品默认慎用，先 A/B 再开。
- **与量化关系** — 低比特使 logits 变「钝」；同样温度可能更胡言或更重复。量化后 **重标定温度/penalty**，勿沿用 FP16 参数。

### 端侧安全兜底
**分层**
1. 规则：关键词/正则/拒答模板/工具白名单。
2. 小分类器：意图/有害/越狱二分类（可 INT8 小模型）。
3. LLM：仅在前两层通过后生成。

- **车规思维** — LLM 非 ASIL 功能安全通道；安全相关动作必须规则/控制器闭环，模型只建议。

### 幻觉与量化
- **机制** — 量化增噪声 → 事实密集、长尾实体更易胡说；RAG 可部分对冲。

**缓解**
- 降温度、强约束格式、引用检索片段、敏感层保留高精度、避免过度 IQ/2bit。
- 评估：事实问答+拒答集，不只报 PPL。
---

## ⑬-9 完整内存公式与典型预算表

### 分项公式（并发=1）
**权重**
- `W ≈ params × bytes_per_weight`
- INT4 常按 ≈0.5 B/param，另加 scale/码本开销（约数%～十几% 量级，视格式）。

- **激活峰值** — 与 batch、hidden、中间 FFN 膨胀、是否 chunked prefill 相关；取 profile 峰值，勿只估一层。

**KV Cache**
- `KV = 2 × L × H_kv × D_h × S × bytes_kv`
- `S = 已用 token（含视觉 token）`；GQA 用 `H_kv`。

- **Runtime / 引擎** — 工作区、算子临时缓冲、内存池碎片；经验预留常需真机标定，量产留安全余量。
- **Tokenizer** — 词表结构 + 预分配字符串；相对 7B 较小，但嵌入式勿忽略。
- **Vision encoder（VLM）** — ViT 权重 + 视觉前向激活峰 + 投影层；可与 LLM **分阶段加载** 削峰。

**总式**
- `Mem ≈ W + KV(S) + Act_peak + Runtime + Tok + Vision(+ LoRA)`
- 可用内存再 ×0.8 作安全系数（温控/系统服务/图形）。

### 数量级示意表（并发=1，INT4 权重，文本模型）

> 仅为数量级尺：假设 GQA、KV 用 FP16、无视觉、未含极端长上下文。真机以公式+profile 为准。

**1.5B INT4**
- 权重：约 0.8–1.2 GiB 级
- KV@2k：通常百 MB 内量级（视层数/头）
- 手机：较舒适；车机入门助手常见档

**3B INT4**
- 权重：约 1.6–2.5 GiB 级
- KV@2k–4k：数百 MB 量级可能
- 旗舰手机/座舱中档；需盯峰值激活与 runtime

**7B INT4**
- 权重：约 3.5–5 GiB 级
- KV@4k：数百 MB～1 GiB+ 量级可能
- 仅高内存座舱/边缘盒；常需短上下文、KV INT8、关双模型投机

### 车机/手机预算思维
- 先定 `S_max` 与是否 VLM，再反推可装参数量。
- 系统服务 + 相机 + HMI 常占数 GB；LLM 只拿「剩余池」。
- 并发=1：优化 TTFT/TPOT 与温控，而非 continuous batching 吞吐。

---

## ⑬-10 路径对比：ExecuTorch · QNN HTP · MLX · llama.cpp

### 文字对比表

| 维度 | ExecuTorch | QNN HTP（骁龙） | MLX | llama.cpp |
|------|------------|-----------------|-----|-----------|
| 主战场 | Android/iOS 端侧，PyTorch 导出链 | 骁龙 NPU/HTP 量产 | Apple Silicon（Mac/iPhone 生态） | 跨平台 CPU/GPU，GGUF 生态 |
| 模型入口 | `torch.export` / 官方 LLM 流程 | ONNX→转换→context binary | 原生 MLX 权重/转换 | HF→GGUF→quantize |
| 量化 | 权重量化+后端委托；跟版本强相关 | AIMET/HTP 约束，INT8/混合精度看算子 | 苹果侧量化与统一内存友好 | K/IQ-quant 最丰富，迭代快 |
| LLM 特性 | 移动委托、逐步完善 KV/生成 API | 需按芯片 SDK 集成自回归与 KV | 统一内存，研发迭代快 | KV、投机、采样、多后端成熟 |
| 性能杠杆 | XNNPACK/CoreML/NPU 委托 | HTP 算力+带宽，图编译与量化对齐 | Metal 与统一内存 | 手工 kernel + 后端 offload |
| 量产适配 | 移动 App 友好；车规看客户基线 | 车机/手机高通方案主候选 | 非苹果硬件不适用 | 产线需自管性能/精度/安全 |
| 代价 | 算子覆盖与委托坑 | 工具链版本写进清单、算子 fallback | 生态绑 Apple | 极致性能需平台相关调参 |
| 何时选 | 要 PyTorch 同源上移动 | 骁龙量产、NPU 必吃满 | 苹果设备演示/产品 | 多平台基线、CPU 兜底、快速量化实验 |

### 选型口诀
- 高通车机/手机：**QNN HTP 为主，llama.cpp 作精度/对照基线**。
- 跨平台快验、桌面/盒子集：**llama.cpp**。
- Android 原生产品化：**ExecuTorch** 评估委托与延迟。
- 仅 Apple：**MLX**。
- NVIDIA 边缘：**TRT-LLM / Edge-LLM**（原文已覆盖，上表作互补）。

### 与原文衔接
- 量化数学、GPTQ/AWQ、FlashAttention、TRT-LLM、QNN 基础流程见 ③⑦⑧⑪。
- 本章补：**选型、新格式、算法边界、Tokenizer/RAG/LoRA、VLM 压缩、采样安全、内存总账、路径表**。

---

## ⑬-11 并入检查清单（工程用）

- [ ] 选型：任务×内存×词表×许可证
- [ ] 量化：格式有内核？W4A16/W8A8/W4A8 何者真加速？
- [ ] 算法：chunked prefill / prefix cache 键设计 / 投机是否装得下
- [ ] 模板：chat template + stop 与训练一致
- [ ] RAG：embedding 位宽 + sqlite-vec 规模 + rerank 去留
- [ ] LoRA：常驻套数 + 前缀缓存键含 lora_id
- [ ] VLM：max visual tokens + OCR 差异化量化
- [ ] 采样：量化后重标定；规则+小分类器兜底
- [ ] 内存：分项公式 + 20% 余量 + 真机 profile
- [ ] 路径：QNN / ExecuTorch / MLX / llama.cpp 职责清晰

---

## ⑬-12 LLM 端到端指标（TTFT / TPOT）

> 验收吵 TTFT 时，先把起停表点说清——本节口径就是 ⑬-12 自身，命令见 ④ / ⑧，不要另起一套课表。平台命令、Genie 编排见 **⑭**。

### 原则
#### 量用户体感，不是 NPU 忙闲
- 时钟从 **请求进入推理热路径** 起，到 **首个可展示 token** 止 = **TTFT**；相邻 token 间隔 = **ITL / TPOT**。
- 端到端必须含：分词、采样、detokenize、上屏。预处理/后处理不进尺子 → 板上绿、产品骂。
- 端侧默认 **并发=1**。云侧「加大 batch 换吞吐」在座舱常是反模式：P50 绿，P99 与壳温一起爆。
- **P50 看体验中枢，P95/P99 看拒收**。稳态 tokens/s 只在功耗墙/温区内测，空调房峰值不当门禁。

#### 口径（定义用，非承诺数字）
- `TTFT ≈ T_tokenize + T_prefill(+视觉编码) + T_first_sample + T_detok_first`
- `TPOT ≈ median(ITL)`；`tokens/s ≈ 1 / mean(ITL)`（须声明是否含首 token）
- 报表必注：`prompt_tokens`、`gen_tokens`、`S_max`、量化档、壳温、热/冷启动

### 检查清单 / 口径表

| 指标 | 起表 | 停表 | 端侧用途 | 勿混用 |
|------|------|------|----------|--------|
| **TTFT** | 请求入推理进程 | 首 token 可展示 | 「第一口」 | 勿用纯 Prefill kernel 时间顶替 |
| **ITL/TPOT** | 上一 token 完成 | 下一 token 完成 | 跟读流畅度 | 不要把含停顿的会话平均当稳态 |
| **tokens/s** | 稳态 Decode 窗 | 同窗 | 温区/功耗回归 | 不要把 Prefill 摊进去报快 |
| **P95/P99** | 同场景多次 | 同左 | 量产门禁 | 勿只报一次 best-of |
| **KV 占用** | 公式见 **⑬-9** | — | 内存墙 | 勿用 MHA 公式套 GQA |

- [ ] Prompt 集冻结：短/中/长三档，**token 数写入报告**（勿用「一句话」）
- [ ] 热启动与冷启动分开报；量产 SLO 通常盯热路径
- [ ] VLM：视觉 token 计入 prompt 长度；OCR 图另列
- [ ] RAG：检索延迟进 TTFT 预算（**⑬-5 / ⑬-19**），或明确「生成段另计」
- [ ] 与 ⑬-12 同一纪律：尺子含前后处理，否则不算验收通过

### 常见翻车点
- **停表点漂移**：有的栈停在「首 logit」，有的停在「首 UTF-8 字」；中文一字多 token，体感差一截。
- **Prompt 长度没写死**：演示数十 token 全绿，车上 system+工具前缀数百 token，TTFT 变差属预期。
- **Decode 报吞吐、用户骂首包**：优化全砸在权重带宽，Prefill 大 GEMM 没吃满加速器。
- **P50 门禁、现场 P99**：热降频、相机抢带宽只出现在尾部；空调房十次平均无意义。
- **数字不可比**：跨机型/跨 SDK 未对齐量化档与 `S`，只可谈数量级，**绝对值需核验**。

---

## ⑬-13 Prefill / Decode 分层 Profiling

> TTFT / TPOT 绿了仍慢时，才进分层：先测稳，再拆 Prefill·Decode；**两段墙拧反是第一大浪费**。融合 / IR Pass 归 **⑯**，此处只分层归因。

### 原则
#### 先分阶段，再分层次
- **Prefill**：整段 prompt 一次（或 chunked）前向 → 更像 **算力墙**，决定 TTFT。
- **Decode**：每步扫权重 + 读写 KV → 更像 **带宽墙**，决定 TPOT。
- 顺序与 ⑬-12 同一棵树：**稳态基线 → 阶段耗时 → 算子 Top-K → 访存/带宽 → 利用率/降频**。
- 端侧并发=1 时，不必先上 Continuous Batching；先问慢在算满还是搬满。

#### 分层看什么
1. **阶段**：Prefill ms vs 单步 Decode ms × 生成长度。长会话里 Decode 累计常更长；用户「第一口」几乎全是 Prefill。
2. **算子**：Attention / GEMM / RMSNorm / RoPE / 采样。Top-K 占比 **需核验**，勿套云侧「Attention 一定过半」。
3. **访存**：权重扫描、KV 读写、中间写回（FlashAttention 类收益见主文档⑧，不在此展开 kernel）。
4. **系统**：CPU 分词、Host–Device 拷贝、核间切换、温控时钟。

### 检查清单 / 分层表

| 层 | 问什么 | 绿的样子（定性） | 下一步 |
|----|--------|------------------|--------|
| 测稳 | 同 prompt、同温、同亲和性，多次可重复 | P50/P99 收得住 | 无基线不谈优化 |
| 阶段 | Prefill 是否吃掉 TTFT | 短 prompt 时 Decode 总时长可超过 Prefill | 长前缀先砍模板/视觉 token |
| 算子 | 是否单核热点 | 无长期霸榜且无融合机会 | 热点再考虑混精/切 backend（**⑯**） |
| 访存 | Decode 是否带宽打满 | 权重 INT4 后 TPOT 明显动 | 无动则查解量化回高位、KV dtype |
| 利用率 | NPU 忙但端到端慢 | Prefill 高、Decode 低较常见 | Decode 别再堆 FLOPs 宣传 |
| 热 | 数分钟后是否掉 tokens/s | 稳态仍过 SLO | 见 **⑬-16**，勿只优化 kernel |

- [ ] Profiling 开关本身有扰动：对比「关采集」的 TTFT/TPOT
- [ ] Chunked Prefill：峰值内存降、总 Prefill 可能升，分开记账
- [ ] Prefix Cache：命中 / 未命中两套 TTFT
- [ ] 工具与产物格式跟栈走；**命令抄主文档⑭，此处不展开**

### 常见翻车点
- **两段混着调**：给 Decode 加更大 batch、给 Prefill 狂压 KV bit，墙对不上。
- **没有稳态**：首次加载、时钟爬升混进平均。
- **只看算子表**：分词在 CPU、拷贝在总线，被误判「模型太大」。
- **开了融合勾仍慢**：中间表写回/未吃到加速器 → 查访存层；**IR 白名单见⑯**。
- **用云侧 Profile 套车机**：vLLM 排队延迟在端侧常不存在。

---

## ⑬-14 精度对齐三站 + chat template

> 板上胡言别先怪量化——对齐要走训练输出 → 导出图 → 板上产物；LLM 多一刀：**模板与分词必须进第 0 站**。词表 / stop 细则见 **⑬-4**，此处只写对齐协议。

### 原则
#### 先对齐「喂进去的字节」，再对齐「算出来的数」
- 板上胡言，优先怀疑 **chat template / special tokens / 思考模式开关**，再怀疑量化。
- 三站必须走 **同一条 greedy 路径**（temperature=0、固定 seed、关闭投机）才能比 logits。
- 站与站之间只改「图从哪来」，不改预处理、模板、tokenizer 版本。
- 任务级（拒答率、工具 JSON 合法率）与张量级（余弦、top-1）分开报，勿用 PPL 单点放行。

#### LLM 三站（对照 CV 的 训练→ONNX→OM）

| 站 | 产物 | 对齐物 | 通过线（定性） |
|----|------|--------|----------------|
| **0 模板** | `chat_template` + tokenizer | 与训练/SFT **字节级**一致 | `apply_chat_template` 哈希写入清单 |
| **1 框架** | HF/PyTorch greedy | 参考 token 序列 + 可选首步 logits | 黄金 prompt 集可复现 |
| **2 导出图** | ONNX / `torch.export` 等 | 同输入下 logits / top-1 | 漂了先查导出与 RoPE/mask |
| **3 板上** | OM / context / GGUF 等 | 同 greedy 输出 | 再谈 PTQ/AWQ 敏感层 |

### 检查清单
- [ ] 模板版本、tokenizer、LoRA id、思考/非思考开关写入 manifest（OTA 同包，见 **⑬-16**）
- [ ] 黄金集：短问答 / 工具调用 / 拒答 / 长前缀；禁止现场随口一句当对齐
- [ ] `eval` + `no_grad` + 固定采样；对齐阶段关闭投机与约束解码
- [ ] 张量级：首步或前 K 步 logits 余弦 / top-1；任务级：字符串或 JSON schema
- [ ] VLM：图像占位 token 数 = 视觉塔输出长度（**⑬-4 / ⑬-7 / ⑬-20**）
- [ ] 端侧常 batch=1；若捆 batch，对照 ⑬-12「多批数值一致」
- [ ] 量化后重标定温度/penalty（**⑬-8**）；对齐阶段仍用 greedy

### 常见翻车点
- **服务端改模板、端侧未改**：角色错乱、工具失败，工单写成「INT4 坏了」。
- **stop 只认 EOS**：工具结束符漏停，「多说话」被当成精度回归。
- **用聊天采样对比三站**：温度一开，token 级对不上是分布问题，不是图转坏。
- **导出站过了、板上漂**：预处理、布局、RoPE 实现差；**不要立刻回炉训练**。
- **GQA / KV dtype 与参考不一致**：短句尚可、长上下文开始胡说 → 先核 **⑬-0 / ⑬-9**。

---

## ⑬-15 端侧 Serving 边界与共板资源账

> 单模绿、共板红，多半是 Serving 边界没画清：MindIE / vLLM 的连续批处理 **不自动等于** 车机 / 手机进程内编排。Genie JSON、ctx-bins 见 **⑭**，此处只画边界与账本。

### 原则
#### Serving 是调度边界，不是「再包一层 HTTP」
- 端侧 LLM 常见形态：**单进程、并发=1、会话内流式 decode**；队列是「拒收/降级」，不是把多路拼成一个 batch。
- 云侧目标 = 吞吐与加速器利用率；端侧目标 = **TTFT/TPOT + 峰值 RSS + 温区**（尺子见 **⑬-12**）。
- 共板：KWS / ASR / 检测 / HMI 与 LLM **抢同一份 DRAM 与 DDR**，不是各报各的「单模能跑」。
- 账本三列：**常驻、忙时峰值、撤台**。只报常驻 → 夜里多模一起炸。

#### 边界怎么切

| 能力 | 端侧默认 | 厚边/云 | 别做的 |
|------|----------|---------|--------|
| 连续批处理 | 通常关 | 主杠杆 | 用云侧并发模型估车机 QPS |
| Prefix Cache | 稳前缀可开；键含模板/LoRA | 多租户命中率高 | 键不含模板版本 |
| 投机解码 | 先过内存账（**⑬-3 / ⑬-9**） | 更常见 | 双模型直接常驻 |
| HTTP Serving | 仅盒子系统可选 | 标配 | 座舱关键路径再套一层网关抖动 |
| 多模型 | 分时/优先级 | 多实例 | 峰值相加当「还够」 |

### 检查清单 / 共板账
- [ ] 常驻：LLM 权重 + KV(`S_max`) + Runtime + 词表（**⑬-9**）
- [ ] 忙时：ASR / 相机 / 视觉塔 / LLM Prefill 峰值是否 **同时** 出现
- [ ] 撤台：会话结束是否释放 KV；切账号/切 LoRA 是否卸峰值
- [ ] 优先级：唤醒词 > 识别 > 生成；LLM 不得堵住倒车影像/仪表
- [ ] 队列：超时是降级到云/本地模板，还是让用户空等
- [ ] 就绪：模型已 load ≠ 首包已热；对外「可对话」探针要对热路径
- [ ] 余量：系统+HMI 先扣，LLM 拿剩余池，再留安全系数（**⑬-9**）

### 常见翻车点
- **单模绿、共板红**：LLM Decode 扫权重时 ASR 正在搬特征，带宽墙叠加。
- **KV 不撤台**：多轮 RSS 只涨不跌，被当成泄漏。
- **把 vLLM 队列语义搬进车机**：P99 变成排队时间，用户感知是死机。
- **Serving 与 HMI 抢加速器**：演示关地图全绿，量产开导航掉 tokens/s。
- **探针只 ping 端口**：图未 load、模板未挂，灰度已切流。

---

## ⑬-16 端云切分 · OTA 整包 · 车规温区对 decode

> 端云切分、OTA 整组、温区 derating——三道闸会直接打到 Decode。ISO / OTA SOP 细则见 **⑮**；本节只把 **LLM decode** 接到闸上。

### 原则
#### 先定验收尺，再定哪段留端
- 留端：低延迟、可离线、隐私；回云：长上下文、重工具、低电量/高温降级。
- 切分键：**延迟 SLO、隐私级、电量/温区、任务类型**，禁止按模型名字拍脑袋。
- OTA 对 LLM 必须 **整组**：权重 + tokenizer + chat template + 引擎/so + manifest。只更权重 = 隐蔽失败。
- 温区打的是 **Decode 带宽与时钟**：室温 tokens/s 是甜点；主机厂温区（常见叙述约 −40℃～85℃，**以规范为准、需核验**）才是正餐。

#### Decode 为何怕热
- Decode 本就是带宽墙；降频 → 有效带宽/时钟一起掉 → **TPOT 先坏**；长 prompt 时 Prefill/TTFT 随后伤。
- 门禁用 **热稳态** 的 TPOT/P99，不是上电数十秒的峰值。

### 检查清单 / 对照表

| 闸 | LLM 要冻住什么 | 回滚带走什么 |
|----|----------------|--------------|
| **端云切** | 本机最大 `S`、是否允许上云、脱敏规则 | 切流开关 + 云端模型 id |
| **OTA 整包** | 权重指纹、模板哈希、tokenizer、Runtime 列车版本 | A/B **整组**；禁止旧模板+新权重 |
| **温区** | 壳温点、持续 decode 时长、功耗墙 | 降级：降 `S`、关 VLM、切云 |
| **安全** | 验签、防回滚、许可证（**⑮**） | 旧包不能开新模型 |

- [ ] 金丝雀：先短 prompt 助手，再放开工具调用 / VLM
- [ ] 回滚演练：整包哈希对得上，且能聊、能停（stop 仍对，**⑬-14** 第 0 站）
- [ ] 温区：至少室温 + 高温稳态；低温同样测时钟/带宽（以实测为准）
- [ ] 功耗：盯持续 decode 墙，不是 Prefill 瞬时尖峰单独过
- [ ] 批次：同指纹多板抽测 greedy 黄金集（**⑬-14**）
- [ ] 切云：超时/过热/OOM 的用户话术与隐私开关

### 常见翻车点
- **全端或全云**：无网死寂，或事事上云把 TTFT 打穿。
- **OTA 只推权重**：tokenizer / 模板留旧版 → 第 0 站失配，像「量化坏了」。
- **引擎 so 与模型解耦失败**：列车版本混用（细节 **⑭**）；表现常是随机崩溃，不像精度问题。
- **室温 SLO 上车**：开导航 + 高温 derating，Decode 结巴；用户骂模型，其实是温区。
- **LLM 当功能安全通道**：生成式只建议；执行必须规则/控制器闭环（**⑬-8 / ⑮**）。

---

## ⑬-17 长上下文 · RoPE 外推 · 滑动窗口

### 先分清三件事
- **位置外推**：训练长度 \(S_{\mathrm{train}}\) 不够用，推理想到 \(S_{\mathrm{infer}}\)。
- **滑动窗口**：注意力只看最近 \(W\) token，KV 常驻可封顶在 \(W\)。
- **KV 预算**：公式见 ⑬-9；GQA 用 `num_kv_heads`，不是 Q 头（⑬-0）。

主文档 ⑨ 已写 RoPE 原理。本节只谈端侧会炸的工程点。④ / 〇：Prefill 偏算力墙，Decode 偏带宽墙——超长 prompt 先砍峰值与常驻，再谈「支持 128K」。

### RoPE 外推（NTK / YaRN）
#### 常见手法
- **NTK-aware**：把 `base` 放大，高频少扭曲。
- **YaRN**：分段插值 + 外推，常再乘注意力温度；编译图多一次 Mul，可能打掉 RoPE fusion（⑯ 同类坑）。
- **线性插值**：简单，长距相对位置更糊，只作对照基线。

#### 端侧纪律
- `rope_theta`、`rope_scaling` 类型与因子进 **manifest**，与权重、chat template 同版本发布（⑪ 指纹 / ⑮ OTA 整组）。
- 无对应 kernel 时外推只是「能生成」，Decode 仍按 ⑬-9 扫 KV；宣称上下文 ≠ 板上可驻上下文。
- `position_ids` 必须与 **已写入 KV 的长度** 一致；滑窗丢块后仍用绝对 pos，错位会重复/乱码，常被误判为量化坏了（对照 ⑬-4 / ⑬-14）。

### 滑动窗口
- 只对 \([t-W,t]\) 做注意力；旧 KV block 可释放。与 ⑬-3 **chunked prefill 正交**：chunked 管激活峰值，滑窗管 KV 常驻。
- 因果 mask 与 sliding mask **模板只认一种**；混用即静默错答。
- 长文档场景：滑窗 + 摘要/RAG（⑬-19）往往比硬外推更可交板。

### 速查
- 先算 `KV(S_max)` 与温控 Decode，再开外推。
- 回归集必须含：短上下文、\(S_{\mathrm{train}}\) 边界、\(S_{\max}\) 三条；只绿短 prompt 是假绿（接 ⑬-22）。

---

## ⑬-18 工具调用 / Function Calling 工程链

相对 ⑬-4（模板 / stop）、⑬-3（约束解码）、⑬-8（白名单）：收成一条可交付链，而不是「模型会吐 JSON」。

### 六站（端侧默认串行）
1. **Schema 进前缀**：工具名、参数 JSON Schema、禁止项写入稳定 system（可吃 Prefix Cache，⑬-3/⑬-6）。
2. **模板固定进包**：`tool` / `tool_call` / 结束符与训练一致；哈希进包（⑬-4）。
3. **生成**：schema 约束解码 **或** 先自由生成再校验；端侧重试次数极小，优先约束。
4. **校验**：缺字段 / 类型错 / 枚举外 → 拒答或一次修复，禁止死循环。
5. **执行**：仅 **白名单**；安全相关动作走规则/控制器，模型只建议（⑮）。
6. **Observation 回填** 再 Decode；并行多工具易叠 TTFT，量产默认串行。

### 与推理算法的冲突
- 约束解码每步掩码 logits，CPU 开销上升；与投机采样叠加时草稿易违规——端侧先关投机，再保 JSON 合法率。
- stop 只停 EOS、不停工具结束符 → 多说话或截断参数（⑬-4）。
- 量化后 logits 变钝（⑬-8）：同样温度更易出坏 JSON；工具路径 **greedy 或低温**，温度在任务集上重标定。

### 验收（不要用闲聊 PPL）
- JSON 合法率、必填字段命中、禁工具调用率、端到端「选对工具」。
- Schema 版本与权重绑同一 digest（⑪）；OTA 禁止「只更工具描述、不更模板」。

---

## ⑬-19 RAG 切块 · 混合检索 · 端侧评测

**相对 ⑬-5 的深化**：⑬-5 管 embedding 位宽、sqlite-vec 规模、rerank 去留。本节补 **切块策略、词法+向量混合、评测协议**。

### 切块
- 按标题 / 句界 / 空行切；代码与表格禁止固定字符硬切。
- `chunk_size` 与 `overlap` 直接决定注入 token 数 → Prefill 与 `KV(S)`（⑬-9）。
- **父子块**：检索用短 chunk，注入 LLM 用父块，省 Decode 上下文。
- 端侧语料常万～十万级：向量 **预计算落盘**；查询延迟计入 TTFT（⑬-12）。

### 混合检索
- 词法（SQLite FTS / BM25）抓专有名词、零件号；向量抓同义改写。
- 融合优先 **RRF**，少上重型交叉编码器——车机避免「召回 + 重排大模型 + 生成」三次前向。
- **元数据先过滤**（车型 / 地区 / 权限）再 ANN，减少无效召回，也减少越权注入（⑮）。

### 端侧评测（任务级）
| 层 | 看什么 | 说明 |
|----|--------|------|
| 检索 | Recall@k / nDCG@k | 切块与 embedding 量化会伤召回；INT4 embedding 必须看跌幅 |
| 生成 | 引用可核对率、拒答率 | 量化 LLM 事实题更易漂（⑬-8），RAG 是对冲不是免检 |
| 体验 | TTFT 含检索 | 检索不进尺子 = 测速报喜 |

校准 / 评测语料 digest 进 CI（⑮）；换切块参数 = 换数据版本，与换权重同等对待。

---

## ⑬-20 VLM 串联时序与异构流水线

**相对 ⑬-7 的深化**：⑬-7 管 visual token 压缩、动态分辨率、视频帧、OCR 混精。本节管 **端到端时序与 CPU/NPU 流水**（异构分派见 ②，VLM 串联见本节与 ⑬-7）。

### ⏱️ 串联时序（并发=1）
相机/解码 → 缩放对齐 → **ViT 一次前向** → projector → 视觉 token 拼进 prompt → **LLM Prefill** → Decode。

- \(\mathrm{TTFT} \approx T_{\mathrm{视觉}} + T_{\mathrm{prefill}}(\text{含 visual tokens}) + T_{\mathrm{排队}}\)。
- 图像占位 token 数 **必须等于** 视觉塔输出个数（⑬-4）；错位会「杯子变碗」，不一定是量化。
- 视觉 token 计入 \(S\)，KV 公式与文本相同（⑬-9）。

### 异构流水
- 预处理：CPU / DVPP / HTP 矢量；ViT：NPU **INT8**；语言塔：W4A16 / HTP GEMM（⑭ Genie 多 binary 常见）。
- **分阶段加载**：ViT 激活峰与 LLM 峰值不要叠在同一帧（⑬-9）；共板峰值按加和验收（⑬-15）。
- 可重叠：下一帧 resize 与当前 Decode 并行；profiler 看的是端到端，不是单算子绿（⑬-13）。

### 与 ⑬-7 的衔接（策略落点）
- 动态分辨率：固定 `最长边 / max_tiles / max_visual_tokens`；编译 **固定上限 shape** + 运行时有效长度（⑭ 动态轴敏感）。
- 视频：帧数 × 每帧 token ≈ 线性涨 KV；实时助手用短窗关键帧，满帧进 LLM 必顶内存。
- OCR：视觉塔 / 连接器偏 INT8，语言塔 W4A16；回归集必须含小字 / 倾斜 / 表格。

---

## ⑬-21 KV 量化与 Paged / Prefix 组合

权重量化（③ / ⑬-2）与 KV 量化 **正交**：单独开关、单独回归（主文档⑧ 常默认不量化 KV）。

### 再写一遍口径
\[
\mathrm{KV}=2\times L\times H_{\mathrm{kv}}\times D_h\times S\times \mathrm{bytes}_{kv}
\]
`bytes`：FP16=2，INT8=1，INT4≈0.5。无对应 Attention kernel 时，KV 量化只省存储，Decode 仍按高位算，带宽收益蒸发（同 ⑬-2「格式≠加速」）。

### 三件套怎么叠
| 杠杆 | 管什么 | 端侧注意 |
|------|--------|----------|
| GQA | 从结构瘦 \(H_{\mathrm{kv}}\) | 选型阶段就定（⑬-1） |
| Paged | 按 block 分配，消掉按 \(S_{\max}\) 预留的空洞 | block 大小以引擎为准；offload 有 host↔device 拉回税 |
| Prefix | 稳定 system/工具前缀锁块，摊销 Prefill | 键含 **模板版本 + LoRA id + KV 量化档**（⑬-3/⑬-6） |
| KV INT8/INT4 | 常驻与 Decode 带宽 | 长上下文任务集回归；勿照搬云侧掉点宣传 |

### 速查
- 组合口诀：**GQA 瘦结构 → Paged 管碎片 → Prefix 摊 Prefill → KV 量化管常驻**。
- 必须测：\(S_{\max}\) 满窗 + 前缀命中 / 未命中；短上下文绿不够。
- 内存余量仍按 ⑬-9 ×0.8，并进 ⑮ 峰值门禁。

---

## ⑬-22 LLM 任务级量化验收与 CI 四站门禁

主文档③ 的三把尺子：张量余弦 / 特征 / **任务**。LLM 的任务 **不是** 只有 PPL：助手看指令遵循，工具看 JSON（⑬-18），RAG 看引用（⑬-19），VLM 看 OCR / 物体名（⑬-20）。PPL 微降但工具全碎 = 假绿。

### LLM 版四站（对照 ⑮ / ⑬-22）
| 站 | 锁定什么 | 失败常见因 |
|----|--------|------------|
| 1 导出 | ONNX / `torch.export` / GGUF 指纹、opset、**chat template 哈希** | 模板漂、动态轴、RoPE 布局 |
| 2 量化 | 校准集 digest、AWQ/GPTQ/K-quant / IQ 配方（③⑦ / ⑬-2） | 校准不像线上话术 / 工具前缀 |
| 3 编译 | ATC / QNN context / TRT 版本列车（⑭） | Attention/RoPE 回落 CPU |
| 4 任务对比 | 固定 greedy 集：JSON / 引用 / OCR | 只比了 logits 或开放域闲聊 |

### 与主文档 ⑮ 门禁对齐
- L0 冒烟出 token；L1 同机 logits 余弦可严、跨平台必须放宽（bit-exact 几乎不可能）。
- **硬门禁 = 任务指标**；余弦只做软告警与归因。
- CI 采样：`temperature=0` / greedy；随机采样只做分布回归。
- 掉点修复阶梯：校准 → 混精 / 敏感层回退 → QAT → 改结构；工具/RAG 集要进校准分布。
- 车规：门禁绿 ≠ ASIL 过；LLM 仍非功能安全通道（⑮）。

---

## ⑬-23 2024–2026 选型与趋势对照（补 ⑬-1）

⑬-1 已给「内存墙 → 带宽墙 → 体验墙」与家族速览。本节补 **具名族 + 可用性口诀 + 年份对照**。下列名称会改版，**立项必须核验版本日与模型卡**（叙述基准日 2026-09-15）。

### 常见族（尺寸以卡片为准）
#### 文本 SLM
- **Qwen2.5 / Qwen3** 小档：中文词表效率高；固定思考/非思考模板（⑬-4）。
- **Llama 3.2** 1B/3B：端侧生态熟；许可证单独过审。
- **Phi-3.5 / Phi-4-mini**：短上下文与工具调用密；长文/多语要实测。
- **Gemma 2/3** 小档：近 Android 生态；架构变更看代际。
- **MiniCPM 系、SmolLM2、OpenELM、MobileLLM、InternLM 小档、Granite 小档**：作同尺寸对照基线，勿只看参数量。

#### 蒸馏小模型
- **DeepSeek-R1-Distill-Qwen**（1.5B/7B 等）：教师推理链会打穿 TTFT/KV；端侧默认关思考或限预算。
- **Llama / Phi 蒸馏线**：蒸馏 ≠ 端侧友好——看学生 **常驻体积 + 是否 GQA**，不看教师榜分。

#### 小 VLM
- **Qwen-VL 小档、InternVL2/2.5 小档、MiniCPM-V、SmolVLM、Gemma 多模态小档、Moondream / FastVLM / PaliGemma 类**。
- 11B 级视觉 Llama、大 MoE（总权重）不当手机默认；MoE 看 **常驻专家内存**（⑬-1）。

### 可用性口诀（评审可念）
**装得下、扫得动、有内核、模板固定、任务绿、证能过。**
- **装**：\(W+\mathrm{KV}+Vision+Runtime \le\) 剩余 DRAM×0.8（⑬-9）
- **扫**：Decode 每 token 扫权重；无 INT4/KV kernel 则假瘦（⑬-2 / ⑬-21）
- **核**：⑭ 算子表有 Attention / RoPE / 采样，禁止静默 CPU 回落
- **模**：chat template + 工具 Schema + stop（⑬-4/⑬-18）
- **绿**：⑬-22 任务四站，不是 PPL alone
- **证**：许可证 + 验签 + OTA 整组（⑮）

### 趋势对照（方向，非 KPI）
| 年附近 | 端侧常见姿态 |
|--------|----------------|
| 2024 | 边缘盒 7B INT4；手机仍以 1–3B 试水 |
| 2025 | 座舱/旗舰手机 1–3B + 小 VLM；GQA、W4A16 成默认讨论基线 |
| 2026 | 新 NPU 的 FP8/MXFP4 **跟厂商配方**；工具调用 + RAG 成为交付项，而不只是会 chat |

选型公式仍是主文档 ⑭：**芯片代际 × 模型类型(CV/ASR/LLM/VLM) × 约束(功耗/内存/时延/ASIL)** → 引擎产物 → 版本列车。工具链名字会换，⑬-9 内存账和 ⑬-22 任务闸可带走。

---

## ⑭ 平台工具链扩展

> 第 14 站：选型树与版本列车，产出 manifest。下一站 ⑮。

量产选型公式：**芯片代际 × 模型类型(CV/ASR/LLM/VLM) × 约束(功耗/内存/时延/ASIL)** → 引擎与产物 → 版本列车与指纹门禁。

⑤ 只让你二选一冒烟跑通；量产立项要的是选型树、版本列车和命名对照——这些放本章。命令骨架勿再反向灌进 ⑤；新 SoC 名录也不在此继续加厚。

---

### 高通：SNPE / QNN / QAIRT / Genie 与产物形态

**命名栈（易混点一次拆清）**
高通端侧 AI 不是「一个 SDK」，而是**多层产品名叠在同一运行时上**；对外文档常换皮，对内最终吃同一套后端 so。
- **SNPE**（Snapdragon Neural Processing Engine）：高层易用 API；主产物 **`.dlc`**；一次 API 可选 CPU/GPU/DSP(HTP)，换后端少改代码。
- **QNN / AI Engine Direct**：细粒度控制面；主产物 **`lib*.so`（图库）** + **context binary**；后端显式加载 `libQnnCpu.so` / `libQnnGpu.so` / `libQnnHtp.so`。
- **QAIRT**（Qualcomm AI Runtime）：底层统一 Runtime；SNPE/QNN/Genie 最终都落到它；系统集成优先 **QAIRT C API**（ABI 稳定），应用层可用 C++ RAII 包装。
- **Genie / GenAI Inference Extensions**：在 QNN 之上做 **LLM/VLM 自回归编排**（Tokenizer、KV、采样、多 binary）；CLI 常见 **`genie-t2t-run`**。
- **选型口诀**：CV/检测快速出包 → SNPE+DLC；要抠算子落点与 HTP 缓存 → QNN+context；要跑生成式 → Genie；要写长期中间件 → QAIRT C。

**Hexagon HTP vs HMX vs HVX（硬件层）**
规格书 TOPS 与部署后端库不是同一张表；必须落到**后端 so + 量化位宽 + profiler 落点**。
- **Hexagon DSP**：矢量/标量底座；历史文档称 DSP runtime；今天多数新图走 HTP 封装。
- **HTP**（Hexagon Tensor Processor）：当前 **NPU 推理主后端**；Genie/QNN 配置里常见 `QnnHtp`。
- **HMX**：Hexagon 内矩阵加速，偏 INT4/INT8 GEMM；**不是第三个独立 SDK**，由 HTP 路径调度。
- **HVX**：矢量扩展，偏逐元素/激活/布局变换；与 HMX 分工「矩阵 vs 矢量」。
- **工程含义**：宣传 TOPS 常含 HMX 峰值；若图大量落 CPU/GPU，实测只有纸面几分之一。
- **验证**：`qnn-profiler` / 厂商 ADX 看逐算子 backend；目标是敏感大算子（Conv/GEMM/Attention 块）上 HTP。
- **坑**：同一 SoC「系统设置开了 AI」≠ 你的 `.so` 链到了正确 `libQnnHtp.so`；App 打进了 stub 或旧版 so 会 silent 回落。

**DLC vs model.so vs Context Binary（产物三段论）**
三者对应**可移植图 → 可 prepare 图库 → 已固化执行上下文**，不是简单新旧替代。

| 产物 | 典型工具 | 本质 | 何时用 | 可移植性 |
|------|----------|------|--------|----------|
| **`.dlc`** | `snpe-onnx-to-dlc` 等 | 图+权重容器；可含量化参数 | SNPE App；或 QNN+`libQnnModelDlc.so` | 同 SDK 列车内较好 |
| **model `.so`** | `qnn-onnx-converter`→codegen | 可执行图库，支持在线 prepare | 开发调试、CPU/GPU 冒烟 | 依赖链接与 ABI |
| **context binary**（`*.serialized`/`*.bin`） | `qnn-context-binary-generator` | 为**特定后端**离线固化的执行上下文 | **HTP 量产主产物** | **与 SoC/固件强绑定** |

典型命令骨架（路径随 SDK 变，**先锁定 QAIRT 版本再抄绝对路径**）：
```bash
# 1) ONNX → QNN
qnn-onnx-converter --input_network model.onnx --output_path model_qnn \
  --input_list input_list.txt

# 2) 量化（二选一）：转换器内 PTQ，或 AIMET 先出量化 ONNX/DLC
# qnn-onnx-converter ... --act_bw 8 --weight_bw 8 --input_list calib_list.txt

# 3) HTP 离线 context（量产）
qnn-context-binary-generator --model libmodel.so --backend libQnnHtp.so \
  --binary_file model_htp.serialized

# 4) 真机/板端冒烟
qnn-net-run --model libmodel.so --backend libQnnHtp.so \
  --input_list input_list.txt --output_dir out/
qnn-profiler ...   # 参数随 SDK，用于逐层耗时
```

DLC→HTP 交叉路径（官方支持，**版本必须齐**）：
```bash
# 例：SNPE 2.15 产出的 DLC 只能配 QNN/QAIRT 2.15 列车（数字仅示例）
qnn-context-binary-generator --model libQnnModelDlc.so --dlc_path model_quant.dlc \
  --backend libQnnHtp.so --binary_file model_from_dlc.serialized
```

- **坑1 · 版本混用**：DLC 与 QNN 跨小版本 → Load fail 或诡异数值。
- **坑2 · 未量化上 HTP**：HTP 路径通常要量化图；FP 图硬推会失败或几乎全回落。
- **坑3 · 绑定**：context binary 换芯片代际、大改 DSP 固件、换 HTP 架构必须重建。
- **坑4 · >2GB 大模型**：老 SNPE 单 DLC 限制与新 QAIRT 大模型路径不同；LLM 走 Genie 多 binary，勿假设单 DLC 塞 7B。
- **量产目录建议**：`onnx/` 源、`dlc/` 可选中间、`ctx/` 发布 serialized、`cfg/` Genie JSON、`manifest.json` 指纹。

**AIMET / AIMET-ONNX（量化侧交接面）**
AIMET 解决「精度怎么压到硬件可吃」；QNN 解决「怎么编到 HTP」；二者中间交接物是**量化 ONNX 或量化 DLC**。
- **AIMET**：PyTorch 友好的 PTQ/QAT、AdaRound、混合精度、逐层敏感度。
- **AIMET-ONNX**：在 ONNX 上插量化节点/导出；适合「训练在 PT、部署只认 ONNX」的流水线。
- **推荐链路**：PT 导出 ONNX → AIMET-ONNX PTQ/QAT →（可选）`onnxsim` → `qnn-onnx-converter` → context binary → `qnn-net-run` 余弦比对。
- **ADX / 混合精度**：敏感层留高位宽，其余 INT8/INT4；与原文「敏感层排除」同思路，工具名不同。
- **已知坑**：假量化粒度（per-channel / per-block）与 HTP kernel 不一致 → 张量余弦尚可、任务指标崩；必须以**任务指标**做门禁。
- **版本**：AIMET 与 QAIRT 兼容表**需核验**当前 Release Notes，勿抄过期 blog 版本号。

**Genie / LLM SDK（生成式专用）**
Genie 把「一次前向 QNN 图」升级为「多步 decode 服务」；没有它，你要自己管 Tokenizer、KV、采样与多图切换。
- **发布包最小集**：一个或多个 **ctx-bins** + `tokenizer` 文件 + **Genie JSON**（dialog / sampler / engine / model）。
- **后端**：`backend.type = QnnHtp`（主）| `QnnGpu` | `QnnGenAiTransformer`（名称以当前文档为准，**需核验**）。
- **CLI**：`genie-t2t-run` 做 text-to-text 冒烟；通过后再接 App/`GenieEngine`。
- **常调旋钮**：`context.size`（KV 上限）、`kv-dim`、`rope-theta`、`spill-fill-bufsize`、`use-mmap`、`mmap-budget`、`cpu-mask`、`n-threads`。
- **多 binary**：大 LLM 常切 prefill/decode 或多段图；JSON 里 `ctx-bins` **顺序错误**会出现「能跑但胡言」而非明确报错。
- **与 llama.cpp**：原型/CPU 回退用 GGUF；骁龙量产 HTP 用 Genie；**权重格式不互通**，两条流水线并行而非互转幻想。
- **验收口径**：TTFT、decode tokens/s、P99、壳温、峰值 RSS；固定 prompt+seed 比 logits/top-1。

**Android 上 QNN HTP 注意事项（真机坑清单）**
x86 主机转换成功 ≠ Android 进程能打开 HTP；中间隔着 **vendor 分区、SELinux、签名、热管理**。
- **库与 ABI**：设备侧需 `libQnnHtp.so`、HTP skel/stub、相关 Hexagon 库；App `abiFilters` 仅 `arm64-v8a`；注意 `extractNativeLibs`。
- **权限**：部分机型 HTP 仅系统/签名应用可访问；先用 `adb push` + 官方 `qnn-net-run` 确认板级通路，再嵌 App。
- **离线 prepare**：量产只发布 context binary；在线 prepare 冷启动长、临时内存高，车机易 OOM。
- **校准域漂移**：`--input_list` 图是 ImageNet、业务是车外摄像头 → INT8 精度「实验室好、路上差」。
- **调度抖动**：`cpu-mask` 与 Android cpuset/热插拔冲突 → tokens/s 方差大；验收抓 P99 与温控曲线。
- **与 NNAPI**：商店通用 App 可走 NNAPI Delegate；车规/座舱要可控性 → **QNN/Genie 直连**。
- **日志**：保留 QNN log / logcat 中 HTP prepare 失败字段；「改了模型却仍报旧错」先查是否加载了旧 serialized 缓存路径。

### NVIDIA：TensorRT 家族、Orin/Thor、DLA、CUDA Graphs

**TensorRT vs TensorRT-LLM vs TensorRT Edge-LLM**
三者共享「TensorRT」品牌，**部署目标与官方支持矩阵完全不同**。

| 组件 | 定位 | 典型产物 | Jetson 态度 | 典型命令/入口 |
|------|------|----------|-------------|---------------|
| **TensorRT** | CV/检测/分割/静态或动态 shape 前向 | `.engine` / `.plan` | ✅ 主路径 | `trtexec --onnx=... --saveEngine=...` |
| **TensorRT-LLM** | 数据中心 LLM（Paged KV、连续批、多卡） | checkpoint+engine 目录 | ❌ **官方不支持 Jetson** | `convert_checkpoint.py` → `trtllm-build` |
| **TensorRT Edge-LLM** | 嵌入式 LLM/VLM 的 **C++** 运行时 | 本机 build 的 engines + native binary | ✅ Orin/Thor 主推 | 随 JetPack/SDK 的 export→build→run |

- **纠正（相对社区传闻）**：Orin 上曾有人折腾 TRT-LLM，**不等于官方量产支持**；Thor 路线明确转向 **Edge-LLM**（随 JetPack 版本演进，**具体 JP 号需核验**）。
- **Edge-LLM 相对桌面 TRT 的本质差异**（与原文⑪对齐并补边界）：内建自回归循环、KV、（视版本）连续批/分页；发布物是**设备相关 engine**，不是「拷贝服务器 plan 就能跑」。
- **CV 与 LLM 双轨**：感知用经典 TRT；对话/VLM 用 Edge-LLM；不要试图把 decode 循环塞进单次 `enqueue` 的静态 engine 硬拧。

**CUDA Graphs（运行时技巧，不是第四个产品）**
把一串 CUDA kernel 启动录成图、一次提交，降 CPU launch 开销。
- **收益最大**：固定 shape 的 decode 步、固定插件组合的 TRT 执行。
- **收益很小/负优化**：每步动态 shape、图结构变、频繁 capture/destroy。
- **与 Edge-LLM**：部分版本内部已用 Graph；业务层勿重复 capture 导致冲突（**需核验**当前 SDK 是否暴露开关）。
- **坑**：Graph 与 stream/context 生命周期绑定；热更新 engine、fork 进程后必须重录。
- **关系**：原文⑤一笔带过 → **深化适用边界**。

**Jetson Orin vs Thor（代际与精度）**
精度是**硅能力**，软件开不出来；engine 与 **SM 架构**绑定。

| 能力 | Orin（Ampere，如 `sm_87`） | Thor（Blackwell 类，如 `sm_110`，SKU **需核验**） |
|------|---------------------------|--------------------------------------------------|
| FP16 | ✅ 基线 | ✅ |
| INT8 | ✅ | ✅ |
| INT4 权重量化（AWQ 等） | ✅ | ✅ |
| FP8 | ❌ | ✅ |
| **NVFP4** | ❌ | ✅（官方材料中的 Thor 甜点档） |
| 内存 | 共享 DRAM/NVMM | 同属嵌入式共享内存，带宽代际提升（**GB/s 需核验板级规格**） |
| LLM 推荐栈 | Edge-LLM 或 llama.cpp；W4A16 为主 | Edge-LLM；优先评估 NVFP4/FP8 |

- **硬约束**：Orin 上 build 的 engine **不能**在 Thor 上 load（反之亦然）；ONNX 可迁移，engine 必须目标板重建。
- **Orin 满频验收**：`sudo nvpmodel -m 0` + `sudo jetson_clocks`；报表注明模式，否则实验室数字不可比。
- **Thor 迁移清单**：重锁 JetPack → 重装 Edge-LLM → 重导量化 → 本机 rebuild → 重跑 PPL/任务集；禁止只换权重文件。
- **关系**：原文⑫点名 Orin/Thor 精度 → **深化为表+迁移纪律**；原文⑧ Orin 无 FP8 → **保持一致**。

**DLA 能力边界**
DLA 是「规则 CNN 加速器」，不是「第二个能跑 Attention 的 GPU」。
- **适合**：Conv/Pool/激活/BN 融合链；INT8/FP16；固定或有限动态 shape 的视觉骨干。
- **不适合**：LLM Attention/采样循环、大量 Gather/Scatter、自定义 plugin 为主的图、高动态序列。
- **调度现实**：TRT 可 GPU+DLA 切分；VLM 里 ViT 或许可切一部分到 DLA，**LLM 主干仍在 GPU**（与原文⑧一致）。
- **反模式**：为刷「DLA 利用率」把图切碎，DRAM 拷贝上涨，端到端反而变慢。
- **验收拆分**：GPU ms / DLA ms / H2D+D2H ms / 端到端 ms；缺拷贝项视为不合格报告。
- **命令线索**：`trtexec` 的 DLA 相关开关（如 `--useDLACore`，**确切参数随 TRT 版本需核验**）+ Nsight Systems 看拷贝。

**NVIDIA 常用可验证命令与产物**

```bash
# CV / 通用 TRT
trtexec --onnx=model.onnx --saveEngine=model_fp16.engine --fp16
trtexec --loadEngine=model_fp16.engine --warmUp=200 --duration=60

# 动态 shape 示例（名称示意）
trtexec --onnx=model.onnx --minShapes=input:1x3x640x640 \
  --optShapes=input:4x3x640x640 --maxShapes=input:8x3x640x640 --fp16

# 环境信息采集
cat /etc/nv_tegra_release
dpkg -l | grep -iE 'tensorrt|cuda-nvcc|cudnn'
```
- **产物**：`*.engine`（发布）、导出用 ONNX（源）、校准 cache（INT8）、Edge-LLM 的引擎目录（LLM）。
- **坑**：主机 x86 TRT 编的 engine 不能当 Jetson 发布物；CI 必须「板端 build」或「同架构 builder 镜像」。

### 华为昇腾：CANN 代际、OM/OMC、ATC vs OMG、MindIE vs Lite

**CANN 代际与「列车」思维**
CANN 是驱动+图编译器+算子+Runtime 的**捆绑列车**；混搭小版本是昇腾部署第一杀手。
- **逻辑分层**：FE 解析 → GE 图优化/调度 → TBE 或 Ascend C 算子 → Runtime/ACL 执行。
- **服务器社区/商用 CANN**：版本线常见 5.x/6.x/7.x/8.x 等；**具体 LTS 以项目锁定的文档为准（勿写死过期号）**。
- **座舱/麒麟 DDK**：原文 `omc0428` 一类**日期环境**与服务器 `toolkit` **不是同一个安装包**；工具名都叫「转模型」，产物与知识库却分家。
- **匹配检查**：板上 `npu-smi`（若有）或厂商版本查询 ↔ 主机转换工具版本 ↔ 模型魔数；缺一不转生产。
- **坑**：用新 ATC 转旧盒子、或旧知识库打新 DDK → Load model failed / 异常体积；先对版本再对图。

**OM vs OMC（纠正隐含误解）**
**不要**把 `.omc` 理解成「`.om` 的压缩版」；二者多为**不同产品线离线格式**。

| 后缀 | 主流工具 | 典型硬件场景 | 加载侧 |
|------|----------|--------------|--------|
| **`.om`** | **ATC** | Ascend 310/310B/910 等云边盒子/服务器 | ACL `aclmdlLoadFromFile` 等 |
| **`.omc`** | **OMG/omg**（原文座舱链） | 麒麟/HiAI 座舱板等定制链 | HiAI / 板端 Runtime / 原文测速脚本 |

- **体积经验**：原文「OMC≈20MB 正常 / 37MB 异常」**只适用于该定制链冒烟**；**.om 不要套用**，改看返回码、GE dump、精度脚本。

**ATC vs OMG（转换器对照）**
| 维度 | ATC | OMG（原文） |
|------|-----|-------------|
| 角色 | CANN 官方张量编译器 | 板端/DDK 离线转换 |
| 输入 | ONNX/TF/AIR/Caffe 等 | ONNX 等（`--framework 5`） |
| 输出 | `*.om` | `*.omc` |
| 量化 | ATC 选项 + 配套压缩流程 | `compress_conf` + `dopt_so.py` 等 |
| 调优 | 算子编译日志、GE/DUMP 图 | `ENABLE_TUNE_DUMP`→`tbe_tune_main`→`hit_bank` |
| 关键参数 | `--soc_version` 必须对芯片 | `--target omc` 等板端参数 |

ATC 最小可验证例：
```bash
atc --model=resnet.onnx --framework=5 --output=out/resnet \
  --soc_version=<SOC_VERSION> --input_shape="input:1,3,224,224"
# 产出 out/resnet.om ；SOC_VERSION 用 npu-smi/文档查询，勿抄错芯片名
```

OMG 侧保持原文流程：量化 → `omg` →（可选）tune → 重转 → `02_measure_omc_speed.py`。

- **选型**：云边 Ascend 推理卡/盒子 → **ATC+.om**；麒麟座舱按已有资产 → **OMG+.omc**。
- **坑**：把 `.om` 当 `.omc` adb push、或反方向 → 必炸；CI 应用 `artifact_type` 字段拦截。

**MindIE vs MindSpore Lite vs HiAI（云侧 vs 端侧）**
- **MindIE**：云/服务器 **大模型推理引擎**（服务化、多卡、调度）；与 CANN **强配套**；对接 MindFormers 等；**不是**手机/座舱轻量 Runtime。
- **MindSpore Lite / mslite**：端侧 Runtime；`.ms`/MindIR；麒麟可 `MSDeviceTypeKirinNPU` + Provider（原文⑩）。
- **HiAI**：麒麟 NPU 厂商接口；可与 mslite、`.omc` 路径并存；在线编译在部分板上不可用（原文已证）。
- **端侧 LLM 现实**：小模型端侧 mslite/HiAI/llama.cpp；大模型 MindIE 上云；端云协同切分，**禁止假设 MindIE 能塞进座舱**。
### 其他 SoC / OS / Web 工具链速览

**地平线 J5 / J6 · BPU**
- **本质**：车规视觉 BPU，CNN/检测/分割舒适区；工具链在 Horizon 开放 SDK 内闭环。
- **产物**：由 ONNX/Caffe 等转出的厂商模型包 + 板端 runtime（具体扩展名随 SDK，**需核验**）。
- **要点**：定点量化与 BPU 指令强绑定；动态 shape、自回归 LLM 非一等公民。
- **选型**：智驾感知 → J5/J6；座舱 LLM/VLM → 另选 Orin/骁龙/云端，勿默认同板硬扛。
- **坑**：用通用 ORT 在 ARM 上「能跑」≠ BPU 加速；必须以厂商工具编译后的剖面为准。

**瑞芯微 RKNN**
- **本质**：消费电子/工控 NPU 栈；主机 **RKNN-Toolkit2**，板端 **librknnrt**。
- **产物**：`*.rknn`。
- **流程概要**：`load_onnx` → `build(do_quantization=...)` → `export_rknn` → 板端 C API/`rknn_bench`。
- **坑**：不支持算子静默拆到 CPU → 延迟数量级变差；用 toolkit 查询 **op support** + 性能分析。
- **LLM**：非主场；若官方无对应 demo，视为高风险预研（**需核验**型号与版本）。

**TI TDA4 · TIDL**
- **本质**：C7x/MMA + **TIDL** 编译；常嵌 Processor SDK / EdgeAI / OpenVX 图。
- **产物**：TIDL artifacts + 图网络配置。
- **擅长**：低功耗、可预测时延的 CNN。
- **边界**：大 Transformer/LLM 基本出局；与「Orin 能上 LLM」不是同类选型。
- **坑**：DSP/A72 异构同步、内存 carveout 配错会导致偶现超时，需按 SDK 内存图检查。

**MTK NeuroPilot · APU**
- **本质**：手机/平板 AI 栈；**NeuroPilot** + **APU**；常见入口 TFLite Neuron Delegate / 厂商转换器。
- **产物**：厂商缓存/编译物（如部分平台 `.dla` 等，命名多变，**需核验**）。
- **坑**：同系列不同 SKU APU 代际差大；商店包必须 **runtime feature detect**，禁止写死「全系同等加速」。

**三星 NPU（Exynos 等）**
- **本质**：多经 **NNAPI / 厂商 Delegate** 暴露；公开文档完整度通常弱于高通。
- **实务**：优先 TFLite 或 ORT+NNAPI；极致性能走 NDA 私有通道。
- **坑**：模拟器无 NPU；One UI/系统升级可改 Delegate → **OS 版本进 manifest**。

**苹果 ANE · Core ML · MLX**
- **ANE**：硬件加速器；不直接写 kernel，经系统框架调度。
- **Core ML**：iOS/iPadOS/macOS 量产格式（`.mlmodel` / `.mlpackage`）；`coremltools.convert`。
- **MLX**：Apple Silicon 研究友好数组框架（统一内存）；适 Mac 实验，**不是** App Store 主交付物。
- **选型**：上架 App → Core ML；Mac 本地 LLM → MLX 或 llama.cpp Metal。
- **坑**：ANE 对动态控制流/大 LLM 受限；长上下文常回落 GPU/CPU；要测真机功耗不是只看 Xcode 模拟。

**Intel OpenVINO**
- **本质**：x86 CPU/iGPU/独显/酷睿 NPU 统一推理栈。
- **产物**：IR（`.xml`+`.bin`）或直接 ONNX；`openvino` Runtime。
- **命令**：`ovc model.onnx`；`benchmark_app -m model.xml -niter 100`。
- **端侧场景**：工控 PC、座舱 x86、边缘盒；与 ARM 手机栈平行。
- **LLM**：GenAI 组件随版本扩展，**模型列表需核验**目标版本。
- **坑**：插件（CPU/GPU/NPU）选错设备；驱动与 OpenVINO 版本不匹配导致 NPU 不可见。

**ARM Ethos-U / 微控制器 NPU**
- **本质**：MCU/AIoT；**Vela** 编译 TFLite 微模型到 Ethos-U 命令流。
- **产物**：经 Vela 处理的 `.tflite` + CMSIS-NN/Ethos 驱动。
- **边界**：KWS、小 ASR、小视觉；与 7B LLM 无交集。
- **关系**：原文语音「Cortex-M7 可跑 RNNNoise」同档 → **补全 NPU 编译器名 Vela**。

**Windows DirectML**
- **本质**：经 DX12 的 Windows ML 抽象；ORT **`DmlExecutionProvider`** / WinML。
- **产物**：仍是 ONNX；加速在 EP/驱动。
- **坑**：笔记本双显卡绑错适配器；WDDM/驱动升级后性能回退，需锁定驱动版本。

**Android NNAPI / NNAPI 2.0**
- **本质**：系统 NeuralNetworks API；TFLite/ORT 经 Delegate 下发到厂商实现。
- **2.0+**：更丰富的控制流与类型支持（随 API level）。
- **坑**：**实现质量碎片化**——同 APK 在 A 机 NPU、B 机 CPU；量产要机型矩阵与超时兜底。
- **与 QNN**：要榨干骁龙 HTP → 直连；NNAPI 适合「能加速更好」的通用应用。

**WebGPU / WebNN**
- **WebGPU**：浏览器 GPU 计算；ORT Web / Transformers.js 等。
- **WebNN**：浏览器 ML API，后端可接系统 ML（视浏览器/OS）。
- **边界**：演示、插件、隐私侧轻量推理；**非车规主路径**。
- **坑**：算子覆盖与配额限制；无法对标 TRT/QNN 的确定性时延。

---

### 跨平台运行时对照

**总览表**
| 运行时 | 主输入 | 典型产物/后端 | 端侧定位 | 让位给厂商栈的时机 |
|--------|--------|---------------|----------|-------------------|
| **ONNX Runtime** | ONNX | 各 EP | 跨平台默认中间层 | 要 HTP/TRT/BPU 极致时 |
| **ORT Mobile** | ONNX | XNNPACK/NNAPI/CoreML… | 包体裁剪的移动部署 | 同上 |
| **ExecuTorch** | `torch.export` | `.pte` + delegates | PT2 直出派 | 产线已锁定 ORT/QNN 时 |
| **TFLite / LiteRT** | TF/ONNX→TFLite | `.tflite` + Delegates | CV/小模型/MediaPipe | 车规私有 NPU 工具链 |
| **MNN** | 多框架 | 自有表达 | 手机 CV 轻量 | 需厂商认证引擎时 |
| **NCNN** | 自有 param/bin | CPU/GPU | 无依赖嵌入 | 需要 ONNX 生态时 |
| **Paddle Lite** | Paddle/ONNX | 端侧 | 飞桨原生产业链 | 非飞桨训练团队 |
| **MediaPipe** | 任务图 | 内嵌 TFLite 等 | 感知流水线框架 | 当「纯引擎」用时会错 |
| **MLC-LLM** | HF 等 | TVM 编译后端 | 跨平台 LLM 原型 | Jetson 量产 Edge-LLM |
| **MLX** | 自有 | Apple Silicon | Mac 实验 | iOS 量产 Core ML |
| **llama.cpp** | GGUF | CPU/CUDA/Metal/Vulkan… | LLM 基线与回退 | 已上 Genie/Edge-LLM HTP/GPU |

**ONNX Runtime / EP（最常见中间层）**
- **本质**：图执行 + **Execution Provider** 分流。
- **EP 例子**：CPU、CUDA、TensorRT、OpenVINO、DML、QNN（视版本）、NNAPI、CoreML。
- **命令/API**：`onnxruntime_perf_test`；`InferenceSession(..., providers=["TensorrtExecutionProvider", ...])`。
- **坑**：声明了 TRT EP 仍可能大量节点 fallback；必须打日志看 **node placement**。
- **策略**：用 ORT 做「两周内可演示」；并行启动厂商编译，用同一套精度脚本对比。

**ExecuTorch**
- **本质**：PyTorch 生态端侧运行时；`torch.export` → `.pte`。
- **适合**：已 all-in PyTorch 2.x 导出链、愿维护 delegate 的团队。
- **坑**：与 ORT 工具链不共享中间件；LLM 支持以官方 zoo 为准（**需核验**）。

**TFLite / LiteRT**
- **LiteRT** 为新品牌延续（文档是否全量替换 **需核验**）。
- **适合**：移动 CV、MediaPipe、微控制器（经转换）。
- **不适合**：把经典 TFLite 当成 7B LLM 主引擎；大模型看 AI Edge 专项或别的栈。

**MNN / NCNN / Paddle Lite**
- **共同点**：包体小、易 JNI/嵌入、偏 CV。
- **差异**：MNN 后端广；NCNN 极简依赖；Paddle Lite 对飞桨最省事。
- **量化**：各有 INT8 工具；不要假设与 TRT 校准 cache 互通。

**MediaPipe**
- **是任务图框架**，不是与 TRT 对等的「推理引擎替代品」。
- **适合**：摄像头→追踪→手势等流水线；模型节点底层可换 TFLite。

**MLC-LLM**
- **本质**：TVM 编译到 Metal/Vulkan/CUDA/WebGPU 等；一模型多后端原型强。
- **Jetson**：可作实验；量产仍优先 Edge-LLM/llama.cpp 等更常见交付路径。

**运行时选型三句话**
1. 需要**厂商 NPU 确定性** → 离开通用 Runtime，上 QNN/TRT/ATC/RKNN/BPU。  
2. 需要**两周多平台演示** → ORT/TFLite/llama.cpp。  
3. 需要**长期双轨** → 通用 Runtime 保底 + 厂商产物冲性能，manifest 里两套指纹。

---

### 选型决策树：芯片 × 模型类型 × 约束

**Step 0 · 约束一票否决（先写进立项页）**
- **内存**：权重+激活峰值+KV+Runtime > 可用 RAM×0.8 → 先砍参数量/位宽/上下文，别先换引擎幻想。
- **功耗**：持续功耗 > SoC 可持续包络 → 拒绝「实验室满频 tokens/s」作 KPI。
- **时延**：交互看 TTFT/P99；智驾看端到端 ms+抖动；浏览器方案直接剔出车规。
- **安全交付**：要离线编译、批次一致、OTA 回滚 → 必须有可哈希发布物（engine/ctx/om(c)/rknn…）。

**Step 1 · 按模型类型进子树**
**CV（检测/分割/分类/关键点）**
- Orin/Thor → **TensorRT**（评估 DLA 是否真划算）；备选 ORT-TRT EP。  
- 骁龙 → **QNN HTP context**；备选 SNPE DLC。  
- 昇腾盒子 → **ATC→.om**；麒麟座舱 → **OMG→.omc**。  
- J5/J6 → **BPU**；TDA4 → **TIDL**；RK → **RKNN**。  
- 泛手机 → TFLite/ORT+NNAPI；失败再厂商私有。  
- **精度门禁**：余弦/ mAP（原文标准可复用）。

**ASR / 语音增强 / KWS**
- 流式小模型 → ORT/TFLite/MNN + 状态 I/O；MCU → Vela/Ethos 或纯 CPU INT8。  
- 中大模型上 Orin → TRT；骁龙 → QNN。  
- **坑**：RNN/状态量化与动态轴；参考原文⑪ 语音 ONNX 展平 h/c。

**LLM**
- Orin → **Edge-LLM（INT4）** 或 **llama.cpp** 基线；**不要**默认 TRT-LLM。  
- Thor → **Edge-LLM + NVFP4/FP8**（以 JP 能力为准）。  
- 骁龙 → **Genie+QNN HTP**；回退 GPU/CPU。  
- 麒麟 → 小模型 mslite/HiAI；大模型云端 MindIE。  
- x86 → OpenVINO / ORT / llama.cpp。  
- 苹果手机 → 小 Core ML 或云端；Mac → MLX/llama.cpp。

**VLM**
- **拆分量化**：ViT/视觉塔 INT8 友好路径；语言模型 W4A16/NVFP4；LM Head/Embedding 敏感则保高精度。  
- **框架**：优先官方多模态支持的 Edge-LLM / Genie；拒绝「只支持静态 CNN」的 BPU/TIDL 硬吞全图。  
- **坑**：视觉 token 拉长 KV；上下文预算按「图+文」重算。

**Step 2 · 芯片族速查（减枝表）**
| 芯片族 | CV 默认 | LLM 默认 | 明确慎用/否决 |
|--------|---------|----------|----------------|
| 骁龙+HTP | QNN context | Genie | 只用 NNAPI 赌车规性能 |
| Orin | TRT±DLA | Edge-LLM / llama.cpp | TRT-LLM 当官方交付 |
| Thor | TRT | Edge-LLM（NVFP4） | 复用 Orin engine |
| 昇腾 310 系 | ATC/.om | 小模型 ACL；大模型 MindIE(云) | 与座舱 OMC 混用 |
| 麒麟座舱 | OMG/.omc、mslite | 小模型 NPU | 在线编译、超大 KV |
| J5/J6 | BPU | 通常否决 7B | — |
| RK 系 | RKNN | 高风险 | 当 Orin 替代 |
| TDA4 | TIDL | 否决 | — |
| 苹果 | Core ML | 小/云 | MLX 直接当 iOS 交付 |
| 酷睿+NPU | OpenVINO | OV/ORT/llama.cpp | 不锁定驱动 |

**Step 3 · 约束微调启发式**
- **最低功耗**：HTP/BPU/DLA/APU/Ethos > GPU > CPU；接受算子覆盖与回落风险，用 profiler 抓回落。  
- **最低抖动**：离线产物 + 定频 + 绑核 + 关睿频/热惊喜；拒 Web/灰盒 NNAPI。  
- **最低内存**：INT4/NVFP4/Q4_K_M + KV INT8；VLM 分塔位宽。  
- **最快上市**：ORT/TFLite/llama.cpp 双周演示 → 并行厂商编译 → 同脚本验收切换。

**Step 4 · 场景化示例（可直接抄进评审）**
1. **座舱语音助手 1.5B～3B，骁龙旗舰，功耗≤8W** → Genie+HTP INT4；llama.cpp 做精度对照；NNAPI 不进量产主路径。  
2. **Orin 行车记录仪检测 + 小 VLM** → 检测 TRT(+可选 DLA)；VLM Edge-LLM INT4；分进程/分优先级，避免 DLA 拷贝拖死 LLM。  
3. **Thor 机器人本地 8B** → Edge-LLM NVFP4；FP16 仅作精度上限对照；engine 板端 build。  
4. **昇腾 310B 盒子抽帧检测** → ATC INT8 `.om`；不要用座舱 OMG。  
5. **麒麟座舱已有 OMC 流水线** → 继续 OMC+知识库；新 LLM 另开端云，不塞同一 OMC。  
6. **RK3588 面板 CV** → RKNN INT8；弃用「ORT CPU 也能跑」当性能基线。  
7. **Windows 工控质检** → OpenVINO 或 ORT+DML；锁定 GPU/NPU 驱动。  
8. **浏览器插件摘要** → WebGPU/WebNN 或 ORT-Web；明确非车规。

**Step 5 · 决策输出模板**

```
芯片SKU: ... | 模型类型: CV|ASR|LLM|VLM | SLO: TTFT=/P99=/功耗W=/内存GB=
主引擎: ... | 发布产物: ... | 备选回退: ... | 否决项: ...
版本列车: 见版本锁定清单 | 指纹: artifact_sha256= ...
Roofline预筛: 通过|不通过(原因)
```

---

### 版本锁定清单：驱动 / 编译器 / Runtime / 模型指纹

**原则**
- **整车升级**：驱动/镜像/SDK/Runtime so/模型 **同一变更单**；禁止「只换模型文件」。  
- **双指纹**：`artifact_sha256`（内容）+ `env_fingerprint`（可跑环境）；缺一不能入库。  
- **可复现**：校准集哈希、量化配置、max_seq_len/shape 全部入账。

**高通勾选清单**
- [ ] SoC 型号、Android API、vendor DSP/HTP 固件只读属性  
- [ ] QAIRT / QNN / SNPE **同列车版本号**（混用则记配对表）  
- [ ] AIMET/AIMET-ONNX 版本、ONNX opset  
- [ ] `qnn-onnx-converter` / `qnn-context-binary-generator` / Genie 版本  
- [ ] 产物：`dlc`/`so`/`serialized`/Genie JSON/tokenizer 的 sha256  
- [ ] 设备与构建机 `libQnnHtp.so` 等哈希对照  
- [ ] `input_list`/校准集哈希、位宽配置  

**Jetson 勾选清单**
- [ ] JetPack/L4T、`nvpmodel` 模式  
- [ ] CUDA / cuDNN / TensorRT / Edge-LLM 版本  
- [ ] GPU SM（`sm_87`/`sm_110`…）、是否 DLA  
- [ ] engine 在**目标板或同架构**重建的证明（CI 日志）  
- [ ] 量化方案（FP16/INT8/INT4/FP8/NVFP4）与 KV 策略  
- [ ] `max_seq_len`、插件开关、shape 优化配置  

**昇腾 / 麒麟勾选清单**
- [ ] 服务器：`npu-smi`、CANN toolkit/kernels、ATC、`soc_version`  
- [ ] 座舱：DDK 日期环境、omg、`compress_conf`、`hit_bank` 哈希  
- [ ] 产物类型字段强制 `om` 或 `omc`（防混）  
- [ ] MindIE 或 mslite 版本（若使用）与 CANN/板端匹配表  

**跨平台 Runtime 勾选**
- [ ] ORT/TFLite/ExecuTorch/MNN… **精确版本**  
- [ ] EP/Delegate 名称；若 EP 转调 TRT/QNN，厂商清单仍要满足  

**`manifest.json` 最小字段**

```json
{
  "model_name": "task_model_id",
  "model_sha256": "...",
  "artifact_sha256": "...",
  "artifact_type": "qnn_context|trt_engine|om|omc|rknn|gguf|tflite|mlpackage",
  "sdk_train": {
    "platform": "qnn|jetson|ascend|kirin|horizon|rknn|other",
    "sdk_version": "...",
    "compiler": "...",
    "runtime": "...",
    "driver_or_jp_or_cann": "..."
  },
  "quant": {"scheme": "w8a8|w4a16|nvfp4|q4_k_m|...", "calib_sha256": "..."},
  "shapes": {"max_seq_len": 4096, "input": "1x3x640x640"},
  "metrics_baseline": {"p50_ms": 0, "p99_ms": 0, "ppl_delta": 0, "map_delta": 0},
  "built_at": "ISO-8601",
  "git_commit": "..."
}
```

**CI 门禁伪流程**

```bash
# 构建结束
sha256sum "$ARTIFACT" | tee artifact.sha256
python gen_manifest.py --artifact "$ARTIFACT" --env-probe

# 门禁
python check_manifest.py --baseline manifest_golden.json --candidate manifest.json
# 失败条件：sdk_train 漂移 | artifact 哈希变但无变更单号 | artifact_type 与平台不符
```

**环境采集命令碎片**

```bash
# Jetson
cat /etc/nv_tegra_release
dpkg -l | grep -iE 'nvidia-tensorrt|cuda'

# 昇腾服务器
npu-smi info
atc --version 2>/dev/null || true

# Android 高通
adb shell getprop | grep -iE 'version|dsp|vendor'
adb shell ls -l /vendor/lib64/libQnn*.so
```

**版本类已知坑**
- 能 load 偶发错数 → Runtime so 与编译 SDK 微版本漂移。  
- OTA 只更模型不更 HTP so → 隐蔽失败；AB 包应绑 **模型+so 清单**。  
- 校准集未指纹化 → 「同开关」不可复现。  
- Thor 脚本打开 NVFP4 却在 Orin 跑 → 构建失败或忽略，必须以日志为准。  
- 昇腾 `.om`/`.omc` 混后缀入库 → 板上难以理解的 Load failed。

---

### 并入与维护约定

- **学习路径**：保留原文⑤「QNN 或 Orin 二选一主攻」。  
- **立项路径**：用本章决策树 + 版本锁定清单出 `manifest.json`。  
- **实战细节**：昇腾搜算子、QNN 命令细参、Edge-LLM 机制仍以原文⑩⑪⑧为准；本章不重复粘贴。  
- **不确定项**：凡标「需核验」者，入库前用**当前**官方 Release Notes 填版本号，禁止把示例数字写成项目基线。

---

### 附录 A · 产物名速查

| 生态 | 中间/发布产物 | 关键命令或入口 |
|------|---------------|----------------|
| 高通 SNPE | `.dlc` | `snpe-onnx-to-dlc`, `snpe-net-run` |
| 高通 QNN | `.so`, `*.serialized` | `qnn-onnx-converter`, `qnn-context-binary-generator`, `qnn-net-run`, `qnn-profiler` |
| 高通 Genie | ctx-bins + JSON + tokenizer | `genie-t2t-run` |
| TensorRT | `.engine`/`.plan` | `trtexec` |
| TRT-LLM | 服务器 engine 目录 | `trtllm-build`（非 Jetson 官方） |
| Edge-LLM | 板端 engines + C++ runtime | JetPack/SDK 流程 |
| 昇腾 ATC | `.om` | `atc --framework=5` |
| 麒麟 OMG | `.omc` | `omg`, `dopt_so.py` |
| RKNN | `.rknn` | RKNN-Toolkit2 |
| OpenVINO | `.xml+.bin` | `ovc`, `benchmark_app` |
| Core ML | `.mlpackage` | `coremltools` |
| TFLite/LiteRT | `.tflite` | converter + Interpreter |
| ORT | `.onnx` + EP | `InferenceSession` |
| ExecuTorch | `.pte` | `torch.export` 链 |
| llama.cpp | `.gguf` | `llama-quantize`, `llama-cli` |
| Ethos-U | Vela 后 `.tflite` | `vela` |
| DirectML | ONNX | ORT `DmlExecutionProvider` |

| 主题 | 关系类型 | 说明 |
|------|----------|------|
| QNN 基础流 | 补全 | 增加 SNPE/QAIRT/Genie/DLC 差异 |
| Jetson LLM | 纠正 | 明确 TRT-LLM 非 Jetson 官方路径 |
| Orin/Thor 精度 | 深化 | 表化 FP8/NVFP4 与迁移纪律 |
| 昇腾格式 | 纠正+补全 | OM≠OMC；补 ATC |
| MindIE | 补全 | 云侧边界，防误推座舱 |
| 多厂商 SoC | 补全 | ⑫ 点名之外系统化 |
| 跨平台 Runtime | 补全 | 定位与让位时机 |
| 选型树 | 补全 | 工具链视角，接 Roofline |
| 版本指纹 | 深化 | manifest/CI 可执行化 |

### 附录 C · 建议评审提问（防止空话过关）

1. 发布物后缀是什么？能否在干净设备上只靠清单复现？  
2. 若 HTP/NPU 全回落 CPU，谁能在 10 分钟内用 profiler 证明？  
3. Orin↔Thor 或 DDK 升级时，哪一条流水线会强制重建？  
4. LLM 方案是否误用了「仅 CV 友好」的 BPU/TIDL/DLA？  
5. manifest 中校准集哈希是否为空？（空则精度不可审计）

---

### 工程深挖：高通链路排错与验收矩阵

**转换期失败 vs 运行期失败（先分类再查）**
端侧高通问题 80% 来自「阶段误判」：把 Runtime 权限问题当成量化问题，或把算子回落当成模型坏了。
- **转换期**：qnn-onnx-converter / AIMET 退出非 0；看不支持算子列表、opset、动态轴、权重 >2GB 切分策略。
- **生成 context 期**：qnn-context-binary-generator 失败；多与后端 so 路径、未量化、HTP config JSON、图过大有关。
- **设备 Load 期**：找不到 skel、SELinux拒绝、context 与固件不匹配、ABI 混用（把 x86 主机 so 推进手机）。
- **数值期**：能跑但结果漂；校准域、Endian/布局、前处理归一化、Genie 多 bin 顺序。
- **性能期**：能出正确结果但慢；算子在 CPU、热降频、未 mmap、KV 超额换出。

**建议的「最小证据包」（提 Bug 给 BSP 必带）**
- 主机：QAIRT 版本、qnn-onnx-converter --version 输出、完整命令行、ONNX sha256、opset。
- 产物：serialized/dlc 大小与 sha256、生成日志尾部 200 行。
- 设备：getprop 关键项、ls -l 全部 libQnn*.so、同一输入的 qnn-net-run 输出 bin、qnn-profiler csv。
- 若 Genie：完整 JSON（可打码路径）、tokenizer 版本、固定 prompt 的参考输出哈希。
- 环境：壳温、	op/dumpsys thermalservice 片段，避免「冷机一次数据」骗过验收。

**HTP 扩展配置里工程师真改的字段**
不同 SDK 字段名略有差异，以下按「意图」记忆，落地时对照当前 htp_backend_ext_config.json（名称 **需核验**）。
- **vtcm / store-banks 类**：片上内存预算；过大失败、过小溢到 DDR 变慢。
- **spill-fill**：权重/激活溢出到 DDR 的缓冲；LLM 易触达，盲目加大涨 RSS。
- **fp16-relax / 精度模式**：换速度时可能动数值；改后强制重跑余弦与任务集。
- **graph-names / 多图**：与 Genie 多 ctx-bins 对齐；名字不一致会跑错图。
- **device-id**：多 NPU/多会话设备号；车机多进程抢同一 HTP 要协调。

**高通 CV 与 LLM 验收门禁（可写进 CI）**
| 门禁 | CV | LLM/VLM |
|------|----|---------|
| 功能 | qnn-net-run 出口与 ORT CPU 参考对比 | 固定 seed 下 top-1 token 一致率或 KL |
| 精度 | 逐层余弦>99% 或业务 mAP 跌幅阈值 | PPL 增量阈值 + 业务集准确率 |
| 性能 | P50/P99 ms，注明输入分辨率 | TTFT、decode tok/s、P99，注明 context |
| 资源 | RSS/ION 峰值 | RSS+KV 峰值，shell 温度 |
| 产物 | context sha256 匹配 manifest | ctx-bins 列表哈希 + JSON 哈希 |

**SNPE 何时还值得用**
- 团队已有大量 DLC 资产、只需 CPU/GPU/HTP 切换、不想到 QNN 细抠。
- 新项目若从零开始且目标是 HTP 极致与 LLM → **直接 QAIRT/QNN/Genie**，减少一次格式迁徙。
- 长期：DLC 仍可通过 libQnnModelDlc.so 进入 QNN，但要接受**双工具链版本锁**成本。
---

### 工程深挖：Jetson / TensorRT 交付纪律

**「能在 PC 上 trtexec」不等于「能在车上跑」**
- 架构：x86_64 builder 产物与 sm_87/sm_110 不通用。
- 库：主机 TensorRT 大版本与 JetPack 自带 TRT 不一致时，即使同 SM 也可能拒载。
- 插件：自定义 *.so plugin 必须与引擎同 TRT/CUDA 列车，OTA 时与 engine 打同一包。
- 许可与镜像：量产根文件系统应冻结 JetPack 点版本，不跟主线随意 apt upgrade cuda。

**INT8 校准与 LLM 权重量化不要混为一谈**
- **经典 TRT INT8**：激活+权重 PTQ，依赖校准集与 calibrator（Entropy/MinMax 等）；适 CV。
- **LLM INT4/AWQ**：主要是**权重**压缩；激活常 FP16；工具链在 Edge-LLM/相关量化脚本，不是随手 --int8 完事。
- **KV 量化**：独立开关与独立精度风险；与权重 INT4 正交（原文⑧已述，这里强调**别写进同一个「开了量化」开关故事**）。
- **Thor FP8/NVFP4**：属硬件友好低精度，迁移时重新评估校准/缩放，不是改个 flag 从 Orin 继承。

**DLA 实操决策序**
1. 先 GPU-only 建基线 engine，测端到端。  
2. 再允许 DLA，看 TRT 层分配与 	rtexec 报告。  
3. 若 DLA 层少、拷贝多，**关掉 DLA** 保留 GPU。  
4. 只有当骨干整段稳定在 DLA 且拷贝可接受时，才进量产配置。  
5. LLM/VLM 的 Attention 路径默认不参与「必须上 DLA」KPI。

**Jetson LLM 双轨策略（降低进度风险）**
- **轨 A（量产目标）**：Edge-LLM + 板端 engine + C++ 服务。  
- **轨 B（对照/回退）**：llama.cpp/GGUF 或官方容器（如社区常用 ollama/vLLM 容器，**是否允许上车需核验许可与资源**）。  
- 精度门禁脚本两轨共用同一 prompt 集；性能 KPI 只认轨 A。  
- 禁止把轨 B 的 tokens/s 写进对客户的 Orin 承诺书，除非合同写明栈。

**Thor 升级检查表（从 Orin 项目迁）**
- [ ] 新 JetPack 发行说明中 Edge-LLM / TRT / CUDA 版本  
- [ ] 是否支持 NVFP4/FP8；量化脚本是否换  
- [ ] 所有 engine 清理重建，禁止「先试试旧 engine」  
- [ ] 内存与带宽假设按新板重算 KV 与 batch  
- [ ] 热设计功耗墙重测，不沿用 Orin 的 nvpmodel 经验数字  

---

### 工程深挖：昇腾双轨（云边 .om vs 座舱 .omc）

**何时开 ATC 轨，何时开 OMG 轨**
- **ATC 轨**：硬件是 Ascend 训练/推理卡或官方盒子、软件是标准 CANN、应用走 ACL。  
- **OMG 轨**：硬件是原文麒麟座舱一类、软件是日期 DDK、产物 .omc、调优靠 hit_bank。  
- **禁止**：同一 Jenkins Job 不加区分地「有什么工具用什么」；Job 名应含 om 或 omc。

**ATC 轨最小闭环**
1. 固定 CANN 与 soc_version。  
2. ONNX 简化与算子审查（动态 shape 单独开专题）。  
3. atc 出 .om。  
4. ACL 或官方样例加载，aclError 非 0 立即停。  
5. 精度脚本（逐层或业务）+ 性能（多次取稳）。  
6. 入库 manifest：artifact_type=om。

**OMG 轨最小闭环（与原文对齐的「工具链视图」）**
1. 量化 dopt_so.py + compress_conf。  
2. omg 出 .omc，看体积冒烟。  
3. 板上测速 model.csv/op.csv。  
4. 超时算子 tbe_tune_main，更新 hit_bank。  
5. 重转 + 回归。  
6. 入库 artifact_type=omc + hit_bank 哈希。

**MindIE 接入判据（避免误用）**
- 需要 HTTP/gRPC 服务化、多卡、连续批、大上下文 → 考虑 MindIE。  
- 需要进程内库、车机 APK/进程、离线小模型 → MindIE **出局**，看 mslite/HiAI/llama.cpp。  
- MindIE 与 CANN 版本表必须逐项对；升 CANN 不升 MindIE 是常见事故（**具体配对表需核验当年文档**）。

**昇腾常见误报与处理**
| 现象 | 更可能原因 | 动作 |
|------|------------|------|
| Load failed | 版本/格式（om↔omc）错 | 查 artifact_type 与 CANN/DDK |
| 结果全零 | 输入 NCHW/均值方差错 | 先 ORT 对齐前处理 |
| 偶发超时 | 频率/温度/多进程抢 NPU | 定频+单实例复现 |
| OMC 体积异常 | 量化/图优化未生效 | 按原文 20 vs 37MB 经验排查 |
| 调优无收益 | 未命中 hit_bank 或搜错算子 | check_hit_bank 统计 |

---

### 工程深挖：跨厂商「能力成熟度」四级

用同一把尺给平台打标，避免「都能跑 AI」的采购空话。
- **L0 实验**：官方 Demo 能跑；无版本锁、无指纹。  
- **L1 工程**：自有模型转通；有精度脚本；版本记在 README。  
- **L2 准量产**：manifest + CI 门禁；P99/温度；OTA 回滚演练。  
- **L3 量产**：批次一致性、ASIL 流程（若需要）、现场日志与远程指纹核对。  
- **建议**：采购或立项时要求供应商演示到 **L2**；仅 L0 Demo 不算选型完成。

对本章平台的**经验性**成熟度（项目可改，不作为厂商排名）：
| 平台 | CV L2 可达性 | LLM L2 可达性 | 备注 |
|------|--------------|---------------|------|
| 骁龙 QNN/Genie | 高 | 中高（看芯片与授权） | Android 权限是变量 |
| Orin TRT/Edge-LLM | 高 | 中高 | TRT-LLM 不计入 |
| Thor Edge-LLM | 高（预期） | 高（NVFP4，随 JP） | 版本需核验 |
| 昇腾 ATC | 高 | 云侧高/端侧低 | 分轨 |
| 麒麟 OMC/mslite | 中高（有资产时） | 低～中（小模型） | 在线编译坑 |
| J5/J6 | 高（视觉） | 低 | |
| RKNN | 中高 | 低 | |
| TIDL | 中高 | 极低 | |
| OpenVINO | 高 | 中 | 驱动敏感 |
| Core ML | 高 | 低～中 | ANE 限制 |
| ORT 通用 | 中（易 L1） | 中 | 极致需让位 |

---

### 工程深挖：跨平台 Runtime 的「EP 落点审计」

通用 Runtime 最大的隐形成本是 **fallback**。没有落点审计，性能数字不可信。
- **ORT**：打开详细日志，导出各 node 的 EP；统计 TRT/CPU 比例。  
- **TFLite**：Delegate 状态与 ModifyGraphWithDelegate 后的节点划分。  
- **NNAPI**：getSupportedOperations 与实际编译结果；失败回退要有超时。  
- **QNN via ORT EP**：确认 EP 版本与直连 QNN 列车一致，勿「ORT 新、QNN 旧」。  
- **门禁**：若关键大算子（卷积/GEMM/Attention）落在 CPU，CI 直接失败（可配置白名单）。

**何时允许「双 Runtime 共存」**
- 应用层：视觉 ORT、语音 TFLite、LLM Genie —— 可以，但 **进程内内存池**要规划。  
- 同一模型两套后端：只允许「灰度对比期」；量产开关必须单一，避免双倍闪存与双倍认证。
---

### 工程深挖：选型决策的反例集（专门打脸空话）

1. **「我们有 NPU，所以 LLM 没问题」** → NPU 可能只会 INT8 CNN；先跑 1 个 Attention block 的 profiler。  
2. **「ORIN 上 TRT-LLM 跑过，量产同样」** → 官方矩阵不支持则供应链不可交付。  
3. **「OMC 和 OM 差个 c」** → 当成不同物种。  
4. **「先上 NNAPI，不行再优化」** → 车规项目会把碎片化行为带进认证，后期更贵。  
5. **「Thor 沿用 Orin engine」** → 必炸或未测路径。  
6. **「MindIE 推到座舱」** → 错层。  
7. **「GGUF 转 QNN」** → 无此免费午餐。  
8. **「DLA 占用率 KPI」** → 可能优化到更慢。  
9. **「只报平均 tokens/s」** → 无 P99/温度=自评作假。  
10. **「校准集临时拍 50 张」** → 未进指纹，季度复现失败。

---

### 工程深挖：版本锁定的仓库布局建议

`
deploy/
  models/
    src_onnx/          # 仅源，大门禁
    artifacts/         # 按平台分子目录 qnn/ trt/ om/ omc/ ...
  sdk_lock/
    qairt.txt          # 精确版本与下载 URL/哈希
    jetpack.txt
    cann.txt
  manifests/
    <model>_<platform>_<version>.json
  scripts/
    build_*.sh
    probe_env.py
    check_manifest.py
  golden/
    metrics_baseline.json
`

- **权限**：artifacts/ 只读发布；重建走 CI，不手改。  
- **清理策略**：SoC 换代删除旧 engine，保留源 ONNX 与量化配置以便重建。  
- **审计**：季度抽检「金机」上 sha256 是否与仓库一致，防现场私换包。

**probe_env.py 应采集的最小键**
platform、os、kernel、driver、sdk、
untime_so_hashes、
pu_firmware、max_clocks、	hermal_status、git_commit。

**发布签名（车规增强，可选）**
- 对 manifest.json + artifact 做签名（Secure Boot 链路或应用层验签）。  
- 启动时验签失败不推理，防刷机篡改。
---

### 场景全文：从立项到首包的两周节奏（示例）

**假设**：座舱语音助手，输入芯片为骁龙旗舰 + 可选云端，模型 2B 级指令模型，SLO：TTFT<800ms，decode≥20 tok/s，功耗≤8W 持续。

**Day 1–2**：Roofline 与内存预算；确认 INT4 后权重+KV 是否装进 RAM；否决 7B。  
**Day 3–4**：llama.cpp/GGUF Q4_K_M 在同设备 CPU/GPU 出功能基线与精度下限。  
**Day 5–7**：AIMET/官方量化 → QNN → context → Genie JSON；打通 genie-t2t-run。  
**Day 8–9**：profiler 抓回落算子；调 VTCM/spill；绑核；热稳态 30min 测 P99。  
**Day 10**：与 GGUF 基线对比业务集；不过则混合精度/改模。  
**Day 11–12**：写入 manifest、CI、OTA 包结构；准备回退到云端 API 的产品开关。  
**Day 13–14**：金机矩阵（高低温度）、文档与「需核验」项清零（填真实版本号）。

**并行否决点**：若 Day7 仍无法在 HTP 加载，立即评估换模或云端，而不是「再试一周同方法」。

---

### 附录 D · 命令与产物「真伪校验」清单

在把命令写进 SOP 前，用这一节防止文档老化。
- 命令是否仍存在：which qnn-onnx-converter trtexec atc omg ovc vela。  
- 帮助是否可解析：--help 中关键子命令是否改名（高通工具曾多次更名）。  
- 产物是否可识别：文件头魔数/扩展名/strings 中版本串。  
- 设备是否可加载：最小样例（官方 inception/resnet/玩具 LLM）先过。  
- 文档日期：SOP 页脚写「对照 SDK 文档日期」，超过 180 天强制复审。

### 附录 E · 名词别名表（防会议各说各话）

| 口头说法 | 建议书面词 | 避免误解 |
|----------|------------|----------|
| 高通 NPU | HTP（或 Hexagon HTP） | 与安卓抽象 NPU 混淆 |
| AI Engine | QNN / AI Engine Direct | 与 Android NNAPI 混淆 |
| TRT | TensorRT（说明是否 LLM） | 与 TRT-LLM/Edge-LLM 混淆 |
| 转 OM | ATC→.om 或 OMG→.omc | 必须说清后缀 |
| 端侧 MindSpore | MindSpore Lite | 与 MindIE 混淆 |
| 苹果 NPU | ANE | 与 MLX 混淆 |
| 浏览器 GPU | WebGPU | 与 WebNN 混淆 |
| 量化了 | 写清 W8A8 / W4A16 / KV-INT8 / NVFP4 | 无法复现 |

## ⑮ 工程化：门禁、车规、OTA、安全

> 第 15 站：门禁、OTA、车规与值班 SOP。下一站 ⑯。

选型与版本列车定了，发布还差一道闸：指标能拦、OTA 能回滚、温区与安全说得清。本章把「能跑」抬到可量产。

## 量产门禁与运行纪律

### 从「能跑」到「量产」的门禁矩阵

**门禁定义**
- 「能跑」= 单机 demo 出 token；「量产」= 矩阵机型 × 全温区 × OTA 可回滚 × 指标可拦发布。
- 每道门禁必须有：**指标名 / 基线 / 阈值 / 采样口径 / 失败动作**。

**量产门禁 Checklist（发布前全绿）**
| 门禁 | 最低要求（示例口径，按项目改数） | 失败动作 |
|------|----------------------------------|----------|
| 精度 | PPLΔ / 任务指标 / 关键场景人工抽检达标 | 拦发布；回退量化或敏感层混合精度 |
| 性能 | TTFT p50/p95、TPOT p95、tokens/s 稳态 | 拦；profiler 定位后改图/KV/并发 |
| 功耗 | 峰值 / 稳态 / 30min 会话平均 ≤ 预算 | 降频策略或减并发；改 derating 表 |
| 热 | 满载 N 分钟壳温/结温不触发不可恢复降频 | 热节流策略验收；改 workload 切片 |
| 内存余量 | 峰值 RSS + KV + 运行时 ≤ 预算，**预留 ≥20%** | 砍 max_seq / 并发 / KV 量化 |
| 冷启动 | 冷加载→首可用推理 ≤ SLA（分机型） | 预热/缓存 engine；拆包体 |
| 包体 | 模型+引擎+词表+资产 ≤ OTA/分区配额 | 差分/拆分发布；引擎与权重解耦 |
| 兼容机型矩阵 | 目标 SoC × OS × 驱动 × 引擎版本全过 | 不合格机型降级路径或剔除矩阵 |

**门禁 SOP（建议嵌入发布流水线）**
1. 锁定 **manifest**（模型哈希 / 量化配置 / 引擎二进制 / 校准集版本）。
2. 跑 **精度套件**（golden + 任务集）→ 写报告。
3. 跑 **性能套件**（冷/热启动各 N 次，记 p50/p95）。
4. 跑 **功耗/热**（固定场景脚本 + 温度探针或平台 API）。
5. 跑 **内存峰值**（含多会话/多模型共存峰值）。
6. 跑 **兼容矩阵**（抽样真机 + 仿真机型表）。
7. 任一门红 → **禁止打 release tag**；只允许出 debug 包。

---

### CI：golden logits、确定性与跨平台容差

**CI 分层**
- L0 冒烟：加载成功 + 1 条短 prompt 出 token。
- L1 精度：固定 prompt×seed → logits / top-k / 采样轨迹摘要。
- L2 性能：固定 shape 压测 TTFT/TPOT/内存。
- L3 夜间：机型矩阵 + 长稳（热/内存泄漏）。

**确定性现实**
- 端侧跨 NPU/GPU/CPU、跨驱动版本，**bit-exact 几乎不可能**。
- CI 目标改为：**同平台可复现 + 跨平台容差带内**。

**容差策略（粘贴用默认值，按模型标定后改）**
- **同机同引擎**：logits 余弦 ≥ 0.999 或 max\|Δ\| 按层预算；top-1 一致率 ≥ 99%。
- **跨平台 / 跨驱动**：余弦 ≥ 0.99；KL / top-5 Jaccard 设软阈值；**任务指标**为硬门禁。
- **采样路径**：CI 默认 greedy 或固定 seed + temperature=0；随机采样只做统计分布回归。
- **失败分级**：硬失败（任务指标崩、OOM、加载失败）vs 软告警（容差边缘漂移需人工看）。

**CI Checklist**
- [ ] golden 输入/输出入库，版本与模型绑定
- [ ] 禁「无 seed 的随机采样」作为唯一精度门
- [ ] 平台维度：`(SoC, OS, driver, engine_ver)` 写进报告文件名
- [ ] 性能用例与精度用例隔离，避免互相抢 NPU 污染数据
- [ ] flaky 用例有重试上限；超限标红而非静默绿

---

### 资源预算模板（量产立项必填）

**内存分层预算表**
| 项 | 公式/口径 | 预算 | 实测 | 余量 |
|----|-----------|------|------|------|
| 权重 | 参数量×位宽/8 | | | |
| 运行时/引擎 | context binary + workspace | | | |
| 激活峰值 | prefill max shape | | | |
| KV-Cache | 2×L×Hkv×d×seq×bytes × 并发 | | | |
| Tokenizer/词表 | 常驻 mmap 或加载峰值 | | | |
| 其他模型共存 | ASR/降噪/VLM ViT 等 | | | |
| **合计峰值** | | | | **≥20% 空闲** |
| SRAM / on-chip | 算子 tiling / 权重切片命中 | 定性+关键算子 | | |
| LLC / 系统 cache | 命中率影响 decode | profiler | | |
| DDR 带宽 | decode 权重+KV 带宽 | ≤ 平台可持续带宽×利用率上限 | | |

**功耗双档**
- **常驻档**（音频唤醒 / KWS / 轻降噪）：mW～低百 mW 级，CPU/DSP 优先，NPU 可休眠。
- **对话峰值档**（LLM decode + 可选 VLM）：按座舱热设计定 W 级峰值与 30s/5min 平均。
- 记录三值：**峰值 / 稳态 / 会话平均**；禁止只报「瞬时最好」。

**预算 SOP**
1. 产品定义会话：最大并发、max_seq、是否多模同开。
2. 填上表 → 与平台规格对比 → 不达标先砍需求再选型。
3. 原型板测峰值内存与功耗 → 回填「实测」列。
4. 余量 <20% 不得进入量产冻结。

---

### 可观测性（端侧 telemetry）与隐私

**必采指标（车机/消费电子通用最小集）**
- 时延：TTFT / ITL 的 **p50/p95/p99**（会话级 + 日聚合）。
- 可靠性：OOM 次数、推理超时、**NPU hang/复位**、CPU fallback 次数与占比。
- 热与功耗：壳温/结温采样、降频事件、功耗档位切换。
- 资源：RSS 高水位、KV 占用、NPU 利用率、队列等待时长。
- 业务：会话取消率、功能降级触发原因码。

**隐私最小化**
- 默认 **不上报原文/音频/图像**；只上报哈希、长度、错误码、分位数。
- 难例回传必须：**用户同意 + 目的限定 + 留存期限 + 脱敏**（见下节数据闭环）。
- 日志脱敏：token 文本、车牌、通讯录、GPS 精细坐标默认打码或删除。

**Telemetry Checklist**
- [ ] 指标字典版本化，字段增删走评审
- [ ] 端侧本地环形缓冲，网络差可丢不可堵推理线程
- [ ] hang 检测有独立 watchdog，不依赖 LLM 自己报错
- [ ] fallback 静默发生 → 必须打点（见 Runbook）

---

### 数据闭环（合规前提下的迭代）

**闭环三件套**
- **难例回传**：触发条件（低置信、用户负反馈、安全兜底命中、严重超时）。
- **再训练/再量化触发**：指标漂移、机型差分失败、校准集过期。
- **校准集版本管理**：`calib_set_id + 分布标签 + 采集时间 + 许可状态`。

**合规底线**
- 车规/消费电子均按区域隐私法与主机厂数据合同；无授权不落盘明文。
- 回传包最小字段；训练侧与车机身份解耦。

**数据闭环 SOP**
1. 定义难例规则与采样率上限（防刷爆带宽）。
2. 端侧脱敏 → 加密上传 → 平台侧权限隔离。
3. 入库标注 → 进入 eval 集 / 校准候选池（二者隔离 `【合并原文⑧ 校准/评估隔离】`）。
4. 触发再量化或 QAT 补丁 → 走完整门禁矩阵 → OTA。

---

### 模型安全（防提取 / 防篡改 / 许可）

**控制面**
- **加密存储**：权重与 engine 静态加密；解密仅在可信加载路径。
- **TEE / TrustZone**：密钥与许可证校验放 TEE；完整 LLM 进 TEE 通常不现实 → **边界清晰**：密钥/证书/策略在 TEE，大算力在 REE/NPU，防止明文权重长期暴露。
- **密钥注入**：产线 OTP/安全元件注入；禁止固件明文私钥；支持按车型/批次轮换。
- **防提取**：反调试、完整性度量、异常调试口关闭；日志禁止 dump 权重。
- **Watermark / 指纹**：输出侧或权重侧水印，用于追责泄露（强度与体验权衡）。
- **许可证**：离线许可 + 到期策略；与 OTA 防回滚联动（旧许可不能解锁新模型）。

**模型安全 Checklist**
- [ ] 发布包签名验签通过才加载
- [ ] 运行时完整性校验失败 → 拒绝推理并上报
- [ ] 调试版与量产版密钥域隔离
- [ ] 拆包/逆向威胁建模（STRIDE 轻量版）有记录

---

### 语音 + 视觉 + LLM 多模型共存调度

**调度原则**
- **会话优先级**：安全相关感知 > 驾驶辅助提示 > 语音对话 > 娱乐生成。
- **NPU 时间片**：短周期实时模型（KWS/AEC/轻视觉）保硬时限；LLM 吃剩余算力与带宽。
- **可抢占**：LLM decode 可暂停/降级；唤醒与打断路径延迟有上限。
- **内存准入**：新会话前做 admission control（KV+权重峰值预测），宁拒勿 OOM。

**共存 SOP**
1. 建模型目录：常驻集 / 按需加载集 / 互斥集。
2. 定义优先级与最大抢占延迟。
3. NPU 队列：实时队列 vs best-effort 队列分轨。
4. 冲突策略：降 max_tokens、切小模型、ViT 降分辨率、或串行化。
5. 压测：语音唤醒风暴 + 视觉帧 + LLM 长会话同时打，验收无 hang、无静默 fallback 失控。

**共存 Checklist**
- [ ] 有「谁可以杀掉谁」的书面策略
- [ ] 多模型峰值内存纳入门禁
- [ ] 抢 NPU 有指标：排队时延、抢占次数、LLM 卡顿率
---

### 排错 Runbook 扩展

**精度悬崖**
- **现象**：某版本 PPL/任务指标断崖；或仅长上下文崩。
- **排查**：校准集是否换版 → 敏感层是否被误量化 → KV 量化是否新开 → tokenizer/特殊 token 是否漂移。
- **动作**：回退量化配置；逐层余弦定位；冻结 calib_set_id。

**首包慢（TTFT 差）**
- **现象**：冷启动或首句极慢，后续尚可。
- **排查**：engine 加载/解密耗时、权重 mmap 缺页、prefill 图未融合、CPU 预处理堵、热启动缓存未命中。
- **动作**：预加载/常驻引擎；拆分冷热路径；profile prefill 算子。

**随机 hang**
- **现象**：偶发卡死，复现率低。
- **排查**：NPU 驱动超时、多线程同用一 context、异步回调死锁、热降频与超时阈值竞态、内存越界写。
- **动作**：开 hang 检测复位；串行化可疑路径；固定压力脚本抓 dump；升级驱动后重跑矩阵。

**多模型抢 NPU**
- **现象**：单模正常，同开即超时/掉帧。
- **排查**：是否无优先级队列；LLM 占满导致 ASR 超时；带宽打满误判为算力不足。
- **动作**：分轨调度；LLM 限并发；实时模保 SLA。

**Android 后台被杀**
- **现象**：切后台后再回，模型重载或会话丢。
- **排查**：LMK、厂商省电杀进程、前台服务缺失、大 RSS 被优先杀。
- **动作**：会话态外置；缩小常驻；合规前提下申请保活；被杀后快速冷恢复路径。

**CPU fallback 静默发生**
- **现象**：功能「还能跑」但时延/功耗暴涨，看板仍绿。
- **排查**：算子不支持走 CPU；动态 shape 致部分图回落；驱动失败自动降级未打点。
- **动作**：**fallback 必须可观测**；CI/真机断言「NPU 执行占比」；超阈值当失败。

**Runbook 通用步骤**
1. 复现：记下 manifest、机型、温度、并发、是否同开多模。
2. 分类：精度 / 性能 / 资源 / 调度 / 系统杀进程。
3. 取证：profiler + telemetry + logcat/kernel。
4. 单变量修复 → 回归门禁 → 写入案例库。

---

## 车规、OTA 与合规

### 车规与合规工程（LLM 场景的务实落点）

**ISO 26262 与 LLM 的定位**
- LLM 输出开放域，**完整 ASIL 功能安全认证极难**（不可预测、难完备验证）。
- 务实架构：**LLM = QM（或非安全相关项）+ 外围安全机制扛 ASIL**。
- 安全相关决策（制动/转向等）**不得**单靠 LLM；座舱助手/内容生成走 QM，安全功能走传统 ASIL 组件。

**QM + 安全机制（典型拆分）**
- **输入门禁**：意图分类 / 域外拒绝 / 敏感指令拦截（规则或小模型，可测）。
- **输出门禁**：结构化 schema、禁止直接控车指令、黑名单词与策略引擎。
- **执行通道**：真正执行车辆动作用 ASIL 软件；LLM 只出建议。
- **Watchdog**：推理超时、无响应、荒谬输出频率 → 切安全态（静音/仅本地模板/禁用控车相关 skill）。
- **诊断与降级**：故障码、降级等级、驾驶员提示，纳入安全案卷（safety case）。

**ISO 21434 网络安全**
- 威胁分析（TARA）：模型窃取、对抗输入、OTA 投毒、调试口滥用。
- 要求：安全启动链、签名、加密存储、最小权限、漏洞响应流程。
- LLM 特有：提示注入当网络安全用例评审，不单当 NLP 问题。

**UNECE R155 / R156**
- R155：网络安全管理体系（CSMS）与车辆型式相关证据。
- R156：软件更新（SUMS）——更新可追溯、可授权、可回滚、影响评估。
- 模型 OTA 视同软件更新：版本、依赖、回滚演练、用户/车间告知策略按主机厂落地。

**ASPICE**
- 部署工程纳入供应商过程：需求、变更、配置、验证证据可审计。
- 模型/校准集/引擎二进制进配置管理；门禁报告当验证工作产品。

**热管理与 derating**
- 定义温度档：正常 / 降算力 / 禁 LLM / 仅保安全感知。
- derating 表与 NPU/CPU 频率、max 并发、max_tokens 绑定。
- 验收：高低温舱按档位测体验与安全态切换，禁止「高温直接死机」。

**车规落地 Checklist**
- [ ] LLM 在安全架构图中标为 QM，安全边界书面化
- [ ] 控车路径无 LLM 单点
- [ ] watchdog + 降级等级表已测
- [ ] TARA 含模型与 OTA 条目
- [ ] R156 更新包含模型影响评估与回滚演练记录
- [ ] derating 表进量产标定

---

### OTA：模型与软件的量产发布

**机制要点**
- **A/B 分区**：新包写入非当前槽；验证成功再切换；失败保留旧槽启动。
- **模型与引擎解耦发布**：权重/tokenizer 与 runtime/engine 可独立版本；兼容矩阵声明 `model_abi ↔ engine_ver`。
- **灰度**：按车队比例 / 区域 / 车型；观察 telemetry 再放量。
- **回滚**：一键回旧槽；业务配置同步回滚；防「新配置+旧模型」组合。
- **差分包**：系统与引擎可走 bsdiff 等；**权重差分往往难且不稳**（量化重排、加密、对齐）→ 默认按整包或分层大块更新，差分仅作可选优化并强测失败回退。
- **签名验签**：链路签名；安装前验签；运行时再度量。
- **防回滚**：单调版本计数器 / 安全版本号；禁止用旧包覆盖新安全修复（与漏洞修复策略一致）。

**OTA SOP**
1. CI 出 **签名** release 产物 + manifest。
2. 台架全门禁 → 小流量灰度（监控 hang/OOM/精度投诉代理指标）。
3. 放量；异常自动停灰度并建议回滚。
4. 回滚演练每版本至少成功 1 次（台架+实车抽样）。
5. 归档：版本、通过率、回滚记录，满足 R156/SUMS 证据。

**OTA Checklist**
- [ ] A/B 切换与断电场景测试
- [ ] 引擎/模型版本不匹配时拒绝加载并提示升级
- [ ] 灰度指标看板含 NPU hang、fallback 率、热降频
- [ ] 差分失败路径 = 完整包重试或回滚，不留半更新
- [ ] 防回滚计数在安全侧实现

## 量产发布总 Checklist

**发布前**
- [ ] manifest 指纹齐全（模型/引擎/校准集/词表/配置）
- [ ] 精度/性能/功耗/热/内存余量/冷启动/包体/机型矩阵全绿
- [ ] CI 容差策略按平台生效；无静默 CPU fallback
- [ ] 安全：签名、加密、许可、TEE 边界评审通过
- [ ] 车规：QM 边界、watchdog、降级、derating 已测
- [ ] OTA：A/B、灰度、回滚、防回滚演练完成
- [ ] telemetry 字段与隐私最小化评审通过
- [ ] 多模型共存压测通过

**发布后**
- [ ] 灰度观察期指标无回归
- [ ] 难例回传合规开关按策略运行
- [ ] 问题按 Runbook 入库，校准集/模型版本可追溯

---

## 最小 SOP 速查（值班可用）

1. **改模型/量化**：换 calib 版本 → 精度 CI → 性能/内存 → 门禁 → 签名 → 灰度 OTA。  
2. **改引擎**：兼容矩阵 → bit 容差回归 → 真机热功耗 → OTA 解耦发布。  
3. **线上 hang/OOM**：看 telemetry → 判调度/热/fallback → 按 Runbook → 必要时回滚。  
4. **精度投诉**：锁定 manifest → golden 复现 → 校准/KV/tokenizer 三分法 → 修后全门禁。  
5. **多模卡顿**：查 NPU 队列与优先级 → 降 LLM 或串行 → 补压测再放量。

---

---

## ⑯ AI 编译器与算子工程

> 第 16 站（终站）：IR、融合白名单与数值协议——转不过、融掉了按站查。

> 转不过、融掉了、数值对不上——按站查：图如何被 **导出 → 合法化 → 匹配 → 分块 → 选核 → 校验**。  
> 模型语义见 ⑨；量化见 ③；性能意图见 ④；平台命令见 ⑤ / ⑧ / ⑩ / ⑭。  
> 本章只留 IR / 融合白名单 / 数值协议，不附厂商教程。ONNX→厂商图 与 `torch.export`→ExecuTorch 是两条出口，勿画成一条。

---

### 1. torch.export / FX / Dynamo 与 ExecuTorch lowering

**职责划分**
| 层 | 组件 | 产出 | 端侧含义 |
|---|---|---|---|
| 追踪 | Dynamo | 字节码级 guard + FX GraphModule | 抓住「这次前向」的静态图；guard 失败则再编译或回退 eager |
| 图 IR | FX (`call_function` / `call_module` / `get_attr`) | 可改写的 Python 图 | 融合、常量折叠、dtype 改写发生在这里 |
| 合法化导出 | `torch.export` → ExportedProgram | 带 FakeTensor 形状、常量、state_dict 的稳定图 | 比 `jit.trace` 更严：动态控制流必须 `cond`/`map` 或被特化 |
| 量化/改写 | `torchao` / PT2E / QDQ 插入 | 带 quantize/dequantize 的 FX | 必须与运行时 kernel 的 scale 语义一致（见 ③） |
| 端侧 lowering | ExecuTorch | Edge dialect → ExecuTorch IR → 后端 delegate | XNNPACK / QNN / CoreML / Vulkan / CPU 各吃一部分图 |
| 遗留路径 | `torch.onnx.export`（现多走 dynamo=True） | ONNX GraphProto | 仍是 QNN/ORT/部分 NPU 的进料口，**不是** ExecuTorch 主路径 |

**推荐链路（LLM 端侧）**

```
Eager 模型
  → Dynamo 捕获（SDPA/MoE/变长 mask 常打断图）
  → torch.export（固定 example_inputs + dynamic_shapes 声明）
  → 可选 PT2E QDQ
  → to_edge_transform_and_lower / to_backend
  → .pte + 权重 blob
```

对照：`PyTorch → ONNX → qnn-onnx-converter` 仍是高通主路径（见 ⑤⑭），与 ExecuTorch delegate 是 **两条出口**，不要画成一条。

**工程检查点**
- `export` 失败：Python 数据依赖控制流、未注册 custom op、`data-dependent` shape（如 `nonzero`）。
- 成功但变慢：图被切成多段 GraphBreak，中间回 Python。
- ExecuTorch：看 `delegation` 表——未委托节点落到 portable CPU，decode 会被拖死。
---

### 2. ONNX vs StableHLO vs TOSA vs Relay

同一计算，四种「合同」；编译器只认合同，不认 PyTorch 模块名。

| IR | 抽象层级 | 形状/控制流 | 典型下游 | 端侧现实 |
|---|---|---|---|---|
| **ONNX** | 框架无关算子表（opset 版本化） | `dynamic_axes`；控制流靠 If/Loop | ORT、QNN、OMG、TRT（部分） | **交换格式之王**；RMSNorm/RoPE/SDPA 常缺或版本漂移 |
| **StableHLO** | 稳定的 HLO 子集 | 动态维、量化类型更一等公民 | XLA、OpenXLA | 云侧/XLA 强；端侧看厂商是否吃 HLO |
| **TOSA** | 张量算子、偏移动/MCU | 静态可证明、量化类型内建 | MLIR TOSA → 移动 NPU / Arm | 适合静态量化 CV；LLM 动态 seq 很痛 |
| **Relay**（TVM） | 函数式图 IR | 符号 shape、控制流更完整 | TVM → TE/TIR | 定制后端强；与 ⑩ TBE/hit_bank 同类「搜索+知识库」 |

**选型口诀**
- 进 **厂商 SDK**：优先对方官方入口（ONNX 或 `torch.export`），不要为 IR 干净强转三跳。
- 自研 codegen：StableHLO 或 MLIR 比 ONNX 更适合做 pass。
- **TOSA**：静态 INT8 卷积网，而不是 GQA+RoPE 自回归。
- **转换损失发生在合同边界**：ONNX 把 SDPA 拆成 MatMul+Softmax 后，NPU 融合模式就匹配不上。

---

### 3. 图模式匹配失败的典型 LLM 算子

编译器「有 fused kernel」≠「你的图能连上」。失败几乎总是 **图形状与白皮书模板不一致**。

**SDPA**
- 手写 `matmul + /sqrt + mask + softmax + matmul` → 匹配不到 FMHA，多次 DRAM 往返。
- mask 加性 `-inf` vs bool vs sliding window，模板只认一种。
- causal 用 `triu` 现算 vs `is_causal` 属性 → 多一个 Compare+Where 打断融合。
- dropout 训练残留 → 推理模板直接拒。
- **处理**：导出前 `F.scaled_dot_product_attention(..., is_causal=True)`；推理去掉 dropout。

**RMSNorm**
- 手写 `pow2 → mean → add eps → rsqrt → mul γ`，`axes`/`keepdims`/`eps` 位置不同则失败。
- **修复**：换成后端登记的 `RMSNorm` custom op，或与官方 pattern 逐节点一致。

**RoPE**
- `cos/sin` 每次 `arange` 现算 → 动态 shape + trig，NPU 几乎不融。
- interleaved vs half-split 两种布局，模板只认一种；YaRN/NTK 额外 Mul 使 pattern 失效。
- **修复**：cos/sin cache 作常量或 lookup；RoPE 做成单 custom op。

**SwiGLU**
- `SiLU(xW_g)*(xW_u)` 中间夹 to(dtype)/contiguous/bias → 无法 `fused_w1w3`。
- **不该融**：gate/up 已是 INT4 分组量化且 group 轴与拼接轴冲突时，强融会 pack 错。

**GQA 的 `repeat_kv`**
- `repeat_interleave` / `expand+reshape` / `broadcast` 三种写法；很多 NPU 要 **不 repeat、在 attention kernel 内广播 K/V 头**。
- 图上真 `repeat` 成 MHA 体积，④ 的 KV 带宽优化被编译器做废。
- **修复**：保持 `[B, kv_heads, T, D]`，把 repeat 交给 SDPA；禁止 export 前展开。

**通用手法**：失败子图做 graph dump 对照官方 golden 图；差一个 `Squeeze` 就整簇 miss。

---

### 4. Kernel 库：各吃哪一段

| 库 | 硬件 | 擅长 | 端侧角色 |
|---|---|---|---|
| **cuBLAS** | NVIDIA GPU | 大 GEMM | decode 的 `m=1` GEMV 往往不是最优 |
| **CUTLASS** | NVIDIA GPU | 可组合 GEMM、量化 epilogue | 自研 plugin / 量化 GEMM |
| **oneDNN** | x86/部分 Arm | 卷积/GEMM/LN | PC/工控 CPU；不是车规 NPU 主路径 |
| **QNNPACK** | 移动 CPU（量化） | NHWC INT8 卷积 | 老移动路径；LLM decode 收益有限 |
| **XNNPACK** | Arm/x86 CPU | FP16/INT8、ExecuTorch 默认 CPU delegate | 端侧 CPU fallback 与小模型 |
| **KleidiAI** | Arm CPU | SME/SME2、INT4/INT8 GEMM | 新 Arm 上 INT4 LLM 的关键 |
| **CMSIS-NN** | Cortex-M | 极小 INT8 卷积/FC | MCU 语音/检测；塞不进 7B |

**优先级**：NPU 厂商库 > KleidiAI/XNNPACK > 手写 NEON > 通用 BLAS。

---

### 5. 端侧 NPU compiler 共性

1. **静态 shape 偏好**：SRAM/指令按 shape 特化。动态维 → 多份编译、慢速通用核、或拒。
2. **权重预排布**：pad、interleave、INT4 nibble pack；运行时只 DMA。改量化粒度往往要整网重编。
3. **SRAM tiling**：激活切进 on-chip；tile 失败 = 反复进出 DRAM。
4. **DMA 双缓冲**：计算当前 tile 时 DMA 下一块；与 ② 的 runtime 内存池必须对齐。
5. **封闭算子集**：无设备端动态分配；softmax/reduction 轴须静态；精度以定点管道为准。

---

### 6. 动态 shape / 动态 seq_len 的工程折中

| 策略 | 做法 | 代价 | 适用 |
|---|---|---|---|
| **Padding** | 编译 `max_seq`，短序列 pad | 按最坏情况吃 SRAM | 延迟 SLA 松、NPU 完全静态 |
| **Bucket** | 编译 128/512/1024/2048 等档 | 产物变多、冷启动涨 | 车机预载 3–4 档常见 |
| **拆图** | prefill 静态大图；decode `q_len=1` + `kv_len=bucket` | 两套融合/两套校准 | **端侧 LLM 默认架构** |
| Chunked prefill | 长 prompt 切块 | TTFT 变差换峰值内存 | 与 ④ chunked 同一思想 |

**禁止幻想**：封闭 NPU 上完全动态 `seq` 且不损吞吐。`torch.export` 的 `Dim("seq")` 能导出，不代表 NPU lowering 能吃。

---

### 7. 算子融合白名单：能融 vs 不该融

**能融**
- 逐点：`Add+Mul+SiLU/GELU`、QDQ 与随后 GEMM 的 scale epilogue。
- 归一化+残差：`RMSNorm + residual add`。
- Attention 簇：QKVO + scale + mask + softmax（SDPA/FMHA）。
- SwiGLU 的 gate/up 同输入、同量化方案时的成对 GEMM。

**不该融**
- 跨精度边界：INT4 GEMM 与必须 FP16 的 RMSNorm 强融。
- 跨动态/静态边界：RoPE 动态 `position_ids` 拉进静态 SDPA tile。
- `repeat_kv` 融进 Q 投影：GQA 胀成 MHA。
- 采样/KV append/tokenizer 不应进 NPU 图。
- 精度未过就开最高 fusion → layerwise dump 无法归因。
- 超大 fused kernel 超出 I-cache/SRAM → 应主动解融。

规则：**融的目标是减中间张量与 launch，不是把 Python 模块变成一个巨核。**

---

### 8. 调试：graph dump、layerwise dump、数值一致性、NaN

**Graph dump（编译对不对）**
导出后、lowering 后、后端分区后 **三份图**。查：SDPA 是否仍在、哪些 node fallback、QDQ 是否成对、`repeat_kv` 是否被展开。

**Layerwise dump（数对不对）**
固定同一 prompt、同一 weights、同一 dtype 参考（通常 FP16 CPU）。对齐点：embedding 出、每层 attn 出、MLP 出、lm_head logits。指标：余弦、max abs、top-k 一致率。必须记录：是否 fused、是否 INT8 softmax、KV 是否量化。

**数值一致性协议（一次只开一个开关）**
1. FP16/BF16 未量化图 vs PyTorch  
2. 开融合后 vs 步骤 1  
3. 量化后 vs 步骤 2  
4. 真机 NPU vs 仿真  

**NaN / Inf 高频源**
| 源 | 机制 | 对策 |
|---|---|---|
| Softmax 上 `-inf` mask 在低精度下整行全 `-inf` | `exp` 全 0 | 保留至少一行有效；mask 用 dtype 最大负值 |
| RMSNorm `eps` 过小 + INT8 噪声 | rsqrt 爆炸 | 加大 eps；Norm 留 FP16 |
| RoPE 半精度 × 量化 Q | 溢出 | 位置编码 FP16、权重 INT4 |
| INT4 scale 未融对 | 错 scale → 巨大 logits | 核对 per-group 轴 |
| KV 量化饱和 | 新 token clip | 校准含长上下文 |
| 脏 SRAM / 错误内存复用 | overlapping liveness | 关内存复用对比 |

**定位顺序**：logits 是否 NaN → 从后往前第一层 NaN → 该层融合核还是分解核 → 关融合/升精度/查 mask。不要先重训。
