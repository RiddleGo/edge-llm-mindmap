## 一、岗位画像：这个岗位到底在做什么？

端侧模型部署与推理优化工程师的核心使命是：**让大模型在手机、车机、机器人、嵌入式设备等资源受限的端侧平台上，跑得快、跑得稳、跑得省。** 综合各公司JD，工作内容可分为六个层次：

**① 推理框架开发与引擎定制。** 参与端侧大模型推理框架的核心模块开发，包括模型解析、计算图优化、算子适配、内存管理、推理调度等。字节跳动进一步要求"通过CPU/GPU/DSP/NPU的并行计算优化、架构设计、稀疏优化、异构调度等多种高性能优化技术打造业界领先的高性能异构AI推理引擎"。

**② 模型压缩与轻量化。** 主导模型量化（PTQ/QAT）、剪枝、蒸馏、NAS等轻量化技术的落地。中国电信将其细化为"模型轻量化改造，适配国产芯片"。德赛西威要求"理解AI算法网络的计算原理，了解相关模型的计算图优化方法，包括但不限于量化、剪枝、稀疏、蒸馏等"。

**③ 硬件平台适配与部署。** 将AI模型部署至手机、嵌入式装置及定制化NPU平台，执行模型转换、量化、精度验证与效能优化。具体平台覆盖NVIDIA Jetson/Orin、高通8255/8295、地平线征程系列、华为昇腾、自研芯片等。

**④ 性能瓶颈分析与极致调优。** 运用性能分析工具深入剖析推理全过程，精准定位计算、内存、通信瓶颈。腾讯要求"通过量化、剪枝、蒸馏、算子融合、编译优化、显存与调度优化等手段，将硬件算力发挥到极致，持续降延迟、提吞吐、控功耗"。上海AI实验室要求"针对机器人的高频控制闭环需求（如数百Hz的动作输出频率），开展端侧推理性能极致调优"。

**⑤ 评测体系与工具链建设。** 搭建模型性能评测体系，建设自动化模型转换与部署工具链。小鹏要求"参与模型部署工具链建设，完善模型转换、性能基准、端侧验证和发布流程"。

**⑥ 端云协同与工程化封装。** 华为明确要求"熟悉端-边缘-云协同架构，能够决策local设备执行任务（低延迟动作），Edge处理任务（视觉语义理解）"。上海AI实验室要求"负责推理服务的工程化封装，并与机器人底层控制系统或中间件（如ROS/ROS2, DDS）深度集成"。

---

## 二、能力分层图谱：从初级到专家的技术进阶

不同级别岗位的技术要求差异显著，以下按纯技术能力分层：

### 初级

| 维度 | 典型能力要求 |
|---|---|
| **编程** | 熟练掌握C/C++或Python，具备良好的数据结构与算法基础 |
| **理论** | 理解Transformer/LLM基础原理及推理流程，熟悉Attention、KV Cache、Prefill/Decode |
| **框架** | 熟悉至少一种推理框架（TensorRT、MNN、ONNX Runtime），有完整的部署项目实践 |
| **特征** | 能独立完成模型转换、真机部署与基本功能调试；止步于调用转换脚本与工具链API |

### 中级

| 维度 | 典型能力要求 |
|---|---|
| **核心能力** | 独立完成模型适配、功能调试和底层性能问题定位；熟练使用PyTorch，理解计算图、张量处理机制及算子实现 |
| **量化** | 精通PTQ/QAT，熟悉QNN SDK工具链（模型转换/量化/性能分析），能系统性排查量化精度劣化 |
| **平台** | 有高通8155/8295、地平线BPU、NVIDIA Jetson等平台实战经验，理解目标硬件的架构约束 |
| **特征** | 理解算子融合与布局优化原理，能定位"哪个Pass吃了我的精度/性能"；能把Roofline分析落地为优化方向选择 |

### 高级/专家

| 维度 | 典型能力要求 |
|---|---|
| **系统能力** | 精通"端-边缘-云"协同架构；具备编译器/运行时/算子库全栈优化能力 |
| **架构能力** | 定义边缘智能技术战略与路线，定义推理SLA指标体系；能主导从0到1的产品落地 |
| **技术深度** | 能编写推理引擎优化Pass并论证等价性；能编写高性能CUDA Kernel/Triton算子、TensorRT Plugin、NPU自定义算子，解决"最后一公里"问题 |
| **影响力** | 有开源项目贡献（vLLM/SGLang等）、顶会论文（MLSys/ASPLOS/OSDI/NeurIPS）、专利布局 |

**分水岭判据**：初级只会调框架API和trtexec参数；中级理解融合与布局优化原理；高级能写Pass、写算子、论证等价性。是否内化"**推理引擎即编译器**"的心智模型，直接决定问题定位的深度。

---

## 三、技能图谱总览：五层递进结构

端侧推理优化知识有明确的依赖链，建议按以下五层递进掌握：

```
第一层  大模型推理原理        （知道"算什么"）
第二层  模型压缩与量化        （知道"怎么变小"）
第三层  推理框架与AI编译栈     （知道"怎么变快"——软件侧）
第四层  硬件体系结构与性能方法论（知道"怎么变快"——硬件侧）
第五层  系统工程与前沿方向     （知道"怎么落地、怎么演进"）
```

以下第三至第七节逐一展开。

---

## 四、大模型推理核心优化技术

这是2025年后新增的核心要求，JD中反复出现的关键词包括Transformer架构、KV Cache、Prefill/Decode分离、投机推理等。但JD只列关键词，实际工作与面试考察的是深层机制。

### 4.1 Transformer架构与KV Cache基础

- **Transformer架构**：理解Attention机制（QKV计算、缩放因子）、FFN结构、位置编码
- **KV Cache**：理解自回归解码时K/V的存储与复用机制，以及MQA/GQA等变体对Cache规模的压缩
- **Prefill/Decode分离**：地平线要求"构建UCP加速库并与vLLM等主流框架深度适配，优化KVCache复用与Prefill-Decode分离技术"

### 4.2 KV Cache管理进阶

- **PagedAttention**：借鉴OS虚拟内存分页机制，将KV Cache按固定大小block（如16 slot）分页管理，逻辑连续、物理离散，近乎消除内部/外部碎片，显存利用率从~30%提升至90%+；block table实现逻辑→物理映射，支持copy-on-write，是Prefix Caching的基础设施
- **KV Cache量化**：INT8 KV近乎无损，FP8（E4M3）在Hopper/新NPU上是甜点位；per-channel/per-head scale优于per-tensor；K比V对量化更敏感（K含RoPE旋转后的位置信息，幅度波动大）
- **MLA（Multi-head Latent Attention，DeepSeek）**：将K/V投影到低秩latent（如512维），KV Cache只存latent向量+少量解耦RoPE的key，压缩比达~93%；推理时将上投影矩阵"吸收"进Wq/Wo，避免解压回显存。深层问题：RoPE对key做位置相关旋转，破坏了latent的低秩可吸收性，故需单独缓存"解耦RoPE key"
- **端侧KV预算计算（面试必考）**：
  `KV字节 = 2 × layers × kv_heads × head_dim × dtype_size × seq_len × batch`
  例：7B GQA模型（32层、8 KV头、128维、FP16）1条64K上下文≈1GB——在8GB端侧设备上，KV预算必须与权重、激活、系统驻留统筹排布
- **CPU offload/swap**：以换页方式把冷block移到内存，代价是总线带宽成为新瓶颈
- **前沿趋势**：KV分层存储（NPU→CPU→SSD，vLLM/SGLang prefix cache + LMCache路线）、FP4/2bit KV（KIVI）、原生稀疏注意力（NSA）直接砍掉Cache增长、端侧"预算驱动"的上下文截断策略

### 4.3 调度与批处理

- **Continuous Batching**：在iteration级调度，每步完成后新请求立即插入、完成请求立即退出，比static batching吞吐提升数倍；调度器核心是slot管理与抢占策略
- **Chunked Prefill**：将长prompt切成chunk与decode混排，消除"prefill独占导致decode停顿"的TPOT毛刺；chunk大小是TTFT与TPOT的权衡旋钮
- **CUDA Graph静态化**：decode阶段每步只算1 token，kernel launch开销（几十个kernel × μs级launch延迟）占比可达30%+；将整步decode capture成静态图一次launch，前提是形状静态——这是PagedAttention block数必须padding到固定值的原因。局限：动态shape需按shape bucket（batch=1/2/4/8）缓存多张graph或padding；动态控制流需编译期确定
- **P/D分离的端侧映射**：单SoC上NPU负责decode、GPU/CPU协处理prefill，本质是算力/带宽特性错配的利用

### 4.4 注意力与算子优化

- **FlashAttention核心思想**：tiling分块计算 + online softmax（running max/sum，无需先遍历整行）+ 中间结果全程驻留SRAM，把HBM访问从O(N²)降到O(N²·d/M)，显存从O(N²)降至O(N)——实现精确注意力的IO感知加速，也证明"**融合可以改变算法复杂度**"，是fused kernel设计的教科书案例。FA2优化并行度（seq维切分），FA3用Hopper TMA+WGMMA+异步softmax
- **Prefill/Decode二象性（必须能推导）**：prefill批量处理prompt，GEMM大而饱满，compute-bound；decode逐token生成，每步只算1 token却要读全部权重，算术强度≈1 FLOP/byte，严重memory-bound——**decode优化=降字节（量化、投机解码）而非降FLOPs**
- **经典估算题**：7B模型FP16、batch=1、600GB/s带宽 → 单步decode ≈ 14GB ÷ 600GB/s ≈ 23ms ≈ 43 tok/s
- **FlashDecoding**：FA在decode（QLen=1）场景收益骤减（无O(N²)中间矩阵可省，瓶颈转为KV加载），需沿KV维切分并行
- **RoPE优化**：预计算cos/sin表、与QKV投影算子融合（fused RoPE+GEMM）、NTK/YaRN外推时的频率重排

### 4.5 投机解码（Speculative Decoding）

- **范式**：轻量draft模型/头连发k个候选token，target一次forward并行验证，接受前缀即一次产出多token；验证按target分布校正拒绝后的token，保证分布完全一致（无损）
- **路线对比**：
  - **Medusa**：target模型加多个解码头+tree attention，零额外draft模型但头需微调
  - **EAGLE/EAGLE-2**：在feature层自回归draft，draft树每步基于target反馈重新构建，接受率显著高于token级draft；EAGLE-2引入上下文感知的动态draft树
  - **MTP（Multi-Token Prediction）**：DeepSeek V3原生，训练时即做多token预测，与主干共享表征，draft质量高，正在成为新模型标配（训练-推理协同设计）
- **加速比** ≈ `E[接受长度] / (1 + draft开销 + 验证开销比)`；接受率α=0.8、k=4时E[len]≈3.4，但draft串行自回归本身也耗时，实际加速2~3×为主流
- **关键结论：只在decode（memory-bound）下划算**——target验证一次forward读一遍权重可验证k个token，字节摊薄；prefill投机毫无意义。batch增大时收益下降（大batch本身已摊薄权重读取，且验证造成负载不均）
- **深层考点**：为什么验证k个token不违反因果性（tree attention mask）；拒绝后如何保证分布等价（从max(0, p_target−q_draft)重采样）
- **端侧趋势**：draft常驻cache+target间歇唤醒的功耗友好模式、free speculative（n-gram/self-draft免额外模型）、接受率在线自适应

### 4.6 MoE模型推理（2026重点）

- **核心矛盾**：激活稀疏（每token仅k/总专家数，如8/256）但**权重必须全部可寻址**——router的选择对每个token不可预知，显存占用是dense的数倍。服务端靠Expert Parallelism切多卡，端侧只能走Offload：专家权重驻留CPU内存/SSD，按router预测分层预取，命中率依赖热点专家倾斜的局部性
- **Shared Expert**：每token必经，承载通用知识，减少路由抖动
- **端侧挑战全览**：内存墙（权重驻留）、router与专家GEMM的算子切换开销、batch内token路由分散导致专家GEMM变"瘦矩阵"（M极小、compute-bound劣化）、量化需按专家粒度做（expert-wise GPTQ/AWQ）
- **batch=1时MoE的真实优势**：带宽读取只涉及激活专家，若权重驻留内存则带宽收益真实存在
- **趋势**：端侧原生小MoE（激活数百M~几B的edge MoE成为2026端侧标配路线）、专家量化+按热度LRU缓存（hot expert留NPU、cold expert留内存）、MTP×MoE×MLA三件套协同（DeepSeek范式被广泛复制）

---

## 五、模型压缩与量化

### 5.1 基础技术栈

| 技术 | 具体要求 |
|---|---|
| **PTQ（训练后量化）** | 精通INT8/INT4量化流程，熟悉精度对齐方法 |
| **QAT（量化感知训练）** | 理解伪量化节点插入、梯度缩放、STE（直通估计器）机制 |
| **剪枝** | 结构化剪枝与非结构化剪枝，稀疏化加速 |
| **知识蒸馏** | 大模型到小模型的能力迁移 |
| **混合精度** | FP16/INT8混合部署策略 |

### 5.2 权重量化算法谱系（面试深水区）

| 算法 | 核心思想 | 适用场景 |
|---|---|---|
| **RTN** | 直接四舍五入映射，无误差补偿 | INT8够用；≤4bit误差骤增 |
| **GPTQ** | 基于Hessian的贪婪误差补偿——量化一个权重后，把误差"分摊"到同列未量化权重上（OBS/OBQ思想的批量加速版） | 7B模型压到3bit仍可用 |
| **AWQ** | 观察到约0.1%~1%的"显著权重通道"（激活outlier对应）需保护，通过per-channel缩放s在激活侧除以s、权重侧乘以s | 不依赖反向传播，量化速度快、泛化性好 |
| **SmoothQuant** | 将量化难度从激活迁移到权重（数学上等价的缩放变换），实现W8A8全量化推理 | LLM INT8部署主力方案 |
| **HQQ** | 免校准数据，半精度辅助变量直接求解最优投影 | 快速迭代，比GPTQ快数十倍 |

**工程要点**：GPTQ的`group_size=128`+`act_order`对低bit精度影响极大；AWQ对校准分布敏感，domain不匹配时需自采校准集；3bit以下GPTQ/AWQ都会明显掉精度，需配合QAT或mixed-precision；GPTQ量化后务必逐层回测下游任务，不能只看perplexity。

### 5.3 低比特格式前沿

- **FP8**：E4M3（精度优先，前向激活/权重）与E5M2（动态范围优先，梯度/FP8 KV）；E4M3在LayerNorm输出后易溢出，需插入动态per-tensor scale节点
- **MXFP4**：基于Microscaling，每32个元素共享一个8bit block scale，硬件友好
- **NVFP4**：E2M1数据+FP8 block scale+E4M3 per-tensor scale，是Blackwell上FP4的完整格式；原生FP4张量核心实测吞吐约FP8的2倍
- **W4A16 vs W4A8**：W4A16（权重INT4、激活FP16）在batch=1自回归解码中收益最大（decode是memory-bound）；W4A8适合算力受限的端侧NPU，但激活低比特需SmoothQuant类处理outlier。**反直觉考点：W4A16在compute-bound的prefill阶段可能反而不如W8A8快**
- **Group size经验值**：32/64保精度，128是速度-精度平衡点；端侧NPU对INT4/FP4支持碎片化，ONNX算子转换前必须查target的量化规范

### 5.4 量化精度排查方法论（高级岗核心能力）

- **激活outlier的本质**：集中在固定通道（与attention sink、RMSNorm放缩、残差累加相关），呈系统性分布而非随机——这是per-channel/per-group比per-tensor有效的原因
- **标准排查流程**：逐层量化+逐层恢复FP16对比（二分定位敏感层）→ 敏感层keep FP16（mixed-precision，通常<5%参数保高精度即可追平全FP16）→ 回测
- **敏感层清单**：LayerNorm/RMSNorm紧邻的线性层输入、首尾几层transformer block、attention out projection（KV Cache入口）
- **校准集选择**：128条是下限（快速验证），512条使scale统计稳定；**分布比数量更重要**，序列长度需覆盖目标场景；校准集含过长样本会使Hessian估计偏差
- **量化粒度选择**：per-tensor（快、硬件支持最广）→ per-channel（权重量化默认）→ per-group（<8bit标配），需与硬件scale计算开销对齐

### 5.5 KV Cache量化与长上下文

- 估算实例：Llama-7B（32层、GQA 8头、head_dim 128）、32K上下文、batch 4、FP16：2×32×8×128×4×32768×2B ≈ 16GB——INT8/FP8量化直接减半
- GQA/MQA模型因KV头数少、cache小，量化绝对收益下降；**小模型（<3B）与长上下文（>32K）组合对KV量化最敏感**（误差占有效信息比例更高）
- INT8 KV量化在检索类长上下文任务上常见"大海捞针"失败，**务必跑needle-in-haystack测试**；cache量化与权重量化误差会叠加，联合部署时各自回测
- 工程上可做cache分块量化（按token块动态scale）

### 5.6 稀疏化与结构优化

- **2:4结构化稀疏**：理论2倍、实测仅1.2~1.6×（metadata开销+memory-bound）；需配套2:4-aware QAT补偿精度；收益集中在GEMM-bound大batch场景，**batch=1端侧推理基本无收益**
- **非结构化稀疏**（90%+稀疏度）在端侧NPU上几乎无收益——NPU按稠密tile调度，稀疏索引开销抵消计算节省
- **channel-level剪枝+蒸馏**：端侧主流组合，剪掉整个channel可真实降宽、无硬件支持前提；建议按整层FFN或attention头粒度剪；剪枝后需rebuild模型图（宽度变化导致算子不兼容）
- **剪枝与量化的先后顺序（高频考点）**：先剪枝+蒸馏恢复精度、再PTQ/QAT量化——剪枝改变误差分布，后量化可感知最终结构；反过来剪枝会破坏量化scale

### 5.7 端侧专用轻量化与量化友好设计

- **SLM设计趋势**：参数共享（MobileLLM的immediate block-wise weight sharing复用相邻block权重）；**深度优先于宽度**——同参数量下更深更窄的模型在端侧memory-bound解码中更优；GQA/MLA压缩KV；小词表（32K）降低embedding占比
- **量化友好架构设计**：激活outlier抑制（Gemma类模型在残差前约束幅值）、避免无界激活函数（Softsign/GELU-tanh替代）、权重分布正则化训练，使模型天生适配W4A8甚至FP4
- **选型原则**：关注"量化后性能"而非FP16指标——许多模型FP16领先但INT4崩掉
- **端侧落地三件套**：QAT微调 + 硬件原生格式导出（QDQ ONNX/ExecuTorch/TFLite）+ 真机profiling（算子覆盖率与dequant回退是主要瓶颈）

---

