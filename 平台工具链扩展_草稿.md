## 🛠️ 平台工具链扩展

> **与原文关系**：补全并深化「⑤ 平台工具链」「⑩ 昇腾/麒麟」「⑪ QNN/Edge-LLM」「⑫ 车载芯片全景」中未展开的**命名辨析、产物差异、跨厂商选型与版本钉死**。不重复 Roofline/KV/INT4 数学与投机采样原理；命令与产物名以工程可核验为准，不确定处标「需核验」。
>
> **怎么用本章**：⑤仍是「二选一主攻」学习路径；本章是项目立项时的**选型与钉版本手册**。并入建议：插在⑤与⑥之间，或作为⑫前的独立章；原文 OMG/QNN/Edge-LLM 实战段落保留，交叉处互链即可。

量产选型公式：**芯片代际 × 模型类型(CV/ASR/LLM/VLM) × 约束(功耗/内存/时延/ASIL)** → 引擎与产物 → 版本列车与指纹门禁。

---

### 🔷 高通：SNPE / QNN / QAIRT / Genie 与产物形态

#### 命名栈（易混点一次拆清）
高通端侧 AI 不是「一个 SDK」，而是**多层产品名叠在同一运行时上**；对外文档常换皮，对内最终吃同一套后端 so。
- **SNPE**（Snapdragon Neural Processing Engine）：高层易用 API；主产物 **`.dlc`**；一次 API 可选 CPU/GPU/DSP(HTP)，换后端少改代码。
- **QNN / AI Engine Direct**：细粒度控制面；主产物 **`lib*.so`（图库）** + **context binary**；后端显式加载 `libQnnCpu.so` / `libQnnGpu.so` / `libQnnHtp.so`。
- **QAIRT**（Qualcomm AI Runtime）：底层统一 Runtime；SNPE/QNN/Genie 最终都落到它；系统集成优先 **QAIRT C API**（ABI 稳定），应用层可用 C++ RAII 包装。
- **Genie / GenAI Inference Extensions**：在 QNN 之上做 **LLM/VLM 自回归编排**（Tokenizer、KV、采样、多 binary）；CLI 常见 **`genie-t2t-run`**。
- **选型口诀**：CV/检测快速出包 → SNPE+DLC；要抠算子落点与 HTP 缓存 → QNN+context；要跑生成式 → Genie；要写长期中间件 → QAIRT C。
- **关系标注**：原文⑤写「QNN + context binary」正确但未拆 SNPE/Genie → **补全**；⑪「converter≈OMG」是类比 → **深化为产物对照**，勿当成文件格式互通。

#### Hexagon HTP vs HMX vs HVX（硬件层）
规格书 TOPS 与部署后端库不是同一张表；必须落到**后端 so + 量化位宽 + profiler 落点**。
- **Hexagon DSP**：矢量/标量底座；历史文档称 DSP runtime；今天多数新图走 HTP 封装。
- **HTP**（Hexagon Tensor Processor）：当前 **NPU 推理主后端**；Genie/QNN 配置里常见 `QnnHtp`。
- **HMX**：Hexagon 内矩阵加速，偏 INT4/INT8 GEMM；**不是第三个独立 SDK**，由 HTP 路径调度。
- **HVX**：矢量扩展，偏逐元素/激活/布局变换；与 HMX 分工「矩阵 vs 矢量」。
- **工程含义**：宣传 TOPS 常含 HMX 峰值；若图大量落 CPU/GPU，实测只有纸面几分之一。
- **验证**：`qnn-profiler` / 厂商 ADX 看逐算子 backend；目标是敏感大算子（Conv/GEMM/Attention 块）上 HTP。
- **坑**：同一 SoC「系统设置开了 AI」≠ 你的 `.so` 链到了正确 `libQnnHtp.so`；App 打进了 stub 或旧版 so 会 silent 回落。

#### DLC vs model.so vs Context Binary（产物三段论）
三者对应**可移植图 → 可 prepare 图库 → 已固化执行上下文**，不是简单新旧替代。

| 产物 | 典型工具 | 本质 | 何时用 | 可移植性 |
|------|----------|------|--------|----------|
| **`.dlc`** | `snpe-onnx-to-dlc` 等 | 图+权重容器；可含量化参数 | SNPE App；或 QNN+`libQnnModelDlc.so` | 同 SDK 列车内较好 |
| **model `.so`** | `qnn-onnx-converter`→codegen | 可执行图库，支持在线 prepare | 开发调试、CPU/GPU 冒烟 | 依赖链接与 ABI |
| **context binary**（`*.serialized`/`*.bin`） | `qnn-context-binary-generator` | 为**特定后端**离线固化的执行上下文 | **HTP 量产主产物** | **与 SoC/固件强绑定** |

典型命令骨架（路径随 SDK 变，**先钉 QAIRT 版本再抄绝对路径**）：
```bash
# 1) ONNX → QNN
qnn-onnx-converter --input_network model.onnx --output_path model_qnn \
  --input_list input_list.txt

# 2) 量化（二选一）：转换器内 PTQ，或 AIMET 先出量化 ONNX/DLC
# qnn-onnx-converter ... --act_bw 8 --weight_bw 8 --input_list calib_list.txt

# 3) HTP 离线 context（量产）
qnn-context-binary-generator --model libmodel.so --backend libQnnHtp.so \
  --binary_file model_htp.serialized

# 4) 真机/板端冒烟
qnn-net-run --model libmodel.so --backend libQnnHtp.so \
  --input_list input_list.txt --output_dir out/
qnn-profiler ...   # 参数随 SDK，用于逐层耗时
```

DLC→HTP 交叉路径（官方支持，**版本必须齐**）：
```bash
# 例：SNPE 2.15 产出的 DLC 只能配 QNN/QAIRT 2.15 列车（数字仅示例）
qnn-context-binary-generator --model libQnnModelDlc.so --dlc_path model_quant.dlc \
  --backend libQnnHtp.so --binary_file model_from_dlc.serialized
```

- **坑1 · 版本混用**：DLC 与 QNN 跨小版本 → Load fail 或诡异数值；原文未强调 → **纠正/补全**。
- **坑2 · 未量化上 HTP**：HTP 路径通常要量化图；FP 图硬推会失败或几乎全回落。
- **坑3 · 绑定**：context binary 换芯片代际、大改 DSP 固件、换 HTP 架构必须重建。
- **坑4 · >2GB 大模型**：老 SNPE 单 DLC 限制与新 QAIRT 大模型路径不同；LLM 走 Genie 多 binary，勿假设单 DLC 塞 7B。
- **量产目录建议**：`onnx/` 源、`dlc/` 可选中间、`ctx/` 发布 serialized、`cfg/` Genie JSON、`manifest.json` 指纹。

#### AIMET / AIMET-ONNX（量化侧交接面）
AIMET 解决「精度怎么压到硬件可吃」；QNN 解决「怎么编到 HTP」；二者中间交接物是**量化 ONNX 或量化 DLC**。
- **AIMET**：PyTorch 友好的 PTQ/QAT、AdaRound、混合精度、逐层敏感度。
- **AIMET-ONNX**：在 ONNX 上插量化节点/导出；适合「训练在 PT、部署只认 ONNX」的流水线。
- **推荐链路**：PT 导出 ONNX → AIMET-ONNX PTQ/QAT →（可选）`onnxsim` → `qnn-onnx-converter` → context binary → `qnn-net-run` 余弦比对。
- **ADX / 混合精度**：敏感层留高位宽，其余 INT8/INT4；与原文「敏感层排除」同思路，工具名不同。
- **已知坑**：假量化粒度（per-channel / per-block）与 HTP kernel 不一致 → 张量余弦尚可、任务指标崩；必须以**任务指标**做门禁。
- **版本**：AIMET 与 QAIRT 兼容表**需核验**当前 Release Notes，勿抄过期 blog 版本号。
- **关系**：原文⑤一句 AIMET → 本节**深化交接面与门禁**。

#### Genie / LLM SDK（生成式专用）
Genie 把「一次前向 QNN 图」升级为「多步 decode 服务」；没有它，你要自己管 Tokenizer、KV、采样与多图切换。
- **发布包最小集**：一个或多个 **ctx-bins** + `tokenizer` 文件 + **Genie JSON**（dialog / sampler / engine / model）。
- **后端**：`backend.type = QnnHtp`（主）| `QnnGpu` | `QnnGenAiTransformer`（名称以当前文档为准，**需核验**）。
- **CLI**：`genie-t2t-run` 做 text-to-text 冒烟；通过后再接 App/`GenieEngine`。
- **常调旋钮**：`context.size`（KV 上限）、`kv-dim`、`rope-theta`、`spill-fill-bufsize`、`use-mmap`、`mmap-budget`、`cpu-mask`、`n-threads`。
- **多 binary**：大 LLM 常切 prefill/decode 或多段图；JSON 里 `ctx-bins` **顺序错误**会出现「能跑但胡言」而非明确报错。
- **与 llama.cpp**：原型/CPU 回退用 GGUF；骁龙量产 HTP 用 Genie；**权重格式不互通**，两条流水线并行而非互转幻想。
- **验收口径**：TTFT、decode tokens/s、P99、壳温、峰值 RSS；固定 prompt+seed 比 logits/top-1。

#### Android 上 QNN HTP 注意事项（真机坑清单）
x86 主机转换成功 ≠ Android 进程能打开 HTP；中间隔着 **vendor 分区、SELinux、签名、热管理**。
- **库与 ABI**：设备侧需 `libQnnHtp.so`、HTP skel/stub、相关 Hexagon 库；App `abiFilters` 仅 `arm64-v8a`；注意 `extractNativeLibs`。
- **权限**：部分机型 HTP 仅系统/签名应用可访问；先用 `adb push` + 官方 `qnn-net-run` 确认板级通路，再嵌 App。
- **离线 prepare**：量产只发布 context binary；在线 prepare 冷启动长、临时内存高，车机易 OOM。
- **校准域漂移**：`--input_list` 图是 ImageNet、业务是车外摄像头 → INT8 精度「实验室好、路上差」。
- **调度抖动**：`cpu-mask` 与 Android cpuset/热插拔冲突 → tokens/s 方差大；验收抓 P99 与温控曲线。
- **与 NNAPI**：商店通用 App 可走 NNAPI Delegate；车规/座舱要可控性 → **QNN/Genie 直连**。
- **日志**：保留 QNN log / logcat 中 HTP prepare 失败字段；「改了模型却仍报旧错」先查是否加载了旧 serialized 缓存路径。

#### 高通侧「与原文已有内容的关系」小结
| 原文位置 | 本章动作 |
|----------|----------|
| ⑤ QNN 转换/编译/profiler | **补全** SNPE/QAIRT/Genie 命名与 DLC↔context |
| ⑪ QNN≈昇腾类比 | **深化**为可执行命令与版本配对坑 |
| ⑫ 高通 NPU 点名 | **展开** Android HTP 真机约束 |

---

### 🔶 NVIDIA：TensorRT 家族、Orin/Thor、DLA、CUDA Graphs

#### TensorRT vs TensorRT-LLM vs TensorRT Edge-LLM
三者共享「TensorRT」品牌，**部署目标与官方支持矩阵完全不同**。

| 组件 | 定位 | 典型产物 | Jetson 态度 | 典型命令/入口 |
|------|------|----------|-------------|---------------|
| **TensorRT** | CV/检测/分割/静态或动态 shape 前向 | `.engine` / `.plan` | ✅ 主路径 | `trtexec --onnx=... --saveEngine=...` |
| **TensorRT-LLM** | 数据中心 LLM（Paged KV、连续批、多卡） | checkpoint+engine 目录 | ❌ **官方不支持 Jetson** | `convert_checkpoint.py` → `trtllm-build` |
| **TensorRT Edge-LLM** | 嵌入式 LLM/VLM 的 **C++** 运行时 | 本机 build 的 engines + native binary | ✅ Orin/Thor 主推 | 随 JetPack/SDK 的 export→build→run |

- **纠正（相对社区传闻）**：Orin 上曾有人折腾 TRT-LLM，**不等于官方量产支持**；Thor 路线明确转向 **Edge-LLM**（随 JetPack 版本演进，**具体 JP 号需核验**）。
- **Edge-LLM 相对桌面 TRT 的本质差异**（与原文⑪对齐并补边界）：内建自回归循环、KV、（视版本）连续批/分页；发布物是**设备相关 engine**，不是「拷贝服务器 plan 就能跑」。
- **CV 与 LLM 双轨**：感知用经典 TRT；对话/VLM 用 Edge-LLM；不要试图把 decode 循环塞进单次 `enqueue` 的静态 engine 硬拧。

#### CUDA Graphs（运行时技巧，不是第四个产品）
把一串 CUDA kernel 启动录成图、一次提交，降 CPU launch 开销。
- **收益最大**：固定 shape 的 decode 步、固定插件组合的 TRT 执行。
- **收益很小/负优化**：每步动态 shape、图结构变、频繁 capture/destroy。
- **与 Edge-LLM**：部分版本内部已用 Graph；业务层勿重复 capture 导致冲突（**需核验**当前 SDK 是否暴露开关）。
- **坑**：Graph 与 stream/context 生命周期绑定；热更新 engine、fork 进程后必须重录。
- **关系**：原文⑤一笔带过 → **深化适用边界**。

#### Jetson Orin vs Thor（代际与精度）
精度是**硅能力**，软件开不出来；engine 与 **SM 架构**绑定。

| 能力 | Orin（Ampere，如 `sm_87`） | Thor（Blackwell 类，如 `sm_110`，SKU **需核验**） |
|------|---------------------------|--------------------------------------------------|
| FP16 | ✅ 基线 | ✅ |
| INT8 | ✅ | ✅ |
| INT4 权重量化（AWQ 等） | ✅ | ✅ |
| FP8 | ❌ | ✅ |
| **NVFP4** | ❌ | ✅（官方材料中的 Thor 甜点档） |
| 内存 | 共享 DRAM/NVMM | 同属嵌入式共享内存，带宽代际提升（**GB/s 需核验板级规格**） |
| LLM 推荐栈 | Edge-LLM 或 llama.cpp；W4A16 为主 | Edge-LLM；优先评估 NVFP4/FP8 |

- **铁律**：Orin 上 build 的 engine **不能**在 Thor 上 load（反之亦然）；ONNX 可迁移，engine 必须目标板重建。
- **Orin 满频验收**：`sudo nvpmodel -m 0` + `sudo jetson_clocks`；报表注明模式，否则实验室数字不可比。
- **Thor 迁移清单**：重锁 JetPack → 重装 Edge-LLM → 重导量化 → 本机 rebuild → 重跑 PPL/任务集；禁止只换权重文件。
- **关系**：原文⑫点名 Orin/Thor 精度 → **深化为表+迁移纪律**；原文⑧ Orin 无 FP8 → **保持一致**。

#### DLA 能力边界
DLA 是「规则 CNN 加速器」，不是「第二个能跑 Attention 的 GPU」。
- **适合**：Conv/Pool/激活/BN 融合链；INT8/FP16；固定或有限动态 shape 的视觉骨干。
- **不适合**：LLM Attention/采样循环、大量 Gather/Scatter、自定义 plugin 为主的图、高动态序列。
- **调度现实**：TRT 可 GPU+DLA 切分；VLM 里 ViT 或许可切一部分到 DLA，**LLM 主干仍在 GPU**（与原文⑧一致）。
- **反模式**：为刷「DLA 利用率」把图切碎，DRAM 拷贝上涨，端到端反而变慢。
- **验收拆分**：GPU ms / DLA ms / H2D+D2H ms / 端到端 ms；缺拷贝项视为不合格报告。
- **命令线索**：`trtexec` 的 DLA 相关开关（如 `--useDLACore`，**确切参数随 TRT 版本需核验**）+ Nsight Systems 看拷贝。

#### NVIDIA 常用可验证命令与产物
```bash
# CV / 通用 TRT
trtexec --onnx=model.onnx --saveEngine=model_fp16.engine --fp16
trtexec --loadEngine=model_fp16.engine --warmUp=200 --duration=60

# 动态 shape 示例（名称示意）
trtexec --onnx=model.onnx --minShapes=input:1x3x640x640 \
  --optShapes=input:4x3x640x640 --maxShapes=input:8x3x640x640 --fp16

# 环境钉死采集
cat /etc/nv_tegra_release
dpkg -l | grep -iE 'tensorrt|cuda-nvcc|cudnn'
```
- **产物**：`*.engine`（发布）、导出用 ONNX（源）、校准 cache（INT8）、Edge-LLM 的引擎目录（LLM）。
- **坑**：主机 x86 TRT 编的 engine 不能当 Jetson 发布物；CI 必须「板端 build」或「同架构 builder 镜像」。

#### NVIDIA 侧关系小结
| 原文 | 本章 |
|------|------|
| ⑤ TensorRT / Edge-LLM / CUDA Graphs | **纠正** Jetson≠TRT-LLM；**深化** Graphs/DLA |
| ⑧ Orin 无 FP8、engine 绑定 | **扩展** Thor NVFP4 与迁移 |
| ⑪ Edge-LLM vs 桌面 | **补全** 官方支持矩阵 |

---

### 🟣 华为昇腾：CANN 代际、OM/OMC、ATC vs OMG、MindIE vs Lite

#### CANN 代际与「列车」思维
CANN 是驱动+图编译器+算子+Runtime 的**捆绑列车**；混搭小版本是昇腾部署第一杀手。
- **逻辑分层**：FE 解析 → GE 图优化/调度 → TBE 或 Ascend C 算子 → Runtime/ACL 执行。
- **服务器社区/商用 CANN**：版本线常见 5.x/6.x/7.x/8.x 等；**具体 LTS 以项目锁定的文档为准（勿写死过期号）**。
- **座舱/麒麟 DDK**：原文 `omc0428` 一类**日期环境**与服务器 `toolkit` **不是同一个安装包**；工具名都叫「转模型」，产物与知识库却分家。
- **匹配检查**：板上 `npu-smi`（若有）或厂商版本查询 ↔ 主机转换工具版本 ↔ 模型魔数；缺一不转生产。
- **坑**：用新 ATC 转旧盒子、或旧知识库打新 DDK → Load model failed / 异常体积；先对版本再对图。

#### OM vs OMC（纠正隐含误解）
**不要**把 `.omc` 理解成「`.om` 的压缩版」；二者多为**不同产品线离线格式**。

| 后缀 | 主流工具 | 典型硬件场景 | 加载侧 |
|------|----------|--------------|--------|
| **`.om`** | **ATC** | Ascend 310/310B/910 等云边盒子/服务器 | ACL `aclmdlLoadFromFile` 等 |
| **`.omc`** | **OMG/omg**（原文座舱链） | 麒麟/HiAI 座舱板等定制链 | HiAI / 板端 Runtime / 原文测速脚本 |

- **关系标注**：原文⑩⑪ 深耕 OMC → 本章**补全 ATC/.om 主线**并**纠正格式混用**。
- **体积经验**：原文「OMC≈20MB 正常 / 37MB 异常」**只适用于该定制链冒烟**；**.om 不要套用**，改看返回码、GE dump、精度脚本。

#### ATC vs OMG（转换器对照）
| 维度 | ATC | OMG（原文） |
|------|-----|-------------|
| 角色 | CANN 官方张量编译器 | 板端/DDK 离线转换 |
| 输入 | ONNX/TF/AIR/Caffe 等 | ONNX 等（`--framework 5`） |
| 输出 | `*.om` | `*.omc` |
| 量化 | ATC 选项 + 配套压缩流程 | `compress_conf` + `dopt_so.py` 等 |
| 调优 | 算子编译日志、GE/DUMP 图 | `ENABLE_TUNE_DUMP`→`tbe_tune_main`→`hit_bank` |
| 关键参数 | `--soc_version` 必须对芯片 | `--target omc` 等板端参数 |

ATC 最小可验证例：
```bash
atc --model=resnet.onnx --framework=5 --output=out/resnet \
  --soc_version=<SOC_VERSION> --input_shape="input:1,3,224,224"
# 产出 out/resnet.om ；SOC_VERSION 用 npu-smi/文档查询，勿抄错芯片名
```

OMG 侧保持原文流程：量化 → `omg` →（可选）tune → 重转 → `02_measure_omc_speed.py`。

- **选型**：云边 Ascend 推理卡/盒子 → **ATC+.om**；麒麟座舱按已有资产 → **OMG+.omc**。
- **坑**：把 `.om` 当 `.omc` adb push、或反方向 → 必炸；CI 应用 `artifact_type` 字段拦截。

#### MindIE vs MindSpore Lite vs HiAI（云侧 vs 端侧）
- **MindIE**：云/服务器 **大模型推理引擎**（服务化、多卡、调度）；与 CANN **强配套**；对接 MindFormers 等；**不是**手机/座舱轻量 Runtime。
- **MindSpore Lite / mslite**：端侧 Runtime；`.ms`/MindIR；麒麟可 `MSDeviceTypeKirinNPU` + Provider（原文⑩）。
- **HiAI**：麒麟 NPU 厂商接口；可与 mslite、`.omc` 路径并存；在线编译在部分板上不可用（原文已证）。
- **端侧 LLM 现实**：小模型端侧 mslite/HiAI/llama.cpp；大模型 MindIE 上云；端云协同切分，**禁止假设 MindIE 能塞进座舱**。
- **关系**：原文⑩ mslite 实测 → 本章**补全 MindIE 边界**，防名词误推。

#### 昇腾侧关系小结
| 原文 | 本章 |
|------|------|
| ⑩ OMG/OMC/TBE/hit_bank | **保留**；并加 ATC/.om 对照 |
| ⑨ 编译器章提 OMG | **纠正**「昇腾只有一种离线格式」 |
| ⑫ 昇腾点名 | **展开** MindIE vs Lite |

---

### 🌍 其他 SoC / OS / Web 工具链速览

> **与原文关系**：⑫ 仅点到 J5/J6、TDA4、高通；本节**系统补全**「产物名 + 边界 + 典型坑」，便于选型短名单，不替代各厂算子手册。

#### 地平线 J5 / J6 · BPU
- **本质**：车规视觉 BPU，CNN/检测/分割舒适区；工具链在 Horizon 开放 SDK 内闭环。
- **产物**：由 ONNX/Caffe 等转出的厂商模型包 + 板端 runtime（具体扩展名随 SDK，**需核验**）。
- **要点**：定点量化与 BPU 指令强绑定；动态 shape、自回归 LLM 非一等公民。
- **选型**：智驾感知 → J5/J6；座舱 LLM/VLM → 另选 Orin/骁龙/云端，勿默认同板硬扛。
- **坑**：用通用 ORT 在 ARM 上「能跑」≠ BPU 加速；必须以厂商工具编译后的剖面为准。

#### 瑞芯微 RKNN
- **本质**：消费电子/工控 NPU 栈；主机 **RKNN-Toolkit2**，板端 **librknnrt**。
- **产物**：`*.rknn`。
- **流程概要**：`load_onnx` → `build(do_quantization=...)` → `export_rknn` → 板端 C API/`rknn_bench`。
- **坑**：不支持算子静默拆到 CPU → 延迟数量级变差；用 toolkit 查询 **op support** + 性能分析。
- **LLM**：非主场；若官方无对应 demo，视为高风险预研（**需核验**型号与版本）。

#### TI TDA4 · TIDL
- **本质**：C7x/MMA + **TIDL** 编译；常嵌 Processor SDK / EdgeAI / OpenVX 图。
- **产物**：TIDL artifacts + 图网络配置。
- **擅长**：低功耗、可预测时延的 CNN。
- **边界**：大 Transformer/LLM 基本出局；与「Orin 能上 LLM」不是同类选型。
- **坑**：DSP/A72 异构同步、内存 carveout 配错会导致偶现超时，需按 SDK 内存图检查。

#### MTK NeuroPilot · APU
- **本质**：手机/平板 AI 栈；**NeuroPilot** + **APU**；常见入口 TFLite Neuron Delegate / 厂商转换器。
- **产物**：厂商缓存/编译物（如部分平台 `.dla` 等，命名多变，**需核验**）。
- **坑**：同系列不同 SKU APU 代际差大；商店包必须 **runtime feature detect**，禁止写死「全系同等加速」。

#### 三星 NPU（Exynos 等）
- **本质**：多经 **NNAPI / 厂商 Delegate** 暴露；公开文档完整度通常弱于高通。
- **实务**：优先 TFLite 或 ORT+NNAPI；极致性能走 NDA 私有通道。
- **坑**：模拟器无 NPU；One UI/系统升级可改 Delegate → **OS 版本进 manifest**。

#### 苹果 ANE · Core ML · MLX
- **ANE**：硬件加速器；不直接写 kernel，经系统框架调度。
- **Core ML**：iOS/iPadOS/macOS 量产格式（`.mlmodel` / `.mlpackage`）；`coremltools.convert`。
- **MLX**：Apple Silicon 研究友好数组框架（统一内存）；适 Mac 实验，**不是** App Store 主交付物。
- **选型**：上架 App → Core ML；Mac 本地 LLM → MLX 或 llama.cpp Metal。
- **坑**：ANE 对动态控制流/大 LLM 受限；长上下文常回落 GPU/CPU；要测真机功耗不是只看 Xcode 模拟。

#### Intel OpenVINO
- **本质**：x86 CPU/iGPU/独显/酷睿 NPU 统一推理栈。
- **产物**：IR（`.xml`+`.bin`）或直接 ONNX；`openvino` Runtime。
- **命令**：`ovc model.onnx`；`benchmark_app -m model.xml -niter 100`。
- **端侧场景**：工控 PC、座舱 x86、边缘盒；与 ARM 手机栈平行。
- **LLM**：GenAI 组件随版本扩展，**模型列表需核验**目标版本。
- **坑**：插件（CPU/GPU/NPU）选错设备；驱动与 OpenVINO 版本不匹配导致 NPU 不可见。

#### ARM Ethos-U / 微控制器 NPU
- **本质**：MCU/AIoT；**Vela** 编译 TFLite 微模型到 Ethos-U 命令流。
- **产物**：经 Vela 处理的 `.tflite` + CMSIS-NN/Ethos 驱动。
- **边界**：KWS、小 ASR、小视觉；与 7B LLM 无交集。
- **关系**：原文语音「Cortex-M7 可跑 RNNNoise」同档 → **补全 NPU 编译器名 Vela**。

#### Windows DirectML
- **本质**：经 DX12 的 Windows ML 抽象；ORT **`DmlExecutionProvider`** / WinML。
- **产物**：仍是 ONNX；加速在 EP/驱动。
- **坑**：笔记本双显卡绑错适配器；WDDM/驱动升级后性能回退，需钉驱动版本。

#### Android NNAPI / NNAPI 2.0
- **本质**：系统 NeuralNetworks API；TFLite/ORT 经 Delegate 下发到厂商实现。
- **2.0+**：更丰富的控制流与类型支持（随 API level）。
- **坑**：**实现质量碎片化**——同 APK 在 A 机 NPU、B 机 CPU；量产要机型矩阵与超时兜底。
- **与 QNN**：要榨干骁龙 HTP → 直连；NNAPI 适合「能加速更好」的通用应用。

#### WebGPU / WebNN
- **WebGPU**：浏览器 GPU 计算；ORT Web / Transformers.js 等。
- **WebNN**：浏览器 ML API，后端可接系统 ML（视浏览器/OS）。
- **边界**：演示、插件、隐私侧轻量推理；**非车规主路径**。
- **坑**：算子覆盖与配额限制；无法对标 TRT/QNN 的确定性时延。

---

### 🔌 跨平台运行时对照

> **与原文关系**：ORT、llama.cpp、MNN、MediaPipe 等散落各章；本节给**角色定位与何时让位给厂商编译器**。

#### 总览表
| 运行时 | 主输入 | 典型产物/后端 | 端侧定位 | 让位给厂商栈的时机 |
|--------|--------|---------------|----------|-------------------|
| **ONNX Runtime** | ONNX | 各 EP | 跨平台默认中间层 | 要 HTP/TRT/BPU 极致时 |
| **ORT Mobile** | ONNX | XNNPACK/NNAPI/CoreML… | 包体裁剪的移动部署 | 同上 |
| **ExecuTorch** | `torch.export` | `.pte` + delegates | PT2 直出派 | 产线已钉死 ORT/QNN 时 |
| **TFLite / LiteRT** | TF/ONNX→TFLite | `.tflite` + Delegates | CV/小模型/MediaPipe | 车规私有 NPU 工具链 |
| **MNN** | 多框架 | 自有表达 | 手机 CV 轻量 | 需厂商认证引擎时 |
| **NCNN** | 自有 param/bin | CPU/GPU | 无依赖嵌入 | 需要 ONNX 生态时 |
| **Paddle Lite** | Paddle/ONNX | 端侧 | 飞桨原生产业链 | 非飞桨训练团队 |
| **MediaPipe** | 任务图 | 内嵌 TFLite 等 | 感知流水线框架 | 当「纯引擎」用时会错 |
| **MLC-LLM** | HF 等 | TVM 编译后端 | 跨平台 LLM 原型 | Jetson 量产 Edge-LLM |
| **MLX** | 自有 | Apple Silicon | Mac 实验 | iOS 量产 Core ML |
| **llama.cpp** | GGUF | CPU/CUDA/Metal/Vulkan… | LLM 基线与回退 | 已上 Genie/Edge-LLM HTP/GPU |

#### ONNX Runtime / EP（最常见中间层）
- **本质**：图执行 + **Execution Provider** 分流。
- **EP 例子**：CPU、CUDA、TensorRT、OpenVINO、DML、QNN（视版本）、NNAPI、CoreML。
- **命令/API**：`onnxruntime_perf_test`；`InferenceSession(..., providers=["TensorrtExecutionProvider", ...])`。
- **坑**：声明了 TRT EP 仍可能大量节点 fallback；必须打日志看 **node placement**。
- **策略**：用 ORT 做「两周内可演示」；并行启动厂商编译，用同一套精度脚本对比。

#### ExecuTorch
- **本质**：PyTorch 生态端侧运行时；`torch.export` → `.pte`。
- **适合**：已 all-in PyTorch 2.x 导出链、愿维护 delegate 的团队。
- **坑**：与 ORT 工具链不共享中间件；LLM 支持以官方 zoo 为准（**需核验**）。

#### TFLite / LiteRT
- **LiteRT** 为新品牌延续（文档是否全量替换 **需核验**）。
- **适合**：移动 CV、MediaPipe、微控制器（经转换）。
- **不适合**：把经典 TFLite 当成 7B LLM 主引擎；大模型看 AI Edge 专项或别的栈。

#### MNN / NCNN / Paddle Lite
- **共同点**：包体小、易 JNI/嵌入、偏 CV。
- **差异**：MNN 后端广；NCNN 极简依赖；Paddle Lite 对飞桨最省事。
- **量化**：各有 INT8 工具；不要假设与 TRT 校准 cache 互通。

#### MediaPipe
- **是任务图框架**，不是与 TRT 对等的「推理引擎替代品」。
- **适合**：摄像头→追踪→手势等流水线；模型节点底层可换 TFLite。

#### MLC-LLM
- **本质**：TVM 编译到 Metal/Vulkan/CUDA/WebGPU 等；一模型多后端原型强。
- **Jetson**：可作实验；量产仍优先 Edge-LLM/llama.cpp 等更常见交付路径。

#### 运行时选型三句话
1. 需要**厂商 NPU 确定性** → 离开通用 Runtime，上 QNN/TRT/ATC/RKNN/BPU。  
2. 需要**两周多平台演示** → ORT/TFLite/llama.cpp。  
3. 需要**长期双轨** → 通用 Runtime 保底 + 厂商产物冲性能，manifest 里两套指纹。

---

### 🧭 选型决策树：芯片 × 模型类型 × 约束

> **与原文关系**：⑪ Roofline 预筛偏「模型是否装得下」；本节偏「**选哪条工具链**」。二者串联：先 Roofline 可行性，再决策树定引擎。

#### Step 0 · 约束一票否决（先写进立项页）
- **内存**：权重+激活峰值+KV+Runtime > 可用 RAM×0.8 → 先砍参数量/位宽/上下文，别先换引擎幻想。
- **功耗**：持续功耗 > SoC 可持续包络 → 拒绝「实验室满频 tokens/s」作 KPI。
- **时延**：交互看 TTFT/P99；智驾看端到端 ms+抖动；浏览器方案直接剔出车规。
- **安全交付**：要离线编译、批次一致、OTA 回滚 → 必须有可哈希发布物（engine/ctx/om(c)/rknn…）。

#### Step 1 · 按模型类型进子树

**CV（检测/分割/分类/关键点）**
- Orin/Thor → **TensorRT**（评估 DLA 是否真划算）；备选 ORT-TRT EP。  
- 骁龙 → **QNN HTP context**；备选 SNPE DLC。  
- 昇腾盒子 → **ATC→.om**；麒麟座舱 → **OMG→.omc**。  
- J5/J6 → **BPU**；TDA4 → **TIDL**；RK → **RKNN**。  
- 泛手机 → TFLite/ORT+NNAPI；失败再厂商私有。  
- **精度门禁**：余弦/ mAP（原文标准可复用）。

**ASR / 语音增强 / KWS**
- 流式小模型 → ORT/TFLite/MNN + 状态 I/O；MCU → Vela/Ethos 或纯 CPU INT8。  
- 中大模型上 Orin → TRT；骁龙 → QNN。  
- **坑**：RNN/状态量化与动态轴；参考原文⑪ 语音 ONNX 展平 h/c。

**LLM**
- Orin → **Edge-LLM（INT4）** 或 **llama.cpp** 基线；**不要**默认 TRT-LLM。  
- Thor → **Edge-LLM + NVFP4/FP8**（以 JP 能力为准）。  
- 骁龙 → **Genie+QNN HTP**；回退 GPU/CPU。  
- 麒麟 → 小模型 mslite/HiAI；大模型云端 MindIE。  
- x86 → OpenVINO / ORT / llama.cpp。  
- 苹果手机 → 小 Core ML 或云端；Mac → MLX/llama.cpp。

**VLM**
- **拆分量化**：ViT/视觉塔 INT8 友好路径；语言模型 W4A16/NVFP4；LM Head/Embedding 敏感则保高精度。  
- **框架**：优先官方多模态支持的 Edge-LLM / Genie；拒绝「只支持静态 CNN」的 BPU/TIDL 硬吞全图。  
- **坑**：视觉 token 拉长 KV；上下文预算按「图+文」重算。

#### Step 2 · 芯片族速查（减枝表）
| 芯片族 | CV 默认 | LLM 默认 | 明确慎用/否决 |
|--------|---------|----------|----------------|
| 骁龙+HTP | QNN context | Genie | 只用 NNAPI 赌车规性能 |
| Orin | TRT±DLA | Edge-LLM / llama.cpp | TRT-LLM 当官方交付 |
| Thor | TRT | Edge-LLM（NVFP4） | 复用 Orin engine |
| 昇腾 310 系 | ATC/.om | 小模型 ACL；大模型 MindIE(云) | 与座舱 OMC 混用 |
| 麒麟座舱 | OMG/.omc、mslite | 小模型 NPU | 在线编译、超大 KV |
| J5/J6 | BPU | 通常否决 7B | — |
| RK 系 | RKNN | 高风险 | 当 Orin 替代 |
| TDA4 | TIDL | 否决 | — |
| 苹果 | Core ML | 小/云 | MLX 直接当 iOS 交付 |
| 酷睿+NPU | OpenVINO | OV/ORT/llama.cpp | 不钉驱动 |

#### Step 3 · 约束微调启发式
- **最低功耗**：HTP/BPU/DLA/APU/Ethos > GPU > CPU；接受算子覆盖与回落风险，用 profiler 抓回落。  
- **最低抖动**：离线产物 + 定频 + 绑核 + 关睿频/热惊喜；拒 Web/灰盒 NNAPI。  
- **最低内存**：INT4/NVFP4/Q4_K_M + KV INT8；VLM 分塔位宽。  
- **最快上市**：ORT/TFLite/llama.cpp 双周演示 → 并行厂商编译 → 同脚本验收切换。

#### Step 4 · 场景化示例（可直接抄进评审）
1. **座舱语音助手 1.5B～3B，骁龙旗舰，功耗≤8W** → Genie+HTP INT4；llama.cpp 做精度对照；NNAPI 不进量产主路径。  
2. **Orin 行车记录仪检测 + 小 VLM** → 检测 TRT(+可选 DLA)；VLM Edge-LLM INT4；分进程/分优先级，避免 DLA 拷贝拖死 LLM。  
3. **Thor 机器人本地 8B** → Edge-LLM NVFP4；FP16 仅作精度上限对照；engine 板端 build。  
4. **昇腾 310B 盒子抽帧检测** → ATC INT8 `.om`；不要用座舱 OMG。  
5. **麒麟座舱已有 OMC 流水线** → 继续 OMC+知识库；新 LLM 另开端云，不塞同一 OMC。  
6. **RK3588 面板 CV** → RKNN INT8；弃用「ORT CPU 也能跑」当性能基线。  
7. **Windows 工控质检** → OpenVINO 或 ORT+DML；钉 GPU/NPU 驱动。  
8. **浏览器插件摘要** → WebGPU/WebNN 或 ORT-Web；明确非车规。

#### Step 5 · 决策输出模板
```
芯片SKU: ... | 模型类型: CV|ASR|LLM|VLM | SLO: TTFT=/P99=/功耗W=/内存GB=
主引擎: ... | 发布产物: ... | 备选回退: ... | 否决项: ...
版本列车: 见钉死清单 | 指纹: artifact_sha256= ...
Roofline预筛: 通过|不通过(原因)
```

---

### 📌 版本钉死清单：驱动 / 编译器 / Runtime / 模型指纹

> **与原文关系**：⑥版本管理、⑧ JP/TRT 钉死、⑩ DDK 匹配 → **展开为可勾选清单 + manifest + CI 门禁**。

#### 原则
- **整车升级**：驱动/镜像/SDK/Runtime so/模型 **同一变更单**；禁止「只换模型文件」。  
- **双指纹**：`artifact_sha256`（内容）+ `env_fingerprint`（可跑环境）；缺一不能入库。  
- **可复现**：校准集哈希、量化配置、max_seq_len/shape 全部入账。

#### 高通勾选清单
- [ ] SoC 型号、Android API、vendor DSP/HTP 固件只读属性  
- [ ] QAIRT / QNN / SNPE **同列车版本号**（混用则记配对表）  
- [ ] AIMET/AIMET-ONNX 版本、ONNX opset  
- [ ] `qnn-onnx-converter` / `qnn-context-binary-generator` / Genie 版本  
- [ ] 产物：`dlc`/`so`/`serialized`/Genie JSON/tokenizer 的 sha256  
- [ ] 设备与构建机 `libQnnHtp.so` 等哈希对照  
- [ ] `input_list`/校准集哈希、位宽配置  

#### Jetson 勾选清单
- [ ] JetPack/L4T、`nvpmodel` 模式  
- [ ] CUDA / cuDNN / TensorRT / Edge-LLM 版本  
- [ ] GPU SM（`sm_87`/`sm_110`…）、是否 DLA  
- [ ] engine 在**目标板或同架构**重建的证明（CI 日志）  
- [ ] 量化方案（FP16/INT8/INT4/FP8/NVFP4）与 KV 策略  
- [ ] `max_seq_len`、插件开关、shape 优化配置  

#### 昇腾 / 麒麟勾选清单
- [ ] 服务器：`npu-smi`、CANN toolkit/kernels、ATC、`soc_version`  
- [ ] 座舱：DDK 日期环境、omg、`compress_conf`、`hit_bank` 哈希  
- [ ] 产物类型字段强制 `om` 或 `omc`（防混）  
- [ ] MindIE 或 mslite 版本（若使用）与 CANN/板端匹配表  

#### 跨平台 Runtime 勾选
- [ ] ORT/TFLite/ExecuTorch/MNN… **精确版本**  
- [ ] EP/Delegate 名称；若 EP 转调 TRT/QNN，厂商清单仍要满足  

#### `manifest.json` 最小字段
```json
{
  "model_name": "task_model_id",
  "model_sha256": "...",
  "artifact_sha256": "...",
  "artifact_type": "qnn_context|trt_engine|om|omc|rknn|gguf|tflite|mlpackage",
  "sdk_train": {
    "platform": "qnn|jetson|ascend|kirin|horizon|rknn|other",
    "sdk_version": "...",
    "compiler": "...",
    "runtime": "...",
    "driver_or_jp_or_cann": "..."
  },
  "quant": {"scheme": "w8a8|w4a16|nvfp4|q4_k_m|...", "calib_sha256": "..."},
  "shapes": {"max_seq_len": 4096, "input": "1x3x640x640"},
  "metrics_baseline": {"p50_ms": 0, "p99_ms": 0, "ppl_delta": 0, "map_delta": 0},
  "built_at": "ISO-8601",
  "git_commit": "..."
}
```

#### CI 门禁伪流程
```bash
# 构建结束
sha256sum "$ARTIFACT" | tee artifact.sha256
python gen_manifest.py --artifact "$ARTIFACT" --env-probe

# 门禁
python check_manifest.py --baseline manifest_golden.json --candidate manifest.json
# 失败条件：sdk_train 漂移 | artifact 哈希变但无变更单号 | artifact_type 与平台不符
```

#### 环境采集命令碎片
```bash
# Jetson
cat /etc/nv_tegra_release
dpkg -l | grep -iE 'nvidia-tensorrt|cuda'

# 昇腾服务器
npu-smi info
atc --version 2>/dev/null || true

# Android 高通
adb shell getprop | grep -iE 'version|dsp|vendor'
adb shell ls -l /vendor/lib64/libQnn*.so
```

#### 版本类已知坑
- 能 load 偶发错数 → Runtime so 与编译 SDK 微版本漂移。  
- OTA 只更模型不更 HTP so → 隐蔽失败；AB 包应绑 **模型+so 清单**。  
- 校准集未指纹化 → 「同开关」不可复现。  
- Thor 脚本打开 NVFP4 却在 Orin 跑 → 构建失败或忽略，必须以日志为准。  
- 昇腾 `.om`/`.omc` 混后缀入库 → 板上难以理解的 Load failed。

---

### ✅ 并入与维护约定

- **学习路径**：保留原文⑤「QNN 或 Orin 二选一主攻」。  
- **立项路径**：用本章决策树 + 钉死清单出 `manifest.json`。  
- **实战细节**：昇腾搜算子、QNN 命令细参、Edge-LLM 机制仍以原文⑩⑪⑧为准；本章不重复粘贴。  
- **不确定项**：凡标「需核验」者，入库前用**当前**官方 Release Notes 填版本号，禁止把示例数字写成项目基线。

---

### 附录 A · 产物名速查

| 生态 | 中间/发布产物 | 关键命令或入口 |
|------|---------------|----------------|
| 高通 SNPE | `.dlc` | `snpe-onnx-to-dlc`, `snpe-net-run` |
| 高通 QNN | `.so`, `*.serialized` | `qnn-onnx-converter`, `qnn-context-binary-generator`, `qnn-net-run`, `qnn-profiler` |
| 高通 Genie | ctx-bins + JSON + tokenizer | `genie-t2t-run` |
| TensorRT | `.engine`/`.plan` | `trtexec` |
| TRT-LLM | 服务器 engine 目录 | `trtllm-build`（非 Jetson 官方） |
| Edge-LLM | 板端 engines + C++ runtime | JetPack/SDK 流程 |
| 昇腾 ATC | `.om` | `atc --framework=5` |
| 麒麟 OMG | `.omc` | `omg`, `dopt_so.py` |
| RKNN | `.rknn` | RKNN-Toolkit2 |
| OpenVINO | `.xml+.bin` | `ovc`, `benchmark_app` |
| Core ML | `.mlpackage` | `coremltools` |
| TFLite/LiteRT | `.tflite` | converter + Interpreter |
| ORT | `.onnx` + EP | `InferenceSession` |
| ExecuTorch | `.pte` | `torch.export` 链 |
| llama.cpp | `.gguf` | `llama-quantize`, `llama-cli` |
| Ethos-U | Vela 后 `.tflite` | `vela` |
| DirectML | ONNX | ORT `DmlExecutionProvider` |

### 附录 B · 「与原文关系」总表

| 主题 | 关系类型 | 说明 |
|------|----------|------|
| QNN 基础流 | 补全 | 增加 SNPE/QAIRT/Genie/DLC 差异 |
| Jetson LLM | 纠正 | 明确 TRT-LLM 非 Jetson 官方路径 |
| Orin/Thor 精度 | 深化 | 表化 FP8/NVFP4 与迁移纪律 |
| 昇腾格式 | 纠正+补全 | OM≠OMC；补 ATC |
| MindIE | 补全 | 云侧边界，防误推座舱 |
| 多厂商 SoC | 补全 | ⑫ 点名之外系统化 |
| 跨平台 Runtime | 补全 | 定位与让位时机 |
| 选型树 | 补全 | 工具链视角，接 Roofline |
| 版本指纹 | 深化 | manifest/CI 可执行化 |

### 附录 C · 建议评审提问（防止空话过关）

1. 发布物后缀是什么？能否在干净设备上只靠清单复现？  
2. 若 HTP/NPU 全回落 CPU，谁能在 10 分钟内用 profiler 证明？  
3. Orin↔Thor 或 DDK 升级时，哪一条流水线会强制重建？  
4. LLM 方案是否误用了「仅 CV 友好」的 BPU/TIDL/DLA？  
5. manifest 中校准集哈希是否为空？（空则精度不可审计）


---

### 🔧 工程深挖：高通链路排错与验收矩阵

#### 转换期失败 vs 运行期失败（先分类再查）
端侧高通问题 80% 来自「阶段误判」：把 Runtime 权限问题当成量化问题，或把算子回落当成模型坏了。
- **转换期**：qnn-onnx-converter / AIMET 退出非 0；看不支持算子列表、opset、动态轴、权重 >2GB 切分策略。
- **生成 context 期**：qnn-context-binary-generator 失败；多与后端 so 路径、未量化、HTP config JSON、图过大有关。
- **设备 Load 期**：找不到 skel、SELinux拒绝、context 与固件不匹配、ABI 混用（把 x86 主机 so 推进手机）。
- **数值期**：能跑但结果漂；校准域、Endian/布局、前处理归一化、Genie 多 bin 顺序。
- **性能期**：能出正确结果但慢；算子在 CPU、热降频、未 mmap、KV 超额换出。

#### 建议的「最小证据包」（提 Bug 给 BSP 必带）
- 主机：QAIRT 版本、qnn-onnx-converter --version 输出、完整命令行、ONNX sha256、opset。
- 产物：serialized/dlc 大小与 sha256、生成日志尾部 200 行。
- 设备：getprop 关键项、ls -l 全部 libQnn*.so、同一输入的 qnn-net-run 输出 bin、qnn-profiler csv。
- 若 Genie：完整 JSON（可打码路径）、tokenizer 版本、固定 prompt 的参考输出哈希。
- 环境：壳温、	op/dumpsys thermalservice 片段，避免「冷机一次数据」骗过验收。

#### HTP 扩展配置里工程师真改的字段
不同 SDK 字段名略有差异，以下按「意图」记忆，落地时对照当前 htp_backend_ext_config.json（名称 **需核验**）。
- **vtcm / store-banks 类**：片上内存预算；过大失败、过小溢到 DDR 变慢。
- **spill-fill**：权重/激活溢出到 DDR 的缓冲；LLM 易触达，盲目加大涨 RSS。
- **fp16-relax / 精度模式**：换速度时可能动数值；改后强制重跑余弦与任务集。
- **graph-names / 多图**：与 Genie 多 ctx-bins 对齐；名字不一致会跑错图。
- **device-id**：多 NPU/多会话设备号；车机多进程抢同一 HTP 要协调。

#### 高通 CV 与 LLM 验收门禁（可写进 CI）
| 门禁 | CV | LLM/VLM |
|------|----|---------|
| 功能 | qnn-net-run 出口与 ORT CPU 参考对比 | 固定 seed 下 top-1 token 一致率或 KL |
| 精度 | 逐层余弦>99% 或业务 mAP 跌幅阈值 | PPL 增量阈值 + 业务集准确率 |
| 性能 | P50/P99 ms，注明输入分辨率 | TTFT、decode tok/s、P99，注明 context |
| 资源 | RSS/ION 峰值 | RSS+KV 峰值，shell 温度 |
| 产物 | context sha256 匹配 manifest | ctx-bins 列表哈希 + JSON 哈希 |

#### SNPE 何时还值得用
- 团队已有大量 DLC 资产、只需 CPU/GPU/HTP 切换、不想到 QNN 细抠。
- 新项目若从零开始且目标是 HTP 极致与 LLM → **直接 QAIRT/QNN/Genie**，减少一次格式迁徙。
- 长期：DLC 仍可通过 libQnnModelDlc.so 进入 QNN，但要接受**双工具链版本锁**成本。

---

### 🔧 工程深挖：Jetson / TensorRT 交付纪律

#### 「能在 PC 上 trtexec」不等于「能在车上跑」
- 架构：x86_64 builder 产物与 sm_87/sm_110 不通用。
- 库：主机 TensorRT 大版本与 JetPack 自带 TRT 不一致时，即使同 SM 也可能拒载。
- 插件：自定义 *.so plugin 必须与引擎同 TRT/CUDA 列车，OTA 时与 engine 打同一包。
- 许可与镜像：量产根文件系统应冻结 JetPack 点版本，不跟主线随意 pt upgrade cuda。

#### INT8 校准与 LLM 权重量化不要混为一谈
- **经典 TRT INT8**：激活+权重 PTQ，依赖校准集与 calibrator（Entropy/MinMax 等）；适 CV。
- **LLM INT4/AWQ**：主要是**权重**压缩；激活常 FP16；工具链在 Edge-LLM/相关量化脚本，不是随手 --int8 完事。
- **KV 量化**：独立开关与独立精度风险；与权重 INT4 正交（原文⑧已述，这里强调**别写进同一个「开了量化」开关故事**）。
- **Thor FP8/NVFP4**：属硬件友好低精度，迁移时重新评估校准/缩放，不是改个 flag 从 Orin 继承。

#### DLA 实操决策序
1. 先 GPU-only 建基线 engine，测端到端。  
2. 再允许 DLA，看 TRT 层分配与 	rtexec 报告。  
3. 若 DLA 层少、拷贝多，**关掉 DLA** 保留 GPU。  
4. 只有当骨干整段稳定在 DLA 且拷贝可接受时，才进量产配置。  
5. LLM/VLM 的 Attention 路径默认不参与「必须上 DLA」KPI。

#### Jetson LLM 双轨策略（降低进度风险）
- **轨 A（量产目标）**：Edge-LLM + 板端 engine + C++ 服务。  
- **轨 B（对照/回退）**：llama.cpp/GGUF 或官方容器（如社区常用 ollama/vLLM 容器，**是否允许上车需核验许可与资源**）。  
- 精度门禁脚本两轨共用同一 prompt 集；性能 KPI 只认轨 A。  
- 禁止把轨 B 的 tokens/s 写进对客户的 Orin 承诺书，除非合同写明栈。

#### Thor 升级检查表（从 Orin 项目迁）
- [ ] 新 JetPack 发行说明中 Edge-LLM / TRT / CUDA 版本  
- [ ] 是否支持 NVFP4/FP8；量化脚本是否换  
- [ ] 所有 engine 清理重建，禁止「先试试旧 engine」  
- [ ] 内存与带宽假设按新板重算 KV 与 batch  
- [ ] 热设计功耗墙重测，不沿用 Orin 的 nvpmodel 经验数字  

---

### 🔧 工程深挖：昇腾双轨（云边 .om vs 座舱 .omc）

#### 何时开 ATC 轨，何时开 OMG 轨
- **ATC 轨**：硬件是 Ascend 训练/推理卡或官方盒子、软件是标准 CANN、应用走 ACL。  
- **OMG 轨**：硬件是原文麒麟座舱一类、软件是日期 DDK、产物 .omc、调优靠 hit_bank。  
- **禁止**：同一 Jenkins Job 不加区分地「有什么工具用什么」；Job 名应含 om 或 omc。

#### ATC 轨最小闭环
1. 固定 CANN 与 soc_version。  
2. ONNX 简化与算子审查（动态 shape 单独开专题）。  
3. tc 出 .om。  
4. ACL 或官方样例加载，clError 非 0 立即停。  
5. 精度脚本（逐层或业务）+ 性能（多次取稳）。  
6. 入库 manifest：rtifact_type=om。

#### OMG 轨最小闭环（与原文对齐的「工具链视图」）
1. 量化 dopt_so.py + compress_conf。  
2. omg 出 .omc，看体积冒烟。  
3. 板上测速 model.csv/op.csv。  
4. 超时算子 	be_tune_main，更新 hit_bank。  
5. 重转 + 回归。  
6. 入库 rtifact_type=omc + hit_bank 哈希。

#### MindIE 接入判据（避免误用）
- 需要 HTTP/gRPC 服务化、多卡、连续批、大上下文 → 考虑 MindIE。  
- 需要进程内库、车机 APK/进程、离线小模型 → MindIE **出局**，看 mslite/HiAI/llama.cpp。  
- MindIE 与 CANN 版本表必须逐项对；升 CANN 不升 MindIE 是常见事故（**具体配对表需核验当年文档**）。

#### 昇腾常见误报与处理
| 现象 | 更可能原因 | 动作 |
|------|------------|------|
| Load failed | 版本/格式（om↔omc）错 | 查 artifact_type 与 CANN/DDK |
| 结果全零 | 输入 NCHW/均值方差错 | 先 ORT 对齐前处理 |
| 偶发超时 | 频率/温度/多进程抢 NPU | 定频+单实例复现 |
| OMC 体积异常 | 量化/图优化未生效 | 按原文 20 vs 37MB 经验排查 |
| 调优无收益 | 未命中 hit_bank 或搜错算子 | check_hit_bank 统计 |

---

### 🔧 工程深挖：跨厂商「能力成熟度」四级

用同一把尺给平台打标，避免「都能跑 AI」的采购空话。
- **L0 实验**：官方 Demo 能跑；无版本锁、无指纹。  
- **L1 工程**：自有模型转通；有精度脚本；版本记在 README。  
- **L2 准量产**：manifest + CI 门禁；P99/温度；OTA 回滚演练。  
- **L3 量产**：批次一致性、ASIL 流程（若需要）、现场日志与远程指纹核对。  
- **建议**：采购或立项时要求供应商演示到 **L2**；仅 L0 Demo 不算选型完成。

对本章平台的**经验性**成熟度（项目可改，不作为厂商排名）：
| 平台 | CV L2 可达性 | LLM L2 可达性 | 备注 |
|------|--------------|---------------|------|
| 骁龙 QNN/Genie | 高 | 中高（看芯片与授权） | Android 权限是变量 |
| Orin TRT/Edge-LLM | 高 | 中高 | TRT-LLM 不计入 |
| Thor Edge-LLM | 高（预期） | 高（NVFP4，随 JP） | 版本需核验 |
| 昇腾 ATC | 高 | 云侧高/端侧低 | 分轨 |
| 麒麟 OMC/mslite | 中高（有资产时） | 低～中（小模型） | 在线编译坑 |
| J5/J6 | 高（视觉） | 低 | |
| RKNN | 中高 | 低 | |
| TIDL | 中高 | 极低 | |
| OpenVINO | 高 | 中 | 驱动敏感 |
| Core ML | 高 | 低～中 | ANE 限制 |
| ORT 通用 | 中（易 L1） | 中 | 极致需让位 |

---

### 🔧 工程深挖：跨平台 Runtime 的「EP 落点审计」

通用 Runtime 最大的隐形成本是 **fallback**。没有落点审计，性能数字不可信。
- **ORT**：打开详细日志，导出各 node 的 EP；统计 TRT/CPU 比例。  
- **TFLite**：Delegate 状态与 ModifyGraphWithDelegate 后的节点划分。  
- **NNAPI**：getSupportedOperations 与实际编译结果；失败回退要有超时。  
- **QNN via ORT EP**：确认 EP 版本与直连 QNN 列车一致，勿「ORT 新、QNN 旧」。  
- **门禁**：若关键大算子（卷积/GEMM/Attention）落在 CPU，CI 直接失败（可配置白名单）。

#### 何时允许「双 Runtime 共存」
- 应用层：视觉 ORT、语音 TFLite、LLM Genie —— 可以，但 **进程内内存池**要规划。  
- 同一模型两套后端：只允许「灰度对比期」；量产开关必须单一，避免双倍闪存与双倍认证。

---

### 🔧 工程深挖：选型决策的反例集（专门打脸空话）

1. **「我们有 NPU，所以 LLM 没问题」** → NPU 可能只会 INT8 CNN；先跑 1 个 Attention block 的 profiler。  
2. **「ORIN 上 TRT-LLM 跑过，量产同样」** → 官方矩阵不支持则供应链不可交付。  
3. **「OMC 和 OM 差个 c」** → 当成不同物种。  
4. **「先上 NNAPI，不行再优化」** → 车规项目会把碎片化行为带进认证，后期更贵。  
5. **「Thor 沿用 Orin engine」** → 必炸或未测路径。  
6. **「MindIE 推到座舱」** → 错层。  
7. **「GGUF 转 QNN」** → 无此免费午餐。  
8. **「DLA 占用率 KPI」** → 可能优化到更慢。  
9. **「只报平均 tokens/s」** → 无 P99/温度=自评作假。  
10. **「校准集临时拍 50 张」** → 未进指纹，季度复现失败。

---

### 🔧 工程深挖：版本钉死的仓库布局建议

`
deploy/
  models/
    src_onnx/          # 仅源，大门禁
    artifacts/         # 按平台分子目录 qnn/ trt/ om/ omc/ ...
  sdk_lock/
    qairt.txt          # 精确版本与下载 URL/哈希
    jetpack.txt
    cann.txt
  manifests/
    <model>_<platform>_<version>.json
  scripts/
    build_*.sh
    probe_env.py
    check_manifest.py
  golden/
    metrics_baseline.json
`

- **权限**：rtifacts/ 只读发布；重建走 CI，不手改。  
- **清理策略**：SoC 换代删除旧 engine，保留源 ONNX 与量化配置以便重建。  
- **审计**：季度抽检「金机」上 sha256 是否与仓库一致，防现场私换包。

#### probe_env.py 应采集的最小键
platform、os、kernel、driver、sdk、
untime_so_hashes、
pu_firmware、max_clocks、	hermal_status、git_commit。

#### 发布签名（车规增强，可选）
- 对 manifest.json + artifact 做签名（Secure Boot 链路或应用层验签）。  
- 启动时验签失败不推理，防刷机篡改。

---

### 🔧 场景全文：从立项到首包的两周节奏（示例）

**假设**：座舱语音助手，输入芯片为骁龙旗舰 + 可选云端，模型 2B 级指令模型，SLO：TTFT<800ms，decode≥20 tok/s，功耗≤8W 持续。

**Day 1–2**：Roofline 与内存预算；确认 INT4 后权重+KV 是否装进 RAM；否决 7B。  
**Day 3–4**：llama.cpp/GGUF Q4_K_M 在同设备 CPU/GPU 出功能基线与精度下限。  
**Day 5–7**：AIMET/官方量化 → QNN → context → Genie JSON；打通 genie-t2t-run。  
**Day 8–9**：profiler 抓回落算子；调 VTCM/spill；绑核；热稳态 30min 测 P99。  
**Day 10**：与 GGUF 基线对比业务集；不过则混合精度/改模。  
**Day 11–12**：写入 manifest、CI、OTA 包结构；准备回退到云端 API 的产品开关。  
**Day 13–14**：金机矩阵（高低温度）、文档与「需核验」项清零（填真实版本号）。

**并行否决点**：若 Day7 仍无法在 HTP 加载，立即评估换模或云端，而不是「再试一周同方法」。

---

### 附录 D · 命令与产物「真伪校验」清单

在把命令写进 SOP 前，用这一节防止文档老化。
- 命令是否仍存在：which qnn-onnx-converter trtexec atc omg ovc vela。  
- 帮助是否可解析：--help 中关键子命令是否改名（高通工具曾多次更名）。  
- 产物是否可识别：文件头魔数/扩展名/strings 中版本串。  
- 设备是否可加载：最小样例（官方 inception/resnet/玩具 LLM）先过。  
- 文档日期：SOP 页脚写「对照 SDK 文档日期」，超过 180 天强制复审。

### 附录 E · 名词别名表（防会议各说各话）

| 口头说法 | 建议书面词 | 避免误解 |
|----------|------------|----------|
| 高通 NPU | HTP（或 Hexagon HTP） | 与安卓抽象 NPU 混淆 |
| AI Engine | QNN / AI Engine Direct | 与 Android NNAPI 混淆 |
| TRT | TensorRT（说明是否 LLM） | 与 TRT-LLM/Edge-LLM 混淆 |
| 转 OM | ATC→.om 或 OMG→.omc | 必须说清后缀 |
| 端侧 MindSpore | MindSpore Lite | 与 MindIE 混淆 |
| 苹果 NPU | ANE | 与 MLX 混淆 |
| 浏览器 GPU | WebGPU | 与 WebNN 混淆 |
| 量化了 | 写清 W8A8 / W4A16 / KV-INT8 / NVFP4 | 无法复现 |

### 附录 F · 本章覆盖对照（需求清单）

| 需求点 | 覆盖位置 |
|--------|----------|
| SNPE vs QNN vs AI Engine Direct vs HTP/HMX | 高通命名栈 + 硬件层 |
| AIMET-ONNX | 量化交接面 |
| Genie/LLM SDK | Genie 节 + 验收矩阵 |
| Android QNN HTP 注意 | Android 坑清单 + 证据包 |
| DLC vs context | 产物三段论 |
| TRT vs TRT-LLM vs Edge-LLM vs Graphs | NVIDIA 首节 + Graphs |
| Orin/Thor FP8/NVFP4 | 代际表 |
| DLA 边界 | DLA 节 + 决策序 |
| CANN/OM/OMC/ATC/OMG | 昇腾双轨 |
| MindIE vs Lite | 云端侧边界 |
| J5/J6、RKNN、TIDL、NeuroPilot、三星、苹果、OpenVINO、Ethos、DML、NNAPI、WebGPU/WebNN | 其他 SoC 节 |
| ORT/ET/TFLite/MNN/NCNN/Paddle/MediaPipe/MLC/MLX | Runtime 对照 |
| 选型决策树 | 决策树 + 反例 + 两周节奏 |
| 版本钉死 | 钉死清单 + 仓库布局 |
