# 端侧思维导图 · 维护手册

目标：在**任意一台电脑**上改讲义，并更新网上的页面。

网上地址：https://riddlego.github.io/edge-llm-mindmap/

---

## 0. 先记住三件事

1. **只改 Markdown 文档，不要改网页里的代码。**  
   - 端侧主线 → `端侧模型部署.md`  
   - JD 补全 → `JD能力缺口补全.md`
2. 改完运行一次 `python 更新网页.py`，网站才会变。
3. 网页本身是静态的，浏览器里改字**存不回去**。

---

## 1. 新电脑第一次准备（只做一次）

### 1.1 安装软件

需要三样：

| 软件 | 干什么 | 去哪装 |
|------|--------|--------|
| Python 3.10+ | 跑生成脚本 | https://www.python.org/downloads/ （安装勾选 Add Python to PATH） |
| Git | 拉代码 | https://git-scm.com/ |
| GitHub CLI `gh` | 推送到网站 | https://cli.github.com/ |

安装完打开终端，检查：

```text
python --version
git --version
gh --version
```

都能打出版本号即可。这些脚本只用 Python 标准库，**不用 pip 装包**。

### 1.2 登录 GitHub（每台电脑做一次，之后就能一键）

**不要把 Personal Access Token 写进仓库、手册或发给别人。**  
写进 GitHub 的文件等于公开密钥，任何人都能改你的库。

正确做法：用 GitHub CLI 把登录存在**这台电脑自己的钥匙串**里。`更新网页.py` 会自动用这份登录推送，你不用每次贴 token。

```text
gh auth login
```

按提示选：GitHub.com → HTTPS → Login with a web browser。

检查：

```text
gh auth status
```

应看到已登录（例如账号 `RiddleGo`）。

以后在这台电脑上：改 md → `python 更新网页.py`（或双击 `更新网页.bat`）即可。  
换电脑：重复安装 + 再 `gh auth login` 一次，仍然不要拷贝 token 进文件夹。

### 1.3 拿到这份工程

**推荐：直接克隆网上那份仓库**（里面有 md、脚本、HTML）：

```text
gh repo clone RiddleGo/edge-llm-mindmap
cd edge-llm-mindmap
```

或用 Git：

```text
git clone https://github.com/RiddleGo/edge-llm-mindmap.git
cd edge-llm-mindmap
```

文件夹可以放在任意盘、任意路径（例如 `C:\work\edge-llm-mindmap` 或 Mac 的 `~/code/edge-llm-mindmap`）。脚本会跟着当前文件夹走，**不依赖** `D:\推理VS训练`。

用 U 盘拷贝整个文件夹也可以，但新电脑仍要做 1.1 和 1.2。

---

## 2. 平时怎么改内容（举例）

用记事本、VS Code、Cursor 打开 `端侧模型部署.md`。

### 2.1 结构和网页的对应关系

```text
## 〇 先认端侧                          ← 网页左边第 1 章
### 端 · 近边 · 云：三个词先对齐        ← 网页中间的主题
（空一行）
这里是第一段。                          ← 右侧讲义第 1 段
（空一行）
这里是第二段。                          ← 右侧讲义第 2 段
```

顶栏切到「JD补全」时，对应的是 `JD能力缺口补全.md`，规则相同：`##` 章、`###` 主题、空行分段。

### 2.2 实例：改「端」这一段

文件里现在大约是：

```markdown
### 端 · 近边 · 云：三个词先对齐

评审里最常翻车的，是把「能 ping 通的局域网盒子」或 MEC 应用写成「端侧」。三个词混着说，后面选型、验收、故障回落都会对不上。工程上先问：**数据在哪产生、算力归谁管、失败回落哪一层**。

**端（on-device）** — 数据产生/消费处的终端或嵌入式节点：……
```

网页路径：进入地图 → **1 · 先认端侧** → **1.1 · 端 · 近边 · 云：三个词先对齐** → 看右边。

假如你只想改「端」的定义，把那一长段换成：

```markdown
**端（on-device）** — 手机、车机、旁侧盒：权重在本机内存里跑。断网还得能完成约定任务。别把 MEC 写成端侧。
```

保存文件。此时**网站还没变**。

### 2.3 写法约定

- 改 `###` 后面的标题 → 树上那个节点的名字会变。
- 只改段落、不改标题 → 节点还在原位置，只换右侧正文。
- **空一行** = 网页上新起一段（段间距较大）。
- `**加粗**` = 右侧高亮重点。
- 不要依赖网页编辑；不要手改 `index.html` 里 `const TREE_MAIN = ...` 那一大段。

新增一小节：在对应 `##` 章下面加一个 `### 新标题`，下面写几段正文，保存后再发布。

---

## 3. 改完如何发到网上

在**工程根目录**打开终端（就是有 `端侧模型部署.md` 和 `更新网页.py` 的那一层）：

Windows PowerShell / CMD：

```text
python 更新网页.py
```

Mac / Linux：

```text
python3 更新网页.py
```

它会依次：从 md 生成树 → 灌进 HTML → 推到 GitHub。成功时最后会打印 Pages 地址。

等 **1～3 分钟**，浏览器强制刷新（Ctrl+F5 或 Cmd+Shift+R）：  
https://riddlego.github.io/edge-llm-mindmap/

再点开你改过的那一节，核对右侧文字。

---

## 4. 仓库里各文件是干什么的

| 文件 | 要不要手改 | 说明 |
|------|------------|------|
| `端侧模型部署.md` | **要，主线讲义** | 唯一主线原稿 |
| `JD能力缺口补全.md` | **要，JD 讲义** | 唯一 JD 原稿 |
| `更新网页.py` | 不要 | 一键重建 + 推送 |
| `build_mindmap_tree.py` 等 | 一般不要 | 被 `更新网页.py` 调用 |
| `index.html` | 不要手改讲义 JSON | 生成结果，给网站用 |
| `端侧部署思维导图.html` | 不要 | 与 `index.html` 同步的另一入口 |
| `mindmap-tree.json` | 不要 | 主线树的中间文件 |
| `jd-gap-tree.json` | 不要 | JD 树的中间文件 |
| `README.md` | 可改简介 | 仓库说明 |

`更新网页.py` 会把上表里网站需要的文件写到 GitHub 仓库 `RiddleGo/edge-llm-mindmap` 的 `main` 分支。GitHub Pages 再从 `main` 发布。

---

## 5. 换电脑检查清单

- [ ] 已装 Python、Git、`gh`
- [ ] `gh auth status` 显示已登录且是有写权限的账号
- [ ] 已 `cd` 进这份工程根目录
- [ ] 只改了 `.md`，保存了
- [ ] 跑过 `python 更新网页.py` 且没有报错
- [ ] 等一两分钟后强制刷新网页，点开改过的节点

---

## 6. 常见问题

**Q：能不能把 token 存进项目，换电脑直接一键？**  
不能存进仓库。公开/半公开仓库里的 token 会泄露。正确做法是每台电脑执行一次 `gh auth login`，登录信息只留在本机；之后 `python 更新网页.py` 就是一键。若脚本提示未登录，按它打印的命令登录即可。

**Q：跑脚本提示 `gh` 不是命令？**  
没装 GitHub CLI，或没进 PATH。重装 https://cli.github.com/ ，新开一个终端。

**Q：提示 API 失败 / Bad credentials？**  
`gh auth login` 再登一次。确认当前账号能改 `RiddleGo/edge-llm-mindmap`。

**Q：脚本成功了，网页还是旧的？**  
Pages 有缓存。等几分钟，用无痕窗口或 Ctrl+F5。看错「主线 / JD」页签也会以为没更新。

**Q：我只改了 JD，要不要跑整套？**  
跑 `更新网页.py` 即可，它两个树都会重建。只多几十秒。

**Q：标题变成了 `1.1.1 · …`，我在 md 里并没有写编号？**  
正常。编号是生成时加上的讲课序号，你在 md 里写中文标题就行。

**Q：想删掉某一整节？**  
在 md 里删掉那个 `###` 及其下面正文，再运行 `更新网页.py`。

**Q：Python 报找不到文件？**  
当前目录不对。先 `cd` 到含有 `端侧模型部署.md` 的文件夹再跑。

**Q：公司网络 git 推送失败？**  
本仓库的 `更新网页.py` 走的是 `gh api` 传文件，一般不依赖 `git push`。仍须能访问 `api.github.com`。

---

## 7. 建议的工作习惯

1. 先在 md 里改一小段，跑一次 `更新网页.py`，确认网页对得上，再大批量改。  
2. 一段讲义尽量 3～7 个短段，空行分开，右侧更好读。  
3. 拒收口径、验收尺用 `**加粗**`。  
4. 不要同时用两台电脑改同一份 md 还不发布：后推送的会盖住先推送的。改完就发布。

---

## 8. 一句话流程

打开 md → 改字并保存 → 在工程目录执行 `python 更新网页.py` → 过两分钟刷新网站。
