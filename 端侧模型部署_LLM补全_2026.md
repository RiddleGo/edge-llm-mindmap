---

# 🧩 ⑬ LLM/VLM 端侧补全 · 2024–2026

> 补原文缺口：选型、新量化、推理算法、Tokenizer、RAG、LoRA、VLM进阶、采样安全、内存公式、路径对比。  
> **2026-09-15 增补**：对照 `D:\\推理框架\\学习地图.md`（⑤量稳交账 · ⑥大模型专线 · ⑦交付量产 · ④工具链），补 **⑬-12～⑬-23**（指标 / Profiling / 对齐 / Serving·共板 / 端云·OTA·温区 / 长上下文 / 工具调用 / RAG深化 / VLM时序 / KV组合 / 任务门禁 / 选型趋势）。  
> 基准日：2026-09-15。不编造未公开实测；公式与数量级可落地。尺子回指学习地图与主文档 ③⑦⑧⑪⑭⑮。

---

## 🧭 ⑬-0 勘误（相对原文不精确/过时处）

#### ⚠️ 显存/内存公式口径
- 原文多处写 `2×layers×heads×d×seq×dtype`：`heads` 须为 **KV 头数**（GQA 的 `num_kv_heads`），不是 Q 头。
- LLaMA-7B「约 512KB/token」对 **MHA + FP16** 量级大致成立；对 **GQA 模型（如 Qwen2.5-7B）** 应按 `num_kv_heads` 重算，通常显著更小。
- 「7B/seq4096 约 2GB」仅在特定 MHA/FP16 假设下成立；GQA+INT8 KV 时数量级常降到数百 MB 级，部署前用公式重算。

#### ⚠️ 量化档位与硬件
- 「H100 用 FP8 / Ampere 无 FP8」方向对；端侧 Orin=Ampere 仍以 **W4A16** 为主。
- 「FP4(E2M1)…Blackwell/Hexagon 原生」需拆开：**NVFP4** 偏 NVIDIA Blackwell；**MXFP4** 为 microscaling 族，部分 NPU/新卡逐步支持；骁龙 HTP 以厂商文档/算子表为准，勿默认「凡 FP4 皆原生」。
- 「W4A8 甜区」易误导：端侧 **GPU/llama.cpp** 常见甜区是 **W4A16**；**W4A8** 需激活 INT8/FP8 路径与 kernel，多数手机 NPU 未必就绪。

#### ⚠️ 投机采样与加速数字
- 「实测常见 1.5×~3×」为云侧/带宽未饱和常见区间，**非端侧保证**；端侧常因双模型内存、接受率低而净收益≈0。
- Medusa/Lookahead 在 TRT-LLM 有开关；端侧量产需单独评估草稿头内存与接受率，勿照搬桌面数字。

#### ⚠️ 引擎与平台表述
- 「vLLM 依赖 CUDA 不上端侧」大体成立；边缘可用 **TRT Edge-LLM / llama.cpp / ExecuTorch / QNN**，勿把 vLLM 当车机主路径。
- 「Q4_K_M ~300MB」仅对应 **很小参数量**（约亚十亿级）量级感；**7B Q4_K_M 通常 ~3.5–4.5 GiB 级**，勿与体积表混用。
- 词表「Qwen 省 30–50% token」依赖语种与任务，中文场景常更省，勿写成全局定律。

#### ⚠️ VLM
- 「ViT-L/14 @384² → 256 visual token」仅为一种配置示例；动态分辨率/AnyRes/pixel shuffle 下 token 数可变，应按实际 preprocessor 统计。

---

## 🎯 ⑬-1 2024–2026 端侧 SLM / 小 VLM 选型

### 📦 选型三维（先筛再测）
#### 内存墙 → 带宽墙 → 体验墙
- 内存：权重+KV+视觉编码器+runtime 是否装进可用 DRAM（常留 ≥20%）。
- 带宽：decode ≈ 每 token 扫一遍权重；INT4 权重是端侧默认杠杆。
- 体验：TTFT、稳态 tokens/s、长上下文是否掉帧/降频。

### 🧬 文本 SLM 家族速览（端侧视角）
#### Qwen2.5 / Qwen3 小尺寸
- 0.5B–3B：手机/车机助手主力候选；7B：旗舰 SoC + INT4 + 短上下文。
- GQA + 大词表：中文 token 效率高，但 **embedding/lm_head 内存** 不可忽视。
- Qwen3：注意思考/非思考模式与 chat template 差异，端侧要钉死模板版本。

#### Llama 3.2（1B/3B 等）
- Meta 明确面向端侧的小尺寸线；生态（GGUF/ExecuTorch/MLX）成熟。
- 英文强、多语言看词表与数据配比；许可证与商用条款需单独过审。

#### Phi（小参数高密度）
- 参数少、合成/精选数据路线；适合工具调用、短上下文问答。
- 长上下文与多语言需实测；端侧常作 **draft 模型** 或垂直助手。

#### Gemma（2B 级等）
- Google 小模型线；注意版本间架构/词表变更。
- 与 Android/AICore 生态近，车规外场景可评估。

#### MiniCPM 等国产小模型
- 面向端侧优化的系列常见；关注量化友好度与中文对话一致性。
- 选型以 **同硬件对照基线** 为准，勿只看参数量。

### 🖼️ 小尺寸 VLM
#### InternVL / Qwen-VL / MiniCPM-V 小档
- 端侧优先：**视觉编码器可 INT8 + 语言塔 W4A16** 的组合。
- 视觉 token 数决定 TTFT 与 KV；选「支持 token 压缩/动态分辨率」的版本更友好。
- OCR/单据场景：分辨率与视觉塔精度优先于语言塔再压比特。

### 🧠 端侧 MoE：小专家是否可行
#### 可行条件
- **激活参数**小（如每 token 只激活数个专家）→ 算力友好。
- **总权重**仍大 → 内存/Flash 驻留压力大；端侧常需专家卸载或只部署稠密小模型。
- 路由导致专家切换 → 缓存局部性差，decode 带宽更伤。

#### 工程结论（数量级思维）
- 手机/车机 **并发=1、内存紧张**：优先稠密 1.5B–3B INT4。
- MoE 仅当：总权重可放进存储层级、运行时只驻留激活专家、或云端协同。
- 「小专家数 × 大总参」不等于端侧友好；看 **常驻内存** 而非 FLOPs 宣传。

### ✅ 选型清单（可直接进评审）
- 任务：闲聊 / 工具调用 / RAG / OCR-VLM / 多语。
- 硬约束：峰值 RAM、DDR 带宽、NPU 算子表、温控功耗墙。
- 软件约束：chat template、工具 JSON schema、安全兜底。
- 基线模型：同尺寸 2–3 个候选 + 同一量化档 + 同一 prompt 集。

---

## 🧮 ⑬-2 量化新格式与端侧真实约束

### 📦 GGUF：K-quant 之外的 IQ-quant（I-quant）
#### K-quant 回顾
- 超块/子块层次 scale，Q4_K_M 仍是通用默认甜点（兼容性+速度可预期）。

#### IQ-quant 要点
- 更激进比特分配 + **importance matrix（imatrix）** 校准，常见如 IQ4_XS、IQ3_*、IQ2_*。
- 同体积往往 **更省空间或同空间更高保真**；代价：解量化/kernel 更复杂，部分后端 **prefill/decode 更慢或支持不齐**。
- 工程建议：量产默认 Q4_K_M/Q5_K_M；内存极紧再试 IQ4_XS，并用 **本机 llama-bench + 任务集** 验收，勿只看 PPL。

### 🔷 FP8 / FP4 族：MXFP4 · NVFP4 · FP4 · FP8
#### FP8
- E4M3/E5M2 等；云侧 Hopper+ 成熟；端侧 Orin 无原生 FP8 Tensor Core，勿当默认。
- 作激活/KV 时，关注是否有对应 GEMM/attention 路径。

#### NVFP4
- NVIDIA 侧 FP4 方案（偏 Blackwell 代际）；与 TRT-LLM 量化表绑定，Ampere 边缘卡通常不可用。

#### MXFP4（Microscaling）
- 块内 FP4 值 + 多级 scale（microscaling）；部分新 NPU/框架出现 W4A8_MXFP 类路径。
- 端侧采用前先查：**芯片算子是否支持、scale 布局是否匹配、校准工具链是否闭环**。

#### 落地口诀
- 有啥算力用啥格式；**格式≠精度保证**，无 kernel 就等于软件解量化回高位，带宽收益蒸发。

### 🖤 BitNet / 1.58-bit
#### 概念
- 权重极值低比特（含三值/1.58bit 路线），靠专用或高度优化内核换带宽。

#### 端侧现实
- 需模型本身按该范式训练/蒸馏，不是任意 LLM 一键 PTQ。
- 无专用内核时，解码可能不升反降；列为 **预研项**，非 2026 车机默认量产路径。

### 🧪 算法族：HQQ · QuaRot · SpinQuant · AQLM · QQQ
#### HQQ
- 快速 PTQ、校准开销相对低；适合快速出 INT4 候选，再上任务集精筛。

#### QuaRot / SpinQuant
- 用旋转/正交变换改善量化友好性（压低异常值难度）；常与权重/激活量化组合。
- 部署要保证 **推理侧旋转融合进权重或在线旋转可融合**，否则多一次大矩阵。

#### AQLM
- 加性码本类极低比特，压缩狠；端侧解码内核与工程链不如 GPTQ/AWQ/GGUF 普及。

#### QQQ 等
- 权重-激活联合或特定 W4A8 配方的研究/工程变体；选型看目标硬件是否吃 W4A8。

#### 实用优先级（端侧）
1. AWQ/GPTQ → GGUF K-quant（CPU/GPU 通用）
2. 需更压体积 → IQ-quant + imatrix
3. 芯片原生 FP8/FP4 → 跟厂商配方
4. 旋转类/AQLM/BitNet → 有内核与精度闭环再上

### ⚖️ W4A16 vs W4A8 vs W8A8：NPU/GPU 真实约束
#### W4A16（权重 4bit，激活 16bit）
- **端侧 GPU / llama.cpp 主流**：降权重带宽，激活保持 FP16/BF16，实现简单。
- Decode 仍 memory-bound，但收益稳定可预期。

#### W8A8
- 权重+激活都 INT8：算力墙场景（大 prefill、有 INT8 Tensor/NPU）更香。
- 激活离群值敏感；常配 SmoothQuant/旋转/敏感层回退。

#### W4A8
- 理论兼顾权重带宽与激活算力；需要 **W4A8 GEMM** 与激活量化在线开销可控。
- 许多端侧 NPU **只高效支持 W8A8 或 weight-only**；无路径时退化为 W4 解量化 + A16/A8，收益打折。

#### 选型表（文字）
- 手机 CPU/GPU 通用推理：优先 W4A16。
- 高通 HTP/强 INT8 NPU + 大 prefill：评估 W8A8。
- 宣称 W4A8：先跑算子支持矩阵，再谈精度。
- KV 量化与权重量化正交：单独开关、单独回归。

---

## ⚡ ⑬-3 推理算法补全（端侧视角）

### 🧱 Chunked Prefill
#### 动机
- 超长 prompt 一次性 prefill 峰值激活/临时缓冲爆炸；切成 chunk 限制峰值。

#### 端侧要点
- 降峰值内存，可能略增总 prefill 时间；车机并发=1 时主要为了 **防 OOM/防抖动**。
- 与 paged KV 搭配：边算边写入 KV block。

### 🗄️ Prefix Caching / Prompt Cache
#### 前缀缓存
- 相同 system/工具前缀的 KV 块复用，多会话或多轮命中时省 prefill。

#### Prompt cache（引擎侧）
- 对整段 prompt 哈希命中则跳过重复计算；端侧会话少时收益看前缀是否稳定。

#### 端侧限制
- 缓存本身占 RAM；前缀一变（时间戳、用户名、动态政策）即失效。
- 安全：缓存键须包含 **模板版本+LoRA id+采样无关的前缀字节**。

### 🃏 Speculative Decoding 端侧限制
#### 双模型内存
- draft + target 同时驻留；1.5B+7B INT4 可能直接打穿手机预算。
- 自推测/Medusa 头可减轻「第二套完整权重」，但仍占激活与额外头参数。

#### 接受率与带宽
- 端侧 decode 已极 memory-bound；接受率低时验证开销浪费带宽。
- 草稿过强/分布不匹配 → 频繁拒绝；温度高、任务开放域 → 接受率下降。

### 🦅 Medusa / EAGLE / Lookahead
#### Medusa
- 在目标模型上挂多头并行猜多 token；省独立 draft 权重，改头训练/导出链。

#### EAGLE 类
- 用特征层/草稿头做更准的多步猜测；实现与导出复杂，端侧需确认引擎支持。

#### Lookahead
- 窗口/雅可比式多候选验证；TRT-LLM 等有模式开关，端侧要量内存与图固定 shape。

#### 落地建议
- 先保证单模型 INT4 达标；再试点 **无第二权重** 的头式投机；最后才上双模型。

### 🧭 Constrained Decoding / Structured Output
#### 用途
- JSON/工具调用/正则文法约束，降低乱格式。

#### 端侧成本
- 每步掩码 logits 或 FSM 前进：CPU 开销与实现复杂度上升。
- 与投机采样组合困难：草稿 token 可能违反约束，接受逻辑更复杂。

#### 实践
- 工具调用优先：**JSON schema 约束 + 校验重试**；约束解码作增强而非唯一正确性来源。

### 🌡️ 投机采样与温度/采样冲突
#### 冲突点
- 验证阶段按目标分布接受/拒绝；高温度、高随机性 → 接受率↓。
- top-p/top-k/min-p 改变候选集，需与投机实现一致，否则分布不匹配。

#### 工程纪律
- 投机路径固定采样配置做接受率曲线；开放域闲聊高温度时，允许关闭投机。
- 不要假设「开了投机一定更快」。

---

## 🔤 ⑬-4 Tokenizer · Chat Template · Stop Token 端侧坑

### 🧩 Tokenizer 与词表内存
#### 内存构成
- `vocab` 字符串/合并规则 + embedding 表 +（常另计）lm_head。
- 大词表（如 150K+）：embedding≈`vocab×hidden×dtype`；INT4 主权重省下的内存可被词表吃回。

#### 多语言 BPE
- 中文友好词表 → 同句 tokens 更少 → prefill 更短、KV 更小。
- 多语混杂：稀有语种被切碎 → 序列变长，端侧体感「同模型变慢」。

### 📜 Chat Template
#### 必钉死
- 与训练一致的 `chat_template`（含 special tokens、role 标记、思考链开关）。
- 服务端改模板、端侧未改 → 拒答、乱角色、工具调用失败，常被误判为「量化坏了」。

#### 版本管理
- 模板哈希进模型包 manifest；OTA 时模板与权重同版本发布。

### 🛑 Stop Token / 特殊符号
#### 常见坑
- 只停 `EOS` 不停工具结束符 → 多说话或截断工具参数。
- 多 EOS / 用户自定义 stop 与字节级 BPE 边界不一致 → 漏停。
- VLM：图像占位 token 数量必须与视觉塔输出一致，否则错位生成。

#### 检查单
- encode→decode 幂等抽样；特殊 token id 表打印进日志；对比 HF `apply_chat_template` 字节级一致。

---

## 📚 ⑬-5 端侧 RAG

### 🧲 Embedding 模型量化
#### 原则
- 向量维度与召回指标（Recall@k）优先于极限压缩。
- INT8 常可接受；INT4 需看召回跌幅，重要域用 FP16 embedding + INT4 LLM。

#### 运行时
- embedding 多为短前向，NPU/CPU 均可；与 LLM **分进程/分会话加载** 可削峰。

### 🗄️ 向量库（端侧）
#### sqlite-vec 等
- 嵌入式向量检索：随 SQLite 走，运维简单，适合万～十万级向量。
- 大语料仍需分片、倒排/产品量化（PQ）或云端检索 + 端侧生成。

#### 工程要点
- 预计算向量落盘；查询延迟计入 TTFT 预算。
- 元数据过滤（车型/地区/权限）先于 ANN，减少无效召回。

### 🔎 Rerank 是否上端
#### 建议
- 轻量交叉编码器可上端，但延迟与内存叠在 LLM 前。
- 默认策略：**端侧 bi-encoder 召回 Top-k（小 k）→ 规则/轻重排 → LLM**；重型 rerank 放云或省略。
- 车机：宁可召回略糙 + 强 system 约束，也不要三次大模型前向。

---

## 🔌 ⑬-6 LoRA / Adapter 与前缀共享

### 🔥 LoRA 热切换
#### 内存
- 每套 LoRA：约 `Σ 2×rank×(in+out)` 量级（视插层数）；多客制化 = 多份常驻或来回加载。

#### 热切换成本
- 换 adapter：权重合并或运行时注入；合并进基模简单但切换慢；运行时多 LoRA 要内核支持。

#### 多 LoRA
- 端侧建议：**同时常驻 ≤1–2 套**；其余落盘按场景加载。
- 与量化基座对齐：LoRA 训练所用基座比特/模版须一致。

### 🧵 System Prompt 前缀共享
#### KV 前缀
- 相同 system + 工具定义 → 共享 KV prefix block，多会话摊销 prefill。

#### 与 LoRA 交互
- 不同 LoRA 下同前缀 **不可随意共享 KV**（分布已变）；缓存键含 `lora_id`。

#### 实践
- 把稳定长前缀与易变用户话切开；稳定段尽量静态，提升命中率。

---

## 🖼️ ⑬-7 VLM 部署进阶

### 🗜️ Visual Token 压缩
#### Pixel shuffle / 合并
- 空间合并降 token 数，换一定细节损失；TTFT/KV 直接受益。

#### Token prune
- 推理期丢弃低信息视觉 token；需模型/模块支持，简单乱剪会伤 OCR。

### 📐 动态分辨率
#### 行为
- 按原图比例分块/缩放，token 数随图变 → **动态 shape 与内存峰值**。

#### 端侧策略
- 设上限：最长边、最大 tile 数、最大 visual tokens。
- 编译引擎：固定上限 shape + 运行时有效长度，避免无限涨。

### 🎬 视频多帧
#### 成本
- 帧数 × 每帧 visual tokens → prefill 与 KV 近似线性涨。

#### 策略
- 关键帧采样 + 低帧率；帧间共用语言上下文；必要时帧特征离线缓存。
- 实时视频助手：优先「短窗帧差/稀疏帧」，勿满帧进 LLM。

### 🔤 OCR VLM 差异化量化
#### 策略
- 视觉塔/连接器：偏 INT8 或更高；语言塔：W4A16。
- 读字场景：分辨率与视觉精度 > 语言塔再抠比特。
- 回归集必须含小字/倾斜/表格；仅用通用 VQA 会漏伤。

---

## 🎲 ⑬-8 采样与安全兜底

### 🎛️ 采样参数（端侧常用）
#### Repetition penalty
- 抑制套娃重复；过大伤语言流畅、过小长文重复。助手场景常见轻微 >1。

#### min-p
- 相对峰值概率设地板，滤掉长尾噪声；比单纯 top-p 更稳的情况存在，需与模板联调。

#### XTC 等
- 激进多样性策略；端侧产品默认慎用，先 A/B 再开。

#### 与量化关系
- 低比特使 logits 变「钝」；同样温度可能更胡言或更重复。量化后 **重标定温度/penalty**，勿沿用 FP16 参数。

### 🛡️ 端侧安全兜底
#### 分层
1. 规则：关键词/正则/拒答模板/工具白名单。
2. 小分类器：意图/有害/越狱二分类（可 INT8 小模型）。
3. LLM：仅在前两层通过后生成。

#### 车规思维
- LLM 非 ASIL 功能安全通道；安全相关动作必须规则/控制器闭环，模型只建议。

### 🌀 幻觉与量化
#### 机制
- 量化增噪声 → 事实密集、长尾实体更易胡说；RAG 可部分对冲。

#### 缓解
- 降温度、强约束格式、引用检索片段、敏感层保留高精度、避免过度 IQ/2bit。
- 评估：事实问答+拒答集，不只报 PPL。

---

## 🧮 ⑬-9 完整内存公式与典型预算表

### 📐 分项公式（并发=1）
#### 权重
- `W ≈ params × bytes_per_weight`
- INT4 常按 ≈0.5 B/param，另加 scale/码本开销（约数%～十几% 量级，视格式）。

#### 激活峰值
- 与 batch、hidden、中间 FFN 膨胀、是否 chunked prefill 相关；取 profile 峰值，勿只估一层。

#### KV Cache
- `KV = 2 × L × H_kv × D_h × S × bytes_kv`
- `S = 已用 token（含视觉 token）`；GQA 用 `H_kv`。

#### Runtime / 引擎
- 工作区、算子临时缓冲、内存池碎片；经验预留常需真机标定，量产留安全余量。

#### Tokenizer
- 词表结构 + 预分配字符串；相对 7B 较小，但嵌入式勿忽略。

#### Vision encoder（VLM）
- ViT 权重 + 视觉前向激活峰 + 投影层；可与 LLM **分阶段加载** 削峰。

#### 总式
- `Mem ≈ W + KV(S) + Act_peak + Runtime + Tok + Vision(+ LoRA)`
- 可用内存再 ×0.8 作安全系数（温控/系统服务/图形）。

### 📊 数量级示意表（并发=1，INT4 权重，文本模型）

> 仅为数量级尺：假设 GQA、KV 用 FP16、无视觉、未含极端长上下文。真机以公式+profile 为准。

#### 1.5B INT4
- 权重：约 0.8–1.2 GiB 级
- KV@2k：通常百 MB 内量级（视层数/头）
- 手机：较舒适；车机入门助手常见档

#### 3B INT4
- 权重：约 1.6–2.5 GiB 级
- KV@2k–4k：数百 MB 量级可能
- 旗舰手机/座舱中档；需盯峰值激活与 runtime

#### 7B INT4
- 权重：约 3.5–5 GiB 级
- KV@4k：数百 MB～1 GiB+ 量级可能
- 仅高内存座舱/边缘盒；常需短上下文、KV INT8、关双模型投机

### 🚗 车机/手机预算思维
- 先定 `S_max` 与是否 VLM，再反推可装参数量。
- 系统服务 + 相机 + HMI 常占数 GB；LLM 只拿「剩余池」。
- 并发=1：优化 TTFT/TPOT 与温控，而非 continuous batching 吞吐。

---

## 🛤️ ⑬-10 路径对比：ExecuTorch · QNN HTP · MLX · llama.cpp

### 文字对比表

| 维度 | ExecuTorch | QNN HTP（骁龙） | MLX | llama.cpp |
|------|------------|-----------------|-----|-----------|
| 主战场 | Android/iOS 端侧，PyTorch 导出链 | 骁龙 NPU/HTP 量产 | Apple Silicon（Mac/iPhone 生态） | 跨平台 CPU/GPU，GGUF 生态 |
| 模型入口 | `torch.export` / 官方 LLM 流程 | ONNX→转换→context binary | 原生 MLX 权重/转换 | HF→GGUF→quantize |
| 量化 | 权重量化+后端委托；跟版本强相关 | AIMET/HTP 约束，INT8/混合精度看算子 | 苹果侧量化与统一内存友好 | K/IQ-quant 最丰富，迭代快 |
| LLM 特性 | 移动委托、逐步完善 KV/生成 API | 需按芯片 SDK 集成自回归与 KV | 统一内存，研发迭代快 | KV、投机、采样、多后端成熟 |
| 性能杠杆 | XNNPACK/CoreML/NPU 委托 | HTP 算力+带宽，图编译与量化对齐 | Metal 与统一内存 | 手工 kernel + 后端 offload |
| 量产适配 | 移动 App 友好；车规看客户基线 | 车机/手机高通方案主候选 | 非苹果硬件不适用 | 产线需自管性能/精度/安全 |
| 代价 | 算子覆盖与委托坑 | 工具链版本钉死、算子 fallback | 生态绑 Apple | 极致性能需平台相关调参 |
| 何时选 | 要 PyTorch 同源上移动 | 骁龙量产、NPU 必吃满 | 苹果设备演示/产品 | 多平台基线、CPU 兜底、快速量化实验 |

### 选型口诀
- 高通车机/手机：**QNN HTP 为主，llama.cpp 作精度/对照基线**。
- 跨平台快验、桌面/盒子集：**llama.cpp**。
- Android 原生产品化：**ExecuTorch** 评估委托与延迟。
- 仅 Apple：**MLX**。
- NVIDIA 边缘：**TRT-LLM / Edge-LLM**（原文已覆盖，上表作互补）。

### 🔗 与原文 / 学习地图衔接
- 量化数学、GPTQ/AWQ、FlashAttention、TRT-LLM、QNN 基础流程见主文档 ③⑦⑧⑪。
- 本章前半（⑬-0～⑪）补：**选型、新格式、算法边界、Tokenizer/RAG/LoRA、VLM 压缩、采样安全、内存总账、路径表**。
- 本章后半（⑬-12～⑬-23）对齐学习地图：**⑤ 指标·Profiling·对齐**、**⑥ Prefill/Decode·VLM**、**⑦ OTA·温区·门禁**、**④ Serving/共板**；不抢主文档 ⑭ Genie 编排与 ⑯ IR 细节。
- ⑬-19 / ⑬-20 是对 ⑬-5 / ⑬-7 的工程深化；⑬-23 补强 ⑬-1 选型趋势。

---

## ✅ ⑬-11 并入检查清单（工程用）

- [ ] 选型：任务×内存×词表×许可证（对照 ⑬-1 / ⑬-23）
- [ ] 量化：格式有内核？W4A16/W8A8/W4A8 何者真加速？
- [ ] 算法：chunked prefill / prefix cache 键设计 / 投机是否装得下
- [ ] 模板：chat template + stop 与训练一致（对齐第 0 站，⑬-14）
- [ ] RAG：embedding 位宽 + 切块/混合检索 + sqlite-vec 规模 + rerank 去留（⑬-5 / ⑬-19）
- [ ] LoRA：常驻套数 + 前缀缓存键含 lora_id
- [ ] VLM：max visual tokens + 串联时序 + OCR 差异化量化（⑬-7 / ⑬-20）
- [ ] 采样：量化后重标定；规则+小分类器兜底
- [ ] 内存：分项公式 + KV 量化档 + 20% 余量 + 真机 profile（⑬-9 / ⑬-21）
- [ ] 路径：QNN / ExecuTorch / MLX / llama.cpp 职责清晰
- [ ] 指标：TTFT/TPOT/P95 口径含前后处理；热/冷启动分开报（⑬-12）
- [ ] Profiling：Prefill vs Decode 分层后再调（⑬-13）
- [ ] 共板：常驻/忙时峰值/撤台三列账；LLM 不得堵关键路径（⑬-15）
- [ ] OTA：权重+tokenizer+template+引擎 **整组**；温区稳态 tokens/s（⑬-16）
- [ ] 工具调用：schema 约束 + 白名单执行 + JSON 合法率门禁（⑬-18）
- [ ] CI：导出→量化→编译→**任务集**四站；勿只看 PPL（⑬-22）


---

# 📐 增补篇 · 对齐学习地图（⑬-12～⑬-23）

> **增补说明**：长上下文 / RoPE 外推 / 滑窗 → 工具调用链 → RAG 切块·混合检索（深于 ⑬-5）→ VLM 串联时序与异构流水（深于 ⑬-7）→ KV 量化 × Paged/Prefix → LLM 任务级量化与 CI 四站 → 选型趋势对照（补 ⑬-1）；并补齐学习地图 **⑤指标·Profiling·对齐 / ④Serving·共板 / ⑦OTA·温区**。  
> 尺子回主文档：量化 ③⑦；Prefill/Decode·KV ⑧；量产链路 ⑪；平台产物 ⑭；门禁 / OTA / 安全 ⑮。数字只给公式与数量级，不编未公开实测。

---

## 📊 ⑬-12 LLM 端到端指标（TTFT / TPOT）

> 对齐学习地图 **⑤ 量稳交账 · 端到端指标**。CV 的 FPS 不能直接搬来；LLM 必须按 **Prefill / Decode 两段** 立尺。平台命令、Genie 编排见主文档 **⑭**，此处只定口径。

### 🎯 原则
#### 量用户体感，不是 NPU 忙闲
- 时钟从 **请求进入推理热路径** 起，到 **首个可展示 token** 止 = **TTFT**；相邻 token 间隔 = **ITL / TPOT**。
- 端到端必须含：分词、采样、detokenize、上屏。预处理/后处理不进尺子 → 板上绿、产品骂。
- 端侧默认 **并发=1**。云侧「加大 batch 换吞吐」在座舱常是反模式：P50 绿，P99 与壳温一起爆。
- **P50 看体验中枢，P95/P99 看拒收**。稳态 tokens/s 只在功耗墙/温区内测，空调房峰值不当门禁。

#### 口径（定义用，非承诺数字）
- `TTFT ≈ T_tokenize + T_prefill(+视觉编码) + T_first_sample + T_detok_first`
- `TPOT ≈ median(ITL)`；`tokens/s ≈ 1 / mean(ITL)`（须声明是否含首 token）
- 报表必注：`prompt_tokens`、`gen_tokens`、`S_max`、量化档、壳温、热/冷启动

### ✅ 检查清单 / 口径表

| 指标 | 起表 | 停表 | 端侧用途 | 勿混用 |
|------|------|------|----------|--------|
| **TTFT** | 请求入推理进程 | 首 token 可展示 | 「第一口」 | 勿用纯 Prefill kernel 时间顶替 |
| **ITL/TPOT** | 上一 token 完成 | 下一 token 完成 | 跟读流畅度 | 勿把含停顿的会话平均当稳态 |
| **tokens/s** | 稳态 Decode 窗 | 同窗 | 温区/功耗回归 | 勿把 Prefill 摊进去报快 |
| **P95/P99** | 同场景多次 | 同左 | 量产门禁 | 勿只报一次 best-of |
| **KV 占用** | 公式见 **⑬-9** | — | 内存墙 | 勿用 MHA 公式套 GQA |

- [ ] Prompt 集冻结：短/中/长三档，**token 数写入报告**（勿用「一句话」）
- [ ] 热启动与冷启动分开报；量产 SLO 通常盯热路径
- [ ] VLM：视觉 token 计入 prompt 长度；OCR 图另列
- [ ] RAG：检索延迟进 TTFT 预算（**⑬-5 / ⑬-19**），或明确「生成段另计」
- [ ] 与学习地图⑤同一纪律：尺子含前后处理，否则不算交账

### 💥 常见翻车点
- **停表点漂移**：有的栈停在「首 logit」，有的停在「首 UTF-8 字」；中文一字多 token，体感差一截。
- **Prompt 长度没钉死**：演示数十 token 全绿，车上 system+工具前缀数百 token，TTFT 变差属预期。
- **Decode 报吞吐、用户骂首包**：优化全砸在权重带宽，Prefill 大 GEMM 没吃满加速器。
- **P50 门禁、现场 P99**：热降频、相机抢带宽只出现在尾部；空调房十次平均无意义。
- **数字不可比**：跨机型/跨 SDK 未对齐量化档与 `S`，只可谈数量级，**绝对值需核验**。

---

## 🔍 ⑬-13 Prefill / Decode 分层 Profiling

> 对齐学习地图 **⑤ Profiling** + **⑥ Prefill·Decode**。先测稳，再分层；**两段墙拧反是第一大浪费**。融合/IR Pass 归主文档 **⑯**，此处只分层归因。

### 🎯 原则
#### 先分阶段，再分层次
- **Prefill**：整段 prompt 一次（或 chunked）前向 → 更像 **算力墙**，决定 TTFT。
- **Decode**：每步扫权重 + 读写 KV → 更像 **带宽墙**，决定 TPOT。
- 顺序与学习地图⑤同一棵树：**稳态基线 → 阶段耗时 → 算子 Top-K → 访存/带宽 → 利用率/降频**。
- 端侧并发=1 时，不必先上 Continuous Batching；先问慢在算满还是搬满。

#### 分层看什么
1. **阶段**：Prefill ms vs 单步 Decode ms × 生成长度。长会话里 Decode 累计常更长；用户「第一口」几乎全是 Prefill。
2. **算子**：Attention / GEMM / RMSNorm / RoPE / 采样。Top-K 占比 **需核验**，勿套云侧「Attention 一定过半」。
3. **访存**：权重扫描、KV 读写、中间写回（FlashAttention 类收益见主文档⑧，不在此展开 kernel）。
4. **系统**：CPU 分词、Host–Device 拷贝、核间切换、温控时钟。

### ✅ 检查清单 / 分层表

| 层 | 问什么 | 绿的样子（定性） | 下一步 |
|----|--------|------------------|--------|
| 测稳 | 同 prompt、同温、同亲和性，多次可重复 | P50/P99 收得住 | 无基线不谈优化 |
| 阶段 | Prefill 是否吃掉 TTFT | 短 prompt 时 Decode 总时长可超过 Prefill | 长前缀先砍模板/视觉 token |
| 算子 | 是否单核热点 | 无长期霸榜且无融合机会 | 热点再考虑混精/切 backend（**⑯**） |
| 访存 | Decode 是否带宽打满 | 权重 INT4 后 TPOT 明显动 | 无动则查解量化回高位、KV dtype |
| 利用率 | NPU 忙但端到端慢 | Prefill 高、Decode 低较常见 | Decode 别再堆 FLOPs 宣传 |
| 热 | 数分钟后是否掉 tokens/s | 稳态仍过 SLO | 见 **⑬-16**，勿只优化 kernel |

- [ ] Profiling 开关本身有扰动：对比「关采集」的 TTFT/TPOT
- [ ] Chunked Prefill：峰值内存降、总 Prefill 可能升，分开记账
- [ ] Prefix Cache：命中 / 未命中两套 TTFT
- [ ] 工具与产物格式跟栈走；**命令抄主文档⑭，此处不展开**

### 💥 常见翻车点
- **两段混着调**：给 Decode 加更大 batch、给 Prefill 狂压 KV bit，墙对不上。
- **没有稳态**：首次加载、时钟爬升混进平均。
- **只看算子表**：分词在 CPU、拷贝在总线，被误判「模型太大」。
- **开了融合勾仍慢**：中间表写回/未吃到加速器 → 查访存层；**IR 白名单见⑯**。
- **用云侧 Profile 套车机**：vLLM 排队延迟在端侧常不存在。

---

## 🎯 ⑬-14 精度对齐三站 + chat template

> 对齐学习地图 **⑤ 精度对齐三站**（训练输出 → 导出图 → 板上产物）。LLM 多一刀：**模板与分词必须进第 0 站**。词表内存 / stop 细则见 **⑬-4**，此处只写对齐协议。

### 🎯 原则
#### 先对齐「喂进去的字节」，再对齐「算出来的数」
- 板上胡言，优先怀疑 **chat template / special tokens / 思考模式开关**，再怀疑量化。
- 三站必须走 **同一条 greedy 路径**（temperature=0、固定 seed、关闭投机）才能比 logits。
- 站与站之间只改「图从哪来」，不改预处理、模板、tokenizer 版本。
- 任务级（拒答率、工具 JSON 合法率）与张量级（余弦、top-1）分开报，勿用 PPL 单点放行。

#### LLM 三站（对照 CV 的 训练→ONNX→OM）

| 站 | 产物 | 对齐物 | 通过线（定性） |
|----|------|--------|----------------|
| **0 模板** | `chat_template` + tokenizer | 与训练/SFT **字节级**一致 | `apply_chat_template` 哈希钉死 |
| **1 框架** | HF/PyTorch greedy | 参考 token 序列 + 可选首步 logits | 黄金 prompt 集可复现 |
| **2 导出图** | ONNX / `torch.export` 等 | 同输入下 logits / top-1 | 漂了先查导出与 RoPE/mask |
| **3 板上** | OM / context / GGUF 等 | 同 greedy 输出 | 再谈 PTQ/AWQ 敏感层 |

### ✅ 检查清单
- [ ] 模板版本、tokenizer、LoRA id、思考/非思考开关写入 manifest（OTA 同包，见 **⑬-16**）
- [ ] 黄金集：短问答 / 工具调用 / 拒答 / 长前缀；禁止现场随口一句当对齐
- [ ] `eval` + `no_grad` + 固定采样；对齐阶段关闭投机与约束解码
- [ ] 张量级：首步或前 K 步 logits 余弦 / top-1；任务级：字符串或 JSON schema
- [ ] VLM：图像占位 token 数 = 视觉塔输出长度（**⑬-4 / ⑬-7 / ⑬-20**）
- [ ] 端侧常 batch=1；若捆 batch，对照学习地图⑤「多批数值一致」
- [ ] 量化后重标定温度/penalty（**⑬-8**）；对齐阶段仍用 greedy

### 💥 常见翻车点
- **服务端改模板、端侧未改**：角色错乱、工具失败，工单写成「INT4 坏了」。
- **stop 只认 EOS**：工具结束符漏停，「多说话」被当成精度回归。
- **用聊天采样对比三站**：温度一开，token 级对不上是分布问题，不是图转坏。
- **导出站过了、板上漂**：预处理、布局、RoPE 实现差；**不要立刻回炉训练**。
- **GQA / KV dtype 与参考不一致**：短句尚可、长上下文开始胡说 → 先核 **⑬-0 / ⑬-9**。

---

## 🧩 ⑬-15 端侧 Serving 边界与共板资源账

> 对齐学习地图 **④ 工具链 · Serving / 共板资源账**。MindIE / vLLM 的连续批处理 **不自动等于** 车机/手机进程内编排。Genie JSON、ctx-bins 清单见主文档 **⑭**，此处只画边界与账本。

### 🎯 原则
#### Serving 是调度边界，不是「再包一层 HTTP」
- 端侧 LLM 常见形态：**单进程、并发=1、会话内流式 decode**；队列是「拒收/降级」，不是把多路拼成一个 batch。
- 云侧目标 = 吞吐与加速器利用率；端侧目标 = **TTFT/TPOT + 峰值 RSS + 温区**（尺子见 **⑬-12**）。
- 共板：KWS / ASR / 检测 / HMI 与 LLM **抢同一份 DRAM 与 DDR**，不是各报各的「单模能跑」。
- 账本三列：**常驻、忙时峰值、撤台**。只报常驻 → 夜里多模一起炸。

#### 边界怎么切

| 能力 | 端侧默认 | 厚边/云 | 别做的 |
|------|----------|---------|--------|
| 连续批处理 | 通常关 | 主杠杆 | 用云侧并发模型估车机 QPS |
| Prefix Cache | 稳前缀可开；键含模板/LoRA | 多租户命中率高 | 键不含模板版本 |
| 投机解码 | 先过内存账（**⑬-3 / ⑬-9**） | 更常见 | 双模型直接常驻 |
| HTTP Serving | 仅盒子系统可选 | 标配 | 座舱关键路径再套一层网关抖动 |
| 多模型 | 分时/优先级 | 多实例 | 峰值相加当「还够」 |

### ✅ 检查清单 / 共板账
- [ ] 常驻：LLM 权重 + KV(`S_max`) + Runtime + 词表（**⑬-9**）
- [ ] 忙时：ASR / 相机 / 视觉塔 / LLM Prefill 峰值是否 **同时** 出现
- [ ] 撤台：会话结束是否释放 KV；切账号/切 LoRA 是否卸峰值
- [ ] 优先级：唤醒词 > 识别 > 生成；LLM 不得堵住倒车影像/仪表
- [ ] 队列：超时是降级到云/本地模板，还是让用户空等
- [ ] 就绪：模型已 load ≠ 首包已热；对外「可对话」探针要对热路径
- [ ] 余量：系统+HMI 先扣，LLM 拿剩余池，再留安全系数（**⑬-9**）

### 💥 常见翻车点
- **单模绿、共板红**：LLM Decode 扫权重时 ASR 正在搬特征，带宽墙叠加。
- **KV 不撤台**：多轮 RSS 只涨不跌，被当成泄漏。
- **把 vLLM 队列语义搬进车机**：P99 变成排队时间，用户感知是死机。
- **Serving 与 HMI 抢加速器**：演示关地图全绿，量产开导航掉 tokens/s。
- **探针只 ping 端口**：图未 load、模板未挂，灰度已切流。

---

## 🚚 ⑬-16 端云切分 · OTA 整包 · 车规温区对 decode

> 对齐学习地图 **⑦ 交付量产**（云边发布、OTA 整组、车规温区）。ISO / OTA SOP 细则见主文档 **⑮**；本节只把 **LLM decode** 接到这三道闸。

### 🎯 原则
#### 先定验收尺，再定哪段留端
- 留端：低延迟、可离线、隐私；回云：长上下文、重工具、低电量/高温降级。
- 切分键：**延迟 SLO、隐私级、电量/温区、任务类型**，禁止按模型名字拍脑袋。
- OTA 对 LLM 必须 **整组**：权重 + tokenizer + chat template + 引擎/so + manifest。只更权重 = 隐蔽失败。
- 温区打的是 **Decode 带宽与时钟**：室温 tokens/s 是甜点；主机厂温区（常见叙述约 −40℃～85℃，**以规范为准、需核验**）才是正餐。

#### Decode 为何怕热
- Decode 本就是带宽墙；降频 → 有效带宽/时钟一起掉 → **TPOT 先坏**；长 prompt 时 Prefill/TTFT 随后伤。
- 门禁用 **热稳态** 的 TPOT/P99，不是上电数十秒的峰值。

### ✅ 检查清单 / 对照表

| 闸 | LLM 要冻住什么 | 回滚带走什么 |
|----|----------------|--------------|
| **端云切** | 本机最大 `S`、是否允许上云、脱敏规则 | 切流开关 + 云端模型 id |
| **OTA 整包** | 权重指纹、模板哈希、tokenizer、Runtime 列车版本 | A/B **整组**；禁止旧模板+新权重 |
| **温区** | 壳温点、持续 decode 时长、功耗墙 | 降级：降 `S`、关 VLM、切云 |
| **安全** | 验签、防回滚、许可证（**⑮**） | 旧包不能开新模型 |

- [ ] 金丝雀：先短 prompt 助手，再放开工具调用 / VLM
- [ ] 回滚演练：整包哈希对得上，且能聊、能停（stop 仍对，**⑬-14** 第 0 站）
- [ ] 温区：至少室温 + 高温稳态；低温同样测时钟/带宽（以实测为准）
- [ ] 功耗：盯持续 decode 墙，不是 Prefill 瞬时尖峰单独过
- [ ] 批次：同指纹多板抽测 greedy 黄金集（**⑬-14**）
- [ ] 切云：超时/过热/OOM 的用户话术与隐私开关

### 💥 常见翻车点
- **全端或全云**：无网死寂，或事事上云把 TTFT 打穿。
- **OTA 只推权重**：tokenizer / 模板留旧版 → 第 0 站失配，像「量化坏了」。
- **引擎 so 与模型解耦失败**：列车版本混用（细节 **⑭**）；表现常是随机崩溃，不像精度问题。
- **室温 SLO 上车**：开导航 + 高温 derating，Decode 结巴；用户骂模型，其实是温区。
- **LLM 当功能安全通道**：生成式只建议；执行必须规则/控制器闭环（**⑬-8 / ⑮**）。

---

## 🧵 ⑬-17 长上下文 · RoPE 外推 · 滑动窗口

### 🎯 先分清三件事
- **位置外推**：训练长度 \(S_{\mathrm{train}}\) 不够用，推理想到 \(S_{\mathrm{infer}}\)。
- **滑动窗口**：注意力只看最近 \(W\) token，KV 常驻可封顶在 \(W\)。
- **KV 预算**：公式见 ⑬-9；GQA 用 `num_kv_heads`，不是 Q 头（⑬-0）。

主文档 ⑨ 已写 RoPE 原理。本节只谈端侧会炸的工程点。学习地图 ⑥：Prefill 偏算力墙，Decode 偏带宽墙——超长 prompt 先砍峰值与常驻，再谈「支持 128K」。

### 📐 RoPE 外推（NTK / YaRN）
#### 常见手法
- **NTK-aware**：把 `base` 放大，高频少扭曲。
- **YaRN**：分段插值 + 外推，常再乘注意力温度；编译图多一次 Mul，可能打掉 RoPE fusion（⑯ 同类坑）。
- **线性插值**：简单，长距相对位置更糊，只作对照基线。

#### 端侧纪律
- `rope_theta`、`rope_scaling` 类型与因子进 **manifest**，与权重、chat template 同版本发布（⑪ 指纹 / ⑮ OTA 整组）。
- 无对应 kernel 时外推只是「能生成」，Decode 仍按 ⑬-9 扫 KV；宣称上下文 ≠ 板上可驻上下文。
- `position_ids` 必须与 **已写入 KV 的长度** 一致；滑窗丢块后仍用绝对 pos，错位会重复/乱码，常被误判为量化坏了（对照 ⑬-4 / ⑬-14）。

### 🪟 滑动窗口
- 只对 \([t-W,t]\) 做注意力；旧 KV block 可释放。与 ⑬-3 **chunked prefill 正交**：chunked 管激活峰值，滑窗管 KV 常驻。
- 因果 mask 与 sliding mask **模板只认一种**；混用即静默错答。
- 长文档场景：滑窗 + 摘要/RAG（⑬-19）往往比硬外推更可交板。

### ✅ 速查
- 先算 `KV(S_max)` 与温控 Decode，再开外推。
- 回归集必须含：短上下文、\(S_{\mathrm{train}}\) 边界、\(S_{\max}\) 三条；只绿短 prompt 是假绿（接 ⑬-22）。

---

## 🧰 ⑬-18 工具调用 / Function Calling 工程链

相对 ⑬-4（模板 / stop）、⑬-3（约束解码）、⑬-8（白名单）：收成一条可交付链，而不是「模型会吐 JSON」。

### 🔗 六站（端侧默认串行）
1. **Schema 进前缀**：工具名、参数 JSON Schema、禁止项写入稳定 system（可吃 Prefix Cache，⑬-3/⑬-6）。
2. **模板钉死**：`tool` / `tool_call` / 结束符与训练一致；哈希进包（⑬-4）。
3. **生成**：schema 约束解码 **或** 先自由生成再校验；端侧重试次数极小，优先约束。
4. **校验**：缺字段 / 类型错 / 枚举外 → 拒答或一次修复，禁止死循环。
5. **执行**：仅 **白名单**；安全相关动作走规则/控制器，模型只建议（⑮）。
6. **Observation 回填** 再 Decode；并行多工具易叠 TTFT，量产默认串行。

### ⚠️ 与推理算法的冲突
- 约束解码每步掩码 logits，CPU 开销上升；与投机采样叠加时草稿易违规——端侧先关投机，再保 JSON 合法率。
- stop 只停 EOS、不停工具结束符 → 多说话或截断参数（⑬-4）。
- 量化后 logits 变钝（⑬-8）：同样温度更易出坏 JSON；工具路径 **greedy 或低温**，温度在任务集上重标定。

### 🧪 验收（不要用闲聊 PPL）
- JSON 合法率、必填字段命中、禁工具调用率、端到端「选对工具」。
- Schema 版本与权重绑同一 digest（⑪）；OTA 禁止「只更工具描述、不更模板」。

---

## 📚 ⑬-19 RAG 切块 · 混合检索 · 端侧评测

**相对 ⑬-5 的深化**：⑬-5 管 embedding 位宽、sqlite-vec 规模、rerank 去留。本节补 **切块策略、词法+向量混合、评测协议**。

### ✂️ 切块
- 按标题 / 句界 / 空行切；代码与表格禁止固定字符硬切。
- `chunk_size` 与 `overlap` 直接决定注入 token 数 → Prefill 与 `KV(S)`（⑬-9）。
- **父子块**：检索用短 chunk，注入 LLM 用父块，省 Decode 上下文。
- 端侧语料常万～十万级：向量 **预计算落盘**；查询延迟计入 TTFT（⑬-12）。

### 🧲 混合检索
- 词法（SQLite FTS / BM25）抓专有名词、零件号；向量抓同义改写。
- 融合优先 **RRF**，少上重型交叉编码器——车机避免「召回 + 重排大模型 + 生成」三次前向。
- **元数据先过滤**（车型 / 地区 / 权限）再 ANN，减少无效召回，也减少越权注入（⑮）。

### 🧪 端侧评测（任务级）
| 层 | 看什么 | 说明 |
|----|--------|------|
| 检索 | Recall@k / nDCG@k | 切块与 embedding 量化会伤召回；INT4 embedding 必须看跌幅 |
| 生成 | 引用可核对率、拒答率 | 量化 LLM 事实题更易漂（⑬-8），RAG 是对冲不是免检 |
| 体验 | TTFT 含检索 | 检索不进尺子 = 测速报喜 |

校准 / 评测语料 digest 进 CI（⑮）；换切块参数 = 换数据版本，与换权重同等对待。

---

## 👁️ ⑬-20 VLM 串联时序与异构流水线

**相对 ⑬-7 的深化**：⑬-7 管 visual token 压缩、动态分辨率、视频帧、OCR 混精。本节管 **端到端时序与 CPU/NPU 流水**（对齐学习地图 ② 异构 + ⑥ VLM）。

### ⏱️ 串联时序（并发=1）
相机/解码 → 缩放对齐 → **ViT 一次前向** → projector → 视觉 token 拼进 prompt → **LLM Prefill** → Decode。

- \(\mathrm{TTFT} \approx T_{\mathrm{视觉}} + T_{\mathrm{prefill}}(\text{含 visual tokens}) + T_{\mathrm{排队}}\)。
- 图像占位 token 数 **必须等于** 视觉塔输出个数（⑬-4）；错位会「杯子变碗」，不一定是量化。
- 视觉 token 计入 \(S\)，KV 公式与文本相同（⑬-9）。

### 🧩 异构流水
- 预处理：CPU / DVPP / HTP 矢量；ViT：NPU **INT8**；语言塔：W4A16 / HTP GEMM（⑭ Genie 多 binary 常见）。
- **分阶段加载**：ViT 激活峰与 LLM 峰值不要叠在同一帧（⑬-9）；共板峰值按加和验收（⑬-15）。
- 可重叠：下一帧 resize 与当前 Decode 并行；profiler 看的是端到端，不是单算子绿（⑬-13）。

### 📐 与 ⑬-7 的衔接（策略落点）
- 动态分辨率：钉 `最长边 / max_tiles / max_visual_tokens`；编译 **固定上限 shape** + 运行时有效长度（⑭ 动态轴敏感）。
- 视频：帧数 × 每帧 token ≈ 线性涨 KV；实时助手用短窗关键帧，满帧进 LLM 必顶内存。
- OCR：视觉塔 / 连接器偏 INT8，语言塔 W4A16；回归集必须含小字 / 倾斜 / 表格。

---

## 🧊 ⑬-21 KV 量化与 Paged / Prefix 组合

权重量化（③ / ⑬-2）与 KV 量化 **正交**：单独开关、单独回归（主文档⑧ 常默认不量化 KV）。

### 📐 再写一遍口径
\[
\mathrm{KV}=2\times L\times H_{\mathrm{kv}}\times D_h\times S\times \mathrm{bytes}_{kv}
\]
`bytes`：FP16=2，INT8=1，INT4≈0.5。无对应 Attention kernel 时，KV 量化只省存储，Decode 仍按高位算，带宽收益蒸发（同 ⑬-2「格式≠加速」）。

### 🧱 三件套怎么叠
| 杠杆 | 管什么 | 端侧注意 |
|------|--------|----------|
| GQA | 从结构瘦 \(H_{\mathrm{kv}}\) | 选型阶段就定（⑬-1） |
| Paged | 按 block 分配，消掉按 \(S_{\max}\) 预留的空洞 | block 大小以引擎为准；offload 有 host↔device 拉回税 |
| Prefix | 稳定 system/工具前缀锁块，摊销 Prefill | 键含 **模板版本 + LoRA id + KV 量化档**（⑬-3/⑬-6） |
| KV INT8/INT4 | 常驻与 Decode 带宽 | 长上下文任务集回归；勿照搬云侧掉点宣传 |

### ✅ 速查
- 组合口诀：**GQA 瘦结构 → Paged 管碎片 → Prefix 摊 Prefill → KV 量化管常驻**。
- 必须测：\(S_{\max}\) 满窗 + 前缀命中 / 未命中；短上下文绿不够。
- 内存余量仍按 ⑬-9 ×0.8，并进 ⑮ 峰值门禁。

---

## 🧪 ⑬-22 LLM 任务级量化验收与 CI 四站门禁

学习地图 / 主文档③ 的三把尺子：张量余弦 / 特征 / **任务**。LLM 的任务 **不是** 只有 PPL：助手看指令遵循，工具看 JSON（⑬-18），RAG 看引用（⑬-19），VLM 看 OCR / 物体名（⑬-20）。PPL 微降但工具全碎 = 假绿。

### 🚪 LLM 版四站（对照学习地图 ⑦）
| 站 | 钉什么 | 失败常见因 |
|----|--------|------------|
| 1 导出 | ONNX / `torch.export` / GGUF 指纹、opset、**chat template 哈希** | 模板漂、动态轴、RoPE 布局 |
| 2 量化 | 校准集 digest、AWQ/GPTQ/K-quant / IQ 配方（③⑦ / ⑬-2） | 校准不像线上话术 / 工具前缀 |
| 3 编译 | ATC / QNN context / TRT 版本列车（⑭） | Attention/RoPE 回落 CPU |
| 4 任务对比 | 固定 greedy 集：JSON / 引用 / OCR | 只比了 logits 或开放域闲聊 |

### 🎚️ 与主文档 ⑮ 门禁对齐
- L0 冒烟出 token；L1 同机 logits 余弦可严、跨平台必须放宽（bit-exact 几乎不可能）。
- **硬门禁 = 任务指标**；余弦只做软告警与归因。
- CI 采样：`temperature=0` / greedy；随机采样只做分布回归。
- 掉点修复阶梯：校准 → 混精 / 敏感层回退 → QAT → 改结构；工具/RAG 集要进校准分布。
- 车规：门禁绿 ≠ ASIL 过；LLM 仍非功能安全通道（⑮）。

---

## 📈 ⑬-23 2024–2026 选型与趋势对照（补 ⑬-1）

⑬-1 已给「内存墙 → 带宽墙 → 体验墙」与家族速览。本节补 **具名族 + 可用性口诀 + 年份对照**。下列名称会改版，**立项必须核验版本日与模型卡**（叙述基准日 2026-09-15）。

### 🧬 常见族（尺寸以卡片为准）
#### 文本 SLM
- **Qwen2.5 / Qwen3** 小档：中文词表效率高；钉思考/非思考模板（⑬-4）。
- **Llama 3.2** 1B/3B：端侧生态熟；许可证单独过审。
- **Phi-3.5 / Phi-4-mini**：短上下文与工具调用密；长文/多语要实测。
- **Gemma 2/3** 小档：近 Android 生态；架构变更看代际。
- **MiniCPM 系、SmolLM2、OpenELM、MobileLLM、InternLM 小档、Granite 小档**：作同尺寸对照基线，勿只看参数量。

#### 蒸馏小模型
- **DeepSeek-R1-Distill-Qwen**（1.5B/7B 等）：教师推理链会打穿 TTFT/KV；端侧默认关思考或限预算。
- **Llama / Phi 蒸馏线**：蒸馏 ≠ 端侧友好——看学生 **常驻体积 + 是否 GQA**，不看教师榜分。

#### 小 VLM
- **Qwen-VL 小档、InternVL2/2.5 小档、MiniCPM-V、SmolVLM、Gemma 多模态小档、Moondream / FastVLM / PaliGemma 类**。
- 11B 级视觉 Llama、大 MoE（总权重）不当手机默认；MoE 看 **常驻专家内存**（⑬-1）。

### 🧭 可用性口诀（评审可念）
**装得下、扫得动、有内核、钉模板、任务绿、证能过。**
- **装**：\(W+\mathrm{KV}+Vision+Runtime \le\) 剩余 DRAM×0.8（⑬-9）
- **扫**：Decode 每 token 扫权重；无 INT4/KV kernel 则假瘦（⑬-2 / ⑬-21）
- **核**：⑭ 算子表有 Attention / RoPE / 采样，禁止静默 CPU 回落
- **模**：chat template + 工具 Schema + stop（⑬-4/⑬-18）
- **绿**：⑬-22 任务四站，不是 PPL alone
- **证**：许可证 + 验签 + OTA 整组（⑮）

### 📅 趋势对照（方向，非 KPI）
| 年附近 | 端侧常见姿态 |
|--------|----------------|
| 2024 | 边缘盒 7B INT4；手机仍以 1–3B 试水 |
| 2025 | 座舱/旗舰手机 1–3B + 小 VLM；GQA、W4A16 成默认讨论基线 |
| 2026 | 新 NPU 的 FP8/MXFP4 **跟厂商配方**；工具调用 + RAG 成为交付项，而不只是会 chat |

选型公式仍是主文档 ⑭：**芯片代际 × 模型类型(CV/ASR/LLM/VLM) × 约束(功耗/内存/时延/ASIL)** → 引擎产物 → 版本列车。工具链名字会换，⑬-9 内存账和 ⑬-22 任务闸可带走。

---

## ✅ 增补篇收口清单

- [ ] ⑬-12：TTFT/TPOT 口径与学习地图⑤一致，含前后处理
- [ ] ⑬-13：Prefill/Decode 分层后再优化，不拧反墙
- [ ] ⑬-14：第 0 站模板哈希进 OTA 整组
- [ ] ⑬-15：共板三列账（常驻/峰值/撤台）过会
- [ ] ⑬-16：端云切分键 + 温区稳态 tokens/s
- [ ] ⑬-17：外推与滑窗先过 KV 预算
- [ ] ⑬-18：工具链六站 + JSON 合法率门禁
- [ ] ⑬-19：切块/混合检索/评测协议深于 ⑬-5
- [ ] ⑬-20：VLM 时序与异构削峰深于 ⑬-7
- [ ] ⑬-21：GQA→Paged→Prefix→KV 量化组合口诀
- [ ] ⑬-22：CI 四站硬门禁=任务指标
- [ ] ⑬-23：选型口诀六字过评审；版本日需核验
